# Plan 19：会员列表、详情、标签与等级

> PRD 章节：4.2.8+D2.4+E7.1

## 任务清单
- [x] 后台会员分页列表含头像、昵称、手机号、注册时间、订单数、消费总额。
- [x] 会员详情含地址、订单历史和等级（收藏在 plan-23 服务化）。
- [x] 实现普通/会员两级等级和可审计更新入口。
- [x] 实现用户标签覆盖、去重和查询。
- [x] 列表完整分页，手机号默认脱敏。
- [x] 增加 RBAC、幂等写、OpenAPI、shared-types 和 Admin 页面。

## 验收标准
- [x] 会员数据来自数据库，不使用演示回退。

## 主 Agent 验收记录

- 后端：会员字段迁移、分页/详情/标签/等级 API 已接入，ruff/mypy/pytest 通过。
- Admin：会员列表与详情页已接入集中路由、React Query 和防抖写操作；类型、lint、测试、构建通过。
