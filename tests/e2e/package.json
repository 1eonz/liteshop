{
  "name": "@liteshop/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "pnpm --dir ../../packages/shared-types build && pnpm --dir ../../packages/shared-components build",
    "test": "playwright test"
  },
  "devDependencies": {"@playwright/test": "^1.62.1"}
}
