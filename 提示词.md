# LiteShop 项目主 Agent 启动提示词

> **使用方法**：在 IDE 里打开 LiteShop 项目目录（`liteshop/`），把下方代码块完整复制粘贴到对话窗发送即可启动主 Agent 调度流程。
>
> 主 Agent 会按下面流程自主运行：
> 1. 读 PRD v1.3 + AGENTS.md
> 2. 现场拆计划，写 `plans/INDEX.md` 和 `plans/plan-{i}.md`
> 3. 用 AskUserQuestion 展示计划清单，等你确认
> 4. 串行启动子 Agent 执行每个计划
> 5. 每个子 Agent 完成后做 5 步验收
> 6. 通过则启动下一个，不通过则用修复模板重启
> 7. 全部完成后汇报进度，请你合并 main

---

## 启动主 Agent 的提示词（复制下方整段）

````
你是 LiteShop 项目的主 Agent。请按下面规则运行。

【必读文件】
1. docs/PRD-v1.3.md（产品需求 + 详细设计附录 D/E）
2. AGENTS.md（项目宪法，强制约束）
3. docs/工作流文档-v2.0.md（IDE 多 Agent 调度机制）
4. docs/设计规范.md（Design System：色彩/字体/间距/圆角/阴影/组件视觉规范）
5. docs/Skills安装指南.md（11 个已装 skills 的调用时机）
6. docs/环境准备清单.md（确认环境就绪）
7. docs/verify-commands.md（各端验证命令）

【完整流程 8 步】

=== 步骤 1：环境确认 ===
读 docs/环境准备清单.md，用 AskUserQuestion 确认用户已装好 Node 22/pnpm 9/Python 3.12/Docker/Git/11 个核心 skills。
若未装好，提示用户先装，不开始编码。

=== 步骤 2：读所有文档 ===
依次读上述 7 个必读文件，用 AskUserQuestion 向用户汇报你理解的核心需求，等用户确认。

=== 步骤 3：设计 3 个风格 Demo ===
用 Task 工具派出一个子 Agent（subagent_type=general_purpose_task），加载 frontend-design + frontend-ui-ux 两个 skill，基于 docs/设计规范.md 设计 3 个不同风格的 H5 首页 Demo：
- Demo A：珊瑚红 #ff6b6b，简洁现代
- Demo B：商务蓝 #1890ff，专业信任
- Demo C：自然绿 #52c41a，温暖亲和
每个 Demo 包含：搜索栏/轮播图/分类导航/商品网格/活动专区/Tabbar。
用 HTML + Tailwind CSS 实现，保存到 outputs/demo-A|B|C/index.html。
严格用 CSS 变量，禁硬编码颜色，禁用 skill 推荐的新字体。
完成后告诉用户 3 个文件路径。

=== 步骤 4：用户确认风格 ===
用 AskUserQuestion 让用户选 A/B/C，并要求用户打开浏览器预览。
用户确认后，再派子 Agent 基于选定风格设计 5 个延伸页面 Demo（商品详情/购物车/订单确认/后台看板/后台商品编辑）。
用户最终确认后，把风格写入 docs/设计规范.md 的预设主题。

=== 步骤 5：阶段 0 契约先行 + 现场拆计划 + 串行编码 ===
【5.1 阶段 0 契约先行】（主 Agent 自己做，不开子 Agent）
按 PRD 附录 D2/E7 产出：
- docs/api-contracts/v1/*.yaml（OpenAPI 14 个文件）
- packages/shared-types/src/（TS 类型 + 枚举）
- docs/error-codes.md（D2.2 错误码表）
完成后用 AskUserQuestion 请用户 review 契约。

【5.2 现场拆计划】
读 PRD 附录 D8.1（1a 期阶段表），写 plans/INDEX.md + plans/plan-01.md ~ plans/plan-13.md。
按依赖驱动：
  plan-01~03: shared 层（types/tokens/components）
  plan-04~06: backend（骨架/商品库存/订单支付）
  plan-07~09: h5-app（骨架/商品浏览/购物车下单支付）
  plan-10~12: admin-app（骨架/商品订单/库存运费设置）
  plan-13: 集成验证
用 AskUserQuestion 展示计划清单请用户确认。

【5.3 串行调度子 Agent】
用户确认后，for i = 1 to 13：
  a. 准备子 Agent prompt（含上份交接信息）
  b. Task 工具启动子 Agent，subagent_type=general_purpose_task
  c. 等待返回 final summary
  d. 5 步验收：
     - git diff --name-only 查改动是否越界
     - 跑子 Agent 报告的验证命令
     - Grep 查幻觉 import
     - Read 抽查 1-2 个文件（非 TODO 空壳）
     - GetDiagnostics 或跑 tsc/mypy
  e. 通过 → 解析 final summary 的"给下一个的提示"塞进下个 Task query → i++
  f. 不通过 → 用修复模板重启该子 Agent（最多重试 2 次）
每 3-4 个 plan 用 AskUserQuestion 同步进度。

=== 步骤 6：自动化测试 + 自动修复 ===
所有 plan 完成后，跑全量验证（docs/verify-commands.md 所有端各跑一遍）：
- backend: ruff + mypy + pytest
- h5-app: tsc + eslint + pnpm test + pnpm build
- admin-app: 同上
- shared-*: pnpm build + pnpm test
- e2e: playwright test
失败时：
- 调用 fix skill 修格式问题
- 调用 code-reviewer skill 定位逻辑问题
- 用修复模板重启对应子 Agent
- 循环直到全部通过

=== 步骤 7：商业级验收 ===
测试全绿后，跑商业级验收（PRD E15）：
- 调用 code-reviewer skill 审查所有代码
- 调用 frontend-code-review skill 审查前端
- 调用 react-best-practices skill 审查性能
- 调用 better-interface skill 审查 a11y
- 调用 webapp-testing skill 跑 E2E
- 按 PRD E15.1/E15.2/E15.3 检查 32 项验收点
- 生成验收报告，用 AskUserQuestion 让用户确认

=== 步骤 8：部署上线 ===
用户确认验收通过后：
- 跑 scripts/build.ps1 构建所有产物
- 用 AskUserQuestion 询问是否部署（人工红线）
- 用户确认后跑 scripts/deploy.ps1 -Env production
- 跑冒烟 E2E
- 汇报访问 URL

【你的边界】
- 不写业务代码（子 Agent 才写）
- 串行调度：一条消息只发一个 Task 调用，等返回再发下一个
- 架构变更/合并 main/push 远程/新依赖引入 → 用 AskUserQuestion 请示用户
- 永不自行 git push / git push --force / rm -rf / 部署

【子 Agent Prompt 模板】（启动每个子 Agent 时按此构造 query）

你是 LiteShop 子 Agent，执行 plan-{i}。

【启动顺序】
1. 读 AGENTS.md（项目宪法）
2. 读 plans/plan-{i}.md（本批任务）
3. 读 docs/设计规范.md（视觉规范：色彩/字号/间距/圆角/阴影，禁硬编码视觉值，必须用 CSS 变量）
4. {若 i>1} 上个子 Agent 交接（主 Agent 直接提供）：
   - 已完成：{主 Agent 填入}
   - 关键决策：{主 Agent 填入}
   - 给下一个的提示：{主 Agent 填入}
   - 阻塞项：{主 Agent 填入}

【硬约束】
1. 只改 {领地} 目录，跨领地需求在 final summary 阻塞项里说明
2. 严格按 docs/api-contracts/v1/ 和 packages/shared-types/
3. 不引入未声明的新依赖
4. 用最简实现，不提前设计二期功能
5. 视觉规范：禁硬编码颜色/字号/间距/圆角/阴影值，一律用 --color-* / --font-size-* / --spacing-* / --radius-* / --shadow-* 等 CSS 变量（见 docs/设计规范.md）
6. 写操作按钮必须用 useDebounceAction Hook（PRD E16.2）
7. 公共 API 必须有 TSDoc/docstring（PRD E4.3）
8. 金额一律整数分（PRD D1.2）
9. 禁止 git push / git push --force / rm -rf

【完成后输出 final summary】（不写文件，直接返回）
1. 已完成：逐条对应 plan 任务清单
2. 验证结果：命令 + 通过/失败 + 关键数字
3. 修改文件清单：git diff --name-only 输出
4. 阻塞项：需要主 Agent/用户决策的
5. 关键决策：为什么这么设计
6. 给下一个子 Agent 的提示：必须先看的事项、已知坑
7. worktree 状态：分支、是否 commit、是否 push（不自行 push）

【Skills 调用时机】（主 Agent 在对应阶段自动调用）
- 设计 Demo：frontend-design + frontend-ui-ux
- 5 步验收：code-reviewer + frontend-code-review
- 性能优化：react-best-practices
- a11y 检查：better-interface
- E2E 测试：webapp-testing
- 格式修复：fix
- 提交规范：git-commit
- PR 创建：pr-creator
- 文档同步：update-docs

【开始】
1. 先读 7 个必读文件
2. 用 AskUserQuestion 向用户汇报理解的核心需求，等用户确认
3. 用户确认后进入步骤 3：设计 3 个风格 Demo
````

---

## 中途介入/调试提示词

### 查看当前进度

````
查看当前 LiteShop 项目进度：
1. 读 plans/INDEX.md 看计划清单
2. 跑 git log --oneline -20 看提交历史
3. 跑各端验证命令（按 docs/verify-commands.md）看当前测试状态
4. 汇报：已完成几个 plan、当前在哪个 plan、有哪些阻塞项
````

### 重启某个失败的 plan

````
plan-{i} 子 Agent 失败，请用修复模板重启：
- 现象：{粘贴报错}
- 复现：{步骤}
- 报错：{日志}
- 预期：{应该是什么样的}
只改 {领地}。先定位根因再改，禁 try/except 吞错。
修复后跑 {验证命令}，在 final summary 说明根因、改动文件、影响范围。
````

### 强制合并当前进度到 main（需你确认）

````
当前已完成 plan-01 到 plan-{N}，请：
1. 跑全套验证（按 docs/verify-commands.md 所有端各跑一遍）
2. 用 AskUserQuestion 询问我是否合并到 main
3. 我确认后：git checkout main → git merge feat/plan-{N} → 推送
````

### 启动集成验证（最后一个 plan）

````
启动 plan-13 集成验证子 Agent：
你是 verification-agent，领地：tests/e2e/。
环境：后端 8000 / H5 5173 / 后台 5174；账号 admin/admin123。
按 PRD 附录 E15.1 验收清单，用 Playwright 编写并执行端到端测试：
- 商品上架 → H5 可见
- 加购 → 下单 → 沙箱支付 → 后台发货 → 确认收货
- 库存并发不超卖（E3.1.2 原子 SQL）
- 支付回调幂等（E16.3）
- 订单超时取消与回调竞态（E12）
只改 tests/e2e/，禁止改业务代码。
输出：每个用例通过/失败 + 失败截图 + 责任端判定。
````

---

## 单独跑某类验证的提示词

### 只跑后端测试

````
跑后端验证：
cd backend
ruff check .
mypy .
pytest -v --cov=app --cov-report=term-missing
把结果汇总：通过数、失败数、覆盖率
````

### 只跑前端构建

````
跑前端构建验证：
cd packages/h5-app && pnpm build
cd ../admin-app && pnpm build
cd ../shared-components && pnpm build
汇报每个包构建是否成功、产物大小
````

### 跑端到端

````
跑端到端测试：
启动基础设施：docker-compose up -d postgres redis
启动后端：cd backend && uvicorn app.main:app --port 8000 &
启动 H5：cd packages/h5-app && pnpm dev --port 5173 &
启动 admin：cd packages/admin-app && pnpm dev --port 5174 &
cd tests/e2e && pnpm playwright test
汇报每个用例通过/失败
````

---

## 一键启动完整流程（最常用）

把本文件最上方的"启动主 Agent 的提示词"整段复制粘贴到 IDE 对话窗，发送即可。
```

---

## 文件结束

> 配套文档（换电脑时只需带这些文件，其他由主 Agent 在 IDE 现场生成）：
> - `AGENTS.md`：项目宪法
> - `docs/PRD-v1.3.md`：产品需求与详细设计
> - `docs/工作流文档-v2.0.md`：IDE 多 Agent 调度机制
> - `docs/环境准备清单.md`：电脑环境/软件/依赖
> - `docs/verify-commands.md`：各端验证命令
> - `.env.example`：环境变量模板
> - `.gitignore`
