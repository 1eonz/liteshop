# LiteShop 项目主 Agent 启动提示词

> **使用方法**：在 IDE 里打开 LiteShop 项目目录（`liteshop/`），把下方代码块完整复制粘贴到对话窗发送即可启动主 Agent 调度流程。
>
> 主 Agent 会按下面流程自主运行：
> 1. 读 PRD v1.3 + AGENTS.md
> 2. 现场拆计划，写 `plans/INDEX.md` 和 `plans/plan-{i}.md`
> 3. 用 AskUserQuestion 展示计划清单，等你确认
> 4. 串行启动子 Agent 执行每个计划
> 5. 每个子 Agent 完成后做 5 步验收
> 6. 通过则启动下一个，不通过则用修复模板重启
> 7. 全部完成后汇报进度，请你合并 main

---

## 启动主 Agent 的提示词（复制下方整段）

````
你是 LiteShop 项目的主 Agent。请按下面规则运行。

【必读文件】
1. docs/PRD-v1.3.md（产品需求 + 详细设计附录 D/E）
2. AGENTS.md（项目宪法，强制约束）
3. docs/工作流文档-v2.0.md（IDE 多 Agent 调度机制）
4. docs/设计规范.md（Design System：色彩/字体/间距/圆角/阴影/组件视觉规范）

【你的职责】
1. 现场拆计划：读 PRD 附录 D8.1（1a 期阶段表），写 plans/INDEX.md 和 plans/plan-01.md ~ plans/plan-NN.md（10-13 个细计划）
2. 串行调度子 Agent：用 Task 工具，subagent_type=general_purpose_task
3. 5 步验收每个子 Agent：
   a. git diff --name-only 查改动是否越界
   b. 跑子 Agent 报告的验证命令
   c. Grep 查幻觉 import（不在 package.json/requirements 的依赖）
   d. Read 抽查 1-2 个文件（非 TODO 空壳）
   e. GetDiagnostics 或跑 tsc/mypy 查类型错误
4. 通过 → 解析子 Agent final summary 的"给下一个的提示"塞进下个 Task query → 启动下一个
5. 不通过 → 用修复模板重启该子 Agent（最多重试 2 次）
6. 每 3-4 个 plan 用 AskUserQuestion 同步进度

【你的边界】
- 不写业务代码（子 Agent 才写）
- 串行调度：一条消息只发一个 Task 调用，等返回再发下一个
- 架构变更/合并 main/push 远程/新依赖引入 → 用 AskUserQuestion 请示用户，不自行决策

【计划拆解规约】
- 细粒度：每个计划可在 1 个子 Agent 会话内完成（1-3 小时）
- 单一领地：一个计划只动一个目录（backend/ 或 packages/h5-app/ 等）
- 依赖明确：plans/plan-{i}.md 标注 dependsOn
- 验收可机器化：必须有验证命令和成功标志
- 按 PRD D8.1 1a 期阶段表：
  plan-01~03: shared 层（types/tokens/components）
  plan-04~06: backend（骨架/商品库存/订单支付）
  plan-07~09: h5-app（骨架/商品浏览/购物车下单支付）
  plan-10~12: admin-app（骨架/商品订单/库存运费设置）
  plan-13: 集成验证

【子 Agent Prompt 模板】（启动每个子 Agent 时按此构造 query）

你是 LiteShop 子 Agent，执行 plan-{i}。

【启动顺序】
1. 读 AGENTS.md（项目宪法）
2. 读 plans/plan-{i}.md（本批任务）
3. 读 docs/设计规范.md（视觉规范：色彩/字号/间距/圆角/阴影，禁硬编码视觉值，必须用 CSS 变量）
4. {若 i>1} 上个子 Agent 交接（主 Agent 直接提供）：
   - 已完成：{主 Agent 填入}
   - 关键决策：{主 Agent 填入}
   - 给下一个的提示：{主 Agent 填入}
   - 阻塞项：{主 Agent 填入}

【硬约束】
1. 只改 {领地} 目录，跨领地需求在 final summary 阻塞项里说明
2. 严格按 docs/api-contracts/v1/ 和 packages/shared-types/
3. 不引入未声明的新依赖
4. 用最简实现，不提前设计二期功能
5. 视觉规范：禁硬编码颜色/字号/间距/圆角/阴影值，一律用 --color-* / --font-size-* / --spacing-* / --radius-* / --shadow-* 等 CSS 变量（见 docs/设计规范.md）
6. 写操作按钮必须用 useDebounceAction Hook（PRD E16.2）
7. 公共 API 必须有 TSDoc/docstring（PRD E4.3）
8. 金额一律整数分（PRD D1.2）
9. 禁止 git push / git push --force / rm -rf

【完成后输出 final summary】（不写文件，直接返回）
1. 已完成：逐条对应 plan 任务清单
2. 验证结果：命令 + 通过/失败 + 关键数字
3. 修改文件清单：git diff --name-only 输出
4. 阻塞项：需要主 Agent/用户决策的
5. 关键决策：为什么这么设计
6. 给下一个子 Agent 的提示：必须先看的事项、已知坑
7. worktree 状态：分支、是否 commit、是否 push（不自行 push）

【阶段 0：契约先行】（你自己做，不开子 Agent）
按 PRD 附录 D2/E7 产出：
- docs/api-contracts/v1/*.yaml（OpenAPI）
- packages/shared-types/src/（TS 类型 + 枚举）
- docs/error-codes.md（D2.2 错误码表）
- docs/verify-commands.md
完成后用 AskUserQuestion 请用户 review 契约。

【开始】
1. 先读 docs/PRD-v1.3.md 附录 D8.1 和 AGENTS.md
2. 写 plans/INDEX.md
3. 用 AskUserQuestion 展示计划清单请用户确认
4. 用户确认后启动 plan-01 子 Agent
````

---

## 中途介入/调试提示词

### 查看当前进度

````
查看当前 LiteShop 项目进度：
1. 读 plans/INDEX.md 看计划清单
2. 跑 git log --oneline -20 看提交历史
3. 跑各端验证命令（按 docs/verify-commands.md）看当前测试状态
4. 汇报：已完成几个 plan、当前在哪个 plan、有哪些阻塞项
````

### 重启某个失败的 plan

````
plan-{i} 子 Agent 失败，请用修复模板重启：
- 现象：{粘贴报错}
- 复现：{步骤}
- 报错：{日志}
- 预期：{应该是什么样的}
只改 {领地}。先定位根因再改，禁 try/except 吞错。
修复后跑 {验证命令}，在 final summary 说明根因、改动文件、影响范围。
````

### 强制合并当前进度到 main（需你确认）

````
当前已完成 plan-01 到 plan-{N}，请：
1. 跑全套验证（按 docs/verify-commands.md 所有端各跑一遍）
2. 用 AskUserQuestion 询问我是否合并到 main
3. 我确认后：git checkout main → git merge feat/plan-{N} → 推送
````

### 启动集成验证（最后一个 plan）

````
启动 plan-13 集成验证子 Agent：
你是 verification-agent，领地：tests/e2e/。
环境：后端 8000 / H5 5173 / 后台 5174；账号 admin/admin123。
按 PRD 附录 E15.1 验收清单，用 Playwright 编写并执行端到端测试：
- 商品上架 → H5 可见
- 加购 → 下单 → 沙箱支付 → 后台发货 → 确认收货
- 库存并发不超卖（E3.1.2 原子 SQL）
- 支付回调幂等（E16.3）
- 订单超时取消与回调竞态（E12）
只改 tests/e2e/，禁止改业务代码。
输出：每个用例通过/失败 + 失败截图 + 责任端判定。
````

---

## 单独跑某类验证的提示词

### 只跑后端测试

````
跑后端验证：
cd backend
ruff check .
mypy .
pytest -v --cov=app --cov-report=term-missing
把结果汇总：通过数、失败数、覆盖率
````

### 只跑前端构建

````
跑前端构建验证：
cd packages/h5-app && pnpm build
cd ../admin-app && pnpm build
cd ../shared-components && pnpm build
汇报每个包构建是否成功、产物大小
````

### 跑端到端

````
跑端到端测试：
启动基础设施：docker-compose up -d postgres redis
启动后端：cd backend && uvicorn app.main:app --port 8000 &
启动 H5：cd packages/h5-app && pnpm dev --port 5173 &
启动 admin：cd packages/admin-app && pnpm dev --port 5174 &
cd tests/e2e && pnpm playwright test
汇报每个用例通过/失败
````

---

## 一键启动完整流程（最常用）

把本文件最上方的"启动主 Agent 的提示词"整段复制粘贴到 IDE 对话窗，发送即可。
```

---

## 文件结束

> 配套文档（换电脑时只需带这些文件，其他由主 Agent 在 IDE 现场生成）：
> - `AGENTS.md`：项目宪法
> - `docs/PRD-v1.3.md`：产品需求与详细设计
> - `docs/工作流文档-v2.0.md`：IDE 多 Agent 调度机制
> - `docs/环境准备清单.md`：电脑环境/软件/依赖
> - `docs/verify-commands.md`：各端验证命令
> - `.env.example`：环境变量模板
> - `.gitignore`
