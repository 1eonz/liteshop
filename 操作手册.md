# LiteShop 开发操作手册 v1.1

> 这是一份**给你本人看的操作指南**，告诉你每一步在 CodeX IDE 里具体做什么。
> 不是给 Agent 看的，是给你看的。
> 设计原则：**主 Agent 能自动化的全部自动化，你只需要复制粘贴 4 条指令 + 浏览器选风格 + 3 个人工 review 卡点。**
> 调度机制（5+1 步验收 / 批次划分 / 试点校准 / Skills 清单）的唯一权威是 **AGENTS.md §八**，本手册只保留可直接复制的指令文本，机制不在此重复。

---

## 目录

1. [第一步：Agent 自动安装 Skills + 初始化项目](#第一步agent-自动安装-skills--初始化项目)
2. [第二步：主 Agent 读取文档 + 设计 Demo + 你确认风格](#第二步主-agent-读取文档--设计-demo--你确认风格)
3. [第三步：契约先行 + 拆计划 + 试点校准 + 开发至 plan-06](#第三步契约先行--拆计划--试点校准--开发至-plan-06)
4. [第四步：人工 review 事务代码 + 放行全量开发](#第四步人工-review-事务代码--放行全量开发)

---

## 完整流程速查

```
第一步：1 条指令 → Agent 装 Skills（6 常驻 + 3 Demo 临时）+ 初始化项目骨架
  ↓
第二步：1 条指令 → Agent 读文档 + 设计 3 个 Demo → 你浏览器选风格 → 确认
  ↓
第三步：1 条指令 → 契约先行（你真读 OpenAPI review）
        → 现场拆计划（你确认）
        → 试点批 plan-01+04（你逐文件 review + 记三个数 → go/no-go）
        → 第二批 plan-05+06 → Agent 暂停
  ↓
第四步：你人工逐行 review plan-05/06 事务代码 → 1 条放行指令
        → 剩余 plan 每 3 个一批 → 全量测试 → 商业级验收 → 部署
  ↓
完成：LiteShop 上线，可商用
```

**你全程只需：复制粘贴 4 条指令 + 浏览器选 Demo + 3 个人工 review 卡点（契约 / 试点批 / plan-05/06 事务代码）。**

---

## 第一步：Agent 自动安装 Skills + 初始化项目

### 1.1 准备环境（一次性，你手动做）

按 `docs/环境准备清单.md` 准备好电脑环境（Node 22 / pnpm 9 / Python 3.12 / Docker / Git / CodeX IDE）。

### 1.2 你只需输入一条指令

打开 CodeX IDE → 打开 `liteshop/` 目录 → 在聊天窗口复制粘贴：

```
请读取 docs/环境准备清单.md 和 docs/Skills安装指南.md，自动完成以下全部初始化工作：

A. 安装常驻市场 Skills（5 个，常驻清单以 AGENTS.md §8.7 为准）：
   1. code-reviewer              # 代码审查：正确性/安全/性能，含前端规则
   2. fix                        # Prettier + Lint 自动修复格式/编译错误
   3. git-commit                 # Conventional Commits 规范提交
   4. impeccable                 # 设计质量：60 条 CI 确定性规则 + /audit /polish /harden
   5. chart-visualization        # ECharts 图表配置（仅 plan-12 数据看板用）

B. 安装 Demo 阶段临时 Skills（3 个，第二步风格确认后卸载）：
   6. frontend-design            # 从零生成明确视觉风格的界面
   7. frontend-ui-ux             # 微交互/间距/色彩优化
   8. better-interface           # a11y 可访问性审查（卸载后由 impeccable /audit 承接）

C. 安装 project-radar（通用全局 Skill，1 个文件所有项目共用）：
   9. 把项目内 .codex/skills/project-radar/SKILL.md 复制到 ~/.codex/skills/project-radar/SKILL.md 全局生效
     （project-radar：5 层识别项目类型 + 风险分析 + 需求覆盖检查 + 生成 project-context.md，
      是防重复/防幻觉/需求遗漏检查的核心机制，见 AGENTS.md §8.5）

D. 创建项目自定义 Skills（4 个）：
   10. 创建 .codex/skills/ 目录
   11. 创建 4 个项目自定义 Skills：
       - .codex/skills/liteshop-contract/SKILL.md（契约审查）
       - .codex/skills/liteshop-territory/SKILL.md（领地检查）
       - .codex/skills/liteshop-verify/SKILL.md（5+1 步验收）
       - .codex/skills/liteshop-style/SKILL.md（设计规范检查）

E. 生成项目骨架：
   12. 创建 package.json + pnpm-workspace.yaml + turbo.json
   13. 创建 docker-compose.yml（PostgreSQL 16 + Redis 7）
   14. 创建 .env.development（从 .env.example 复制，填入开发默认值）
   15. 创建 .nvmrc（22）+ runtime.txt（3.12）
   16. 创建 packages/ 目录结构（shared-types/tokens/components/3d-components/h5-app/site-app/admin-app）
   17. 创建 backend/ 目录结构（app/{api,core,models,schemas,services,repositories,tasks,enums,errors,main}.py + alembic + tests）
   18. 创建 tests/e2e/ 目录
   19. 创建 scripts/ 目录（setup-env.ps1/install.ps1/test.ps1/build.ps1/start-infra.ps1/deploy.ps1/verify-plan.ps1）

F. 初始化 Git 并启动基础设施：
   20. git init + git add . + 首次 commit "chore: initial project skeleton"
   21. docker compose up -d postgres redis
   22. 等待 PostgreSQL + Redis 健康检查通过

G. 验证：
   23. 跑 $list-skills 确认所有 Skill 已安装
   24. 跑 docker ps 确认 PostgreSQL + Redis 运行中
   25. 跑 pnpm --version 确认 pnpm 正常
   26. 跑 python --version 确认 Python 正常

完成后告诉我：
- 哪些 Skill 安装成功，哪些失败
- 创建了哪些文件
- 项目结构是否正确
- PostgreSQL + Redis 是否运行
```

### 1.3 Agent 自动完成

Agent 会自动执行全部 26 步，包括：
- 安装 5 个常驻市场 Skills + 3 个 Demo 临时 Skills
- 安装 project-radar 到全局（1 个全局 Skill，所有项目共用）
- 创建 4 个项目自定义 Skills
- 生成项目骨架（所有目录 + 配置文件）
- 初始化 Git 仓库
- 启动 PostgreSQL + Redis
- 验证环境

**你只需要等 Agent 汇报结果，确认无误即可。**

如果某个 Skill 安装失败，Agent 会告诉你，你可以在 CodeX 技能市场手动搜索安装。

---

## 第二步：主 Agent 读取文档 + 设计 Demo + 你确认风格

### 2.1 你只需输入一条指令

在 CodeX 聊天窗口复制粘贴：

```
请先读取以下所有项目文档，理解完整需求：

1. docs/PRD-v1.3.md（产品需求 + 详细设计附录 D/E）
2. AGENTS.md（项目宪法，强制约束）
3. docs/工作流文档-v2.1.md（IDE 调度特有内容，机制以 AGENTS.md §八 为准）
4. docs/设计规范.md（Design System：色彩/字体/间距/圆角/阴影/组件）
5. docs/Skills安装指南.md（Skills 调用时机，常驻清单以 AGENTS.md §8.7 为准）
6. docs/verify-commands.md（各端验证命令）
7. docs/CI-CD规范.md（CI 流水线 + 质量门禁）
8. docs/数据库迁移规范.md（Alembic 迁移命名/回滚）
9. docs/可观测性规范.md（监控指标 + 告警）
10. docs/部署发布规范.md（部署 + 回滚 SOP + 环境隔离）

读取完成后，告诉我你理解了哪些核心需求，包括：
- 一期 1a/1b 两阶段范围（§7.1 只取 [1a] 项拆计划）
- 三层库存模型 + 订单状态机
- 金额整数分 + 防抖幂等
- L0-L4 组件分层 + Design Tokens
- 主色 #ff6b6b + 5 套预设主题
- 6 个常驻 Skills 的调用时机

理解确认后，立即用 Task 工具派出一个子 Agent（subagent_type=general_purpose_task），
加载临时安装的 frontend-design + frontend-ui-ux 两个 Skill（Demo 阶段临时 Skill），
基于 docs/设计规范.md 设计 3 个不同风格的 H5 首页 Demo：

- Demo A：珊瑚红 #ff6b6b，简洁现代风，留白充足，强调商品图
- Demo B：商务蓝 #1890ff，专业信任感，卡片化布局
- Demo C：自然绿 #52c41a，温暖亲和，圆角更大

每个 Demo 包含：
- 顶部搜索栏（毛玻璃背景）
- 轮播图（占位图即可）
- 分类导航（8 宫格图标+文字）
- 商品网格（2 列，含价格/销量）
- 活动专区（图片广告位）
- 底部 Tabbar（首页/分类/购物车/我的）

用 HTML + Tailwind CSS 实现，可以直接在浏览器打开预览。
保存到 outputs/demo-A/index.html、outputs/demo-B/index.html、outputs/demo-C/index.html。
严格用 CSS 变量（--color-primary 等），禁硬编码颜色。
禁止使用 Skill 推荐的新字体/新配色，只用 docs/设计规范.md 定义的颜色。

完成后告诉我 3 个 Demo 的文件路径，我来打开预览选风格。
```

### 2.2 Agent 自动完成

Agent 会：
1. 读取上述 10 个文档
2. 向你汇报理解的核心需求
3. 你确认理解无误后，自动派子 Agent
4. 子 Agent 加载临时安装的 frontend-design + frontend-ui-ux Skill
5. 生成 3 个 HTML 文件
6. 告诉你路径

### 2.3 你打开浏览器选风格

在浏览器中打开：
- `outputs/demo-A/index.html`
- `outputs/demo-B/index.html`
- `outputs/demo-C/index.html`

选一个满意的风格，在 CodeX 聊天窗口输入（把 X 换成 A/B/C）：

```
我选择了 Demo X 的风格。

请基于这个风格，继续设计以下 5 个页面的 Demo：
1. 商品详情页（图片轮播 + 价格区 + SKU 选择抽屉 + 详情图）
2. 购物车页（商品列表 + 全选 + 合计 + 去结算）
3. 订单确认页（收货地址 + 商品清单 + 运费 + 提交按钮）
4. 后台数据看板（4 个指标卡 + 折线图 + 商品排行）
5. 后台商品编辑页（SPU 表单 + SKU 规格表格）

保存到 outputs/final-style/，我来确认。
```

### 2.4 你确认最终风格

打开 `outputs/final-style/` 预览，满意后输入：

```
风格确认。请把这个风格作为项目的视觉标准，更新 docs/设计规范.md 的预设主题部分。
然后卸载 3 个 Demo 阶段临时 Skills（frontend-design / frontend-ui-ux / better-interface），
常驻 Skills 保持 6 个不变（AGENTS.md §8.7）。
```

---

## 第三步：契约先行 + 拆计划 + 试点校准 + 开发至 plan-06

### 3.1 你只需输入一条指令

在 CodeX 聊天窗口复制粘贴：

```
风格已确认。现在开始开发 LiteShop 1a 期。按 AGENTS.md §8.3 批次调度执行：
完成第二批（plan-06）验收后必须暂停，等我人工 review 事务代码后放行；其余流程自主完成。

=== 步骤 5.0：项目雷达扫描（project-radar skill，通用全局 Skill）===
启动后第一时间调用 project-radar skill，按其 5 步核心流程执行
（扫描→分析→提示→需求覆盖检查→生成，完整定义见 AGENTS.md §8.5）：
- 产出 .codex/project-context.md（6 部分：代码索引+分层读指引+风险清单+工作流适配+MVP 清单+需求覆盖矩阵）
- MVP 清单只提取 PRD §7.1 标注 [1a] 的项（提取规则见 AGENTS.md §8.5.1）
- 风险与需求遗漏用 AskUserQuestion 提示我，只建议不擅改

**PRD 分层读（禁一次读全 PRD 25 万字符，3 层完整定义见 AGENTS.md §8.5.2）**

=== 阶段 0：契约先行（你自己做，不开子 Agent）===
按 PRD 附录 D2/E7 产出（第 2 层读，只读这些章节）：
- docs/api-contracts/v1/*.yaml（14 个 OpenAPI 文件）
- packages/shared-types/src/（TS 类型 + 枚举，按 D2.4）
- docs/error-codes.md（D2.2 错误码表）
完成后用 AskUserQuestion 请我 review 契约，并明确提示我"真读 OpenAPI，不要只点确认"——
重点核对：支付回调（验签/幂等键/金额校验/状态流转）、库存扣减（原子 SQL 条件/锁定/回滚路径）、
订单状态机（枚举值/非法流转拒绝）、金额全链路 integer 分、分页与错误码格式统一
（核对清单见 docs/工作流文档-v2.1.md §4.2）。
契约确认后，自己同步 docs/api-contracts/ 与 packages/shared-types/ 的一致性，
更新 .codex/project-context.md（shared-types 章节已可填充）。

=== 现场拆计划 ===
按 AGENTS.md §8.5.2 映射表写 plans/INDEX.md + plans/plan-01.md ~ plans/plan-13.md。
每个 plan 文件必须含**完整任务清单**（你已把 PRD 需求翻译好，子 Agent 只读 plan 不读 PRD），
顶部标注"PRD 章节：D1.1+E3.1"（对照 AGENTS.md §8.5.2 映射表）。
拆完 plans/ 后调用 project-radar 的需求覆盖检查（遗漏则 AskUserQuestion 提示补拆）。
用 AskUserQuestion 展示计划清单 + 覆盖检查结果请我确认。

=== 分批调度子 Agent（AGENTS.md §8.3，不一口气串行 13 个）===
试点批：plan-01（shared-types）+ plan-04（backend 骨架）
  → 各自完成 5+1 步验收（AGENTS.md §8.2）→ **暂停**，
    请我人工逐文件 review + 记录三个数（验收耗时/返工次数/可留用比例）→ go/no-go
  → no-go 则先修 Skills 配置/提示词/plan 粒度，重跑试点批，不带病放行
第二批：plan-05（商品库存）+ plan-06（订单支付，核心风险区）
  → 各自完成 5+1 步验收 → **暂停调度**，
    列出全部事务/并发/幂等/状态机/金额相关文件清单，等我人工逐行 review 放行
（后续批次由我放行后推进）

批内每个 plan 的调度循环：
  a. 调用 project-radar 更新 project-context.md，提取相关章节摘要
  b. 准备子 Agent prompt（含前置上下文：project-context 摘要 + 上份交接信息 + PRD 章节指引）
  c. Task 工具启动子 Agent，subagent_type=general_purpose_task
  d. 等待返回 final summary
  e. 5+1 步验收（完整定义见 AGENTS.md §8.2）：
     - git diff --name-only 查改动是否越界
     - 跑子 Agent 报告的验证命令
     - Grep 查幻觉 import（不在 package.json/requirements 的依赖）
     - Read 抽查 1-2 个文件（非 TODO 空壳）
     - GetDiagnostics 或跑 tsc/mypy
     - 重复检测（project-radar）：对照 project-context.md 查重复组件/函数/类型
     - 调用 fix skill 修格式问题（Prettier + Lint 自动修复）
     - 调用 code-reviewer skill 审查代码质量（正确性/安全/性能，含前端规则）
     - 调用 impeccable /audit 审查设计质量（如该 plan 涉及 UI，含 a11y 对比度检查）
  f. 全部通过 → 调用 git-commit skill 规范提交（Conventional Commits）→ 更新 project-context.md
     → 解析 final summary 的"给下一个的提示"塞进下个 Task query → 下一个
  g. 不通过 → 用修复模板重启该子 Agent（最多重试 2 次）
  h. 每批结束后：调用 project-radar 重新扫描增量更新 project-context.md；
     压缩主 Agent 上下文，丢弃已完成 plan 的 final summary 全文和验收输出，
     只保留"给下一个的提示"和 pass/fail 结论。用 AskUserQuestion 同步进度 + 对照基线三个数。
  i. 上下文接近上限时：主动压缩，用 AskUserQuestion 提示我"上下文即将满，建议压缩"

=== 我放行后的后续批次（第四条指令触发）===
plan-02+03+07 → 08+09+10 → 11+12+13（每批 3 个），每批结束对照试点基线检查三个数，劣化则暂停汇报。

=== 自动化测试 ===
所有 plan 完成后，跑全量验证（docs/verify-commands.md 所有端各跑一遍）：
- backend: cd backend && ruff check . && mypy . && pytest -v --cov=app
- h5-app: cd packages/h5-app && tsc --noEmit && pnpm test && pnpm build
- admin-app: 同上
- shared-*: pnpm build && pnpm test
- e2e: cd tests/e2e && pnpm playwright test
失败时自动修复循环：
  - 调用 code-reviewer skill 定位逻辑问题（先审逻辑）
  - 调用 fix skill 修格式问题（后修格式）
  - 用修复模板重启对应子 Agent
  - 循环直到全部通过

=== 商业级验收 ===
测试全绿后，先调用 project-radar 的验收覆盖检查：
- 自适应定位 MVP 清单章节，只提取 PRD §7.1 标注 [1a] 的项（AGENTS.md §8.5.1）
- 对照验收清单逐项检查，遗漏则 AskUserQuestion 提示补充

然后跑商业级验收（PRD E15 + 补充项；运维类只验 AGENTS.md §十四分期矩阵"1a 期必须"列）：
- 调用 code-reviewer skill 审查所有代码（含前端规则与性能）
- 调用 impeccable /audit 审查设计质量（排版/色彩/间距/对比度/a11y）
- 调用 impeccable /polish 上线前精细化
- 调用 impeccable /harden 生产就绪检查
- E2E：cd tests/e2e && pnpm playwright test
- 按 PRD E15.1/E15.2/E15.3 + 补充项检查全部验收点
- 生成验收报告，用 AskUserQuestion 让我确认

=== 部署上线 ===
我确认验收通过后：
- 自己同步 docs/api-contracts/ + docs/error-codes.md（契约/文档与代码对齐）
- 自己创建 PR（合并 feat/plan-* 到 main，先跑预检脚本）
- 用 AskUserQuestion 询问是否部署（人工红线）
- 我确认后跑 scripts/build.ps1 构建所有产物
- 跑 npx impeccable detect CI 设计检测（60 条确定性规则，不调 LLM）
- 本地依赖运行 `scripts/start-infra.ps1`；只有完成镜像、密钥、备份和回滚配置并人工确认后，才运行 `scripts/deploy.ps1 -Env production`
- 跑冒烟 E2E
- 汇报访问 URL

注意事项：
1. 严格遵循 AGENTS.md 的所有规则
2. 严格遵循 docs/设计规范.md（用确认的 Demo 风格）
3. 严格遵循 docs/PRD-v1.3.md 附录 D/E 的所有设计决策
4. 每个子 Agent 完成后自动跑验证命令
5. 禁止 any 类型
6. 禁止硬编码颜色/间距/字号，一律用 CSS 变量
7. 写操作按钮必须用 useDebounceAction Hook
8. 金额一律整数分
9. 架构变更/合并 main/push 远程/新依赖引入 → 用 AskUserQuestion 请示我
```

### 3.2 你在这条指令里要做的 3 个人工卡点

这是校准 Agent 产出质量的关键，**不要只点确认**：

**卡点 1：契约 review（阶段 0 完成后）**
- 真读 OpenAPI 文件，重点：支付回调验签/幂等/金额、库存原子 SQL、订单状态机、金额 integer 分
- 这是 Agent 最容易"看起来对"的地方

**卡点 2：试点批 go/no-go（plan-01 + plan-04 完成后）**
- 人工逐文件 review 两个 plan 的全部产出
- 记录三个数：验收耗时、返工次数、可留用比例（直接可用/小改可用/必须重写）
- go/no-go 阈值（AGENTS.md §8.3）：单 plan 验收 ≤ 1 工作日、返工 ≤ 2 次、可留用 ≥ 80%
- 不达标 → no-go：先修 Skills 配置/提示词/plan 粒度，重跑试点批

**卡点 3：plan-05/06 事务代码 review（第二批完成后）**
- Agent 会暂停并列出全部事务/并发/幂等/状态机/金额相关文件清单
- 见第四步

### 3.3 Agent 自动完成

Agent 会：
1. 阶段 0：自己写 OpenAPI 契约 + shared-types（等你真读 review）
2. 现场拆 13 个 plan（等你确认清单）
3. 试点批 plan-01+04 → 5+1 步验收 → 暂停等你逐文件 review + go/no-go
4. 第二批 plan-05+06 → 5+1 步验收 → 暂停等你逐行 review 事务代码
5. 你放行后：后续批每 3 个一批推进
6. 自动跑全量测试 + 修复失败项（fix + code-reviewer）
7. 商业级验收（code-reviewer + impeccable）
8. 生成验收报告 → 你确认 → 部署上线

Agent 暂停问你的场景（全部 AskUserQuestion 弹窗，选选项即可）：
- 契约 review（真读，见卡点 1）
- 计划清单确认
- 试点批 go/no-go（见卡点 2）
- plan-05/06 事务代码 review（见第四步）
- 架构变更 / 合并 main / 新依赖引入决策
- 每批结束进度同步（对照基线三个数）
- 验收报告确认
- 部署确认

---

## 第四步：人工 review 事务代码 + 放行全量开发

### 4.1 你人工 review plan-05/06 事务代码

Agent 在 plan-06 验收通过后会暂停，给你列出全部事务/并发/幂等/状态机/金额相关文件清单。
**你逐行 review**（这是 1a 期最重要的资金安全关口，Agent 的 code-reviewer 不能替代）：

| 检查项 | 看什么 |
|---|---|
| 库存原子 SQL | `UPDATE ... WHERE (physical_stock - locked_stock) >= :qty` 条件是否正确；锁定/扣减/回滚/售后回滚四条路径 |
| 乐观锁状态机 | `UPDATE orders SET status=:new WHERE id=:id AND status=:old` + 影响行数检查；非法流转拒绝 |
| 支付回调 | 验签、幂等键（重复回调不重复入账）、金额校验（回调金额 vs 订单金额）、状态流转 |
| 订单超时取消与回调竞态 | E12：取消任务和支付回调并发时谁先拿到锁，后到者如何处理 |
| 金额计算 | 全链路 integer 分，无浮点运算，Decimal 转换边界 |
| 幂等 | `_requestId` 幂等键 Redis 5 分钟 + DB 唯一索引兜底 |

**发现问题**：在聊天窗口输入修复指令（现象/复现/报错/预期，只改 backend/，先定位根因再改，禁 try/except 吞错）。

### 4.2 review 通过后，你输入放行指令（第 4 条）

```
plan-05/06 事务代码人工 review 已通过。继续执行剩余批次：
- plan-02+03+07 → 08+09+10 → 11+12+13，每批 3 个，批间用 AskUserQuestion 同步进度 + 对照试点基线三个数，劣化则暂停向我汇报
- 全部完成后：全量测试 → 商业级验收（运维类只验 AGENTS.md §十四分期矩阵 1a 期必须列）→ 验收报告 → 部署确认
```

### 4.3 Agent 继续全自动

放行后 Agent 自主完成：后续 9 个 plan（3 批）→ 全量测试 → 商业级验收 → 验收报告 → 等你部署确认。

**你只需要在批间进度弹窗、验收报告、部署确认时点选项。**

---

## 常见问题

### Q：Agent 卡住了怎么办？
A：在聊天窗口输入"继续"或"跳过当前步骤，进入下一步"。

### Q：Agent 生成的代码不符合规范怎么办？
A：输入"请检查 AGENTS.md 和 docs/设计规范.md，修正不符合规范的地方。调用 code-reviewer skill 审查"。

### Q：Demo 风格不满意怎么办？
A：输入"Demo X 的 XX 部分不好，请改成 YY 风格"。

### Q：想中途暂停怎么办？
A：直接关闭 CodeX 窗口，下次打开输入"继续上次的项目开发"。

### Q：Agent 上下文太满怎么办？
A：输入"压缩上下文，保留当前 plan 进度和关键决策"。

### Q：额度快用完了怎么办？
A：简单任务（写测试、改文案、格式化）切便宜模型，复杂任务（架构/支付/状态机）用最强模型。

### Q：某个 Skill 的规则和项目设计系统冲突怎么办？
A：Agent 会在 SKILL.md 末尾自动加"优先遵循项目 docs/设计规范.md，不覆盖自定义 CSS 变量"。

### Q：如何查看 Agent 当前进度？
A：输入"查看当前进度：读 plans/INDEX.md，跑 git log --oneline -20，汇报已完成几个 plan、当前在哪个 plan、有哪些阻塞项"。

### Q：试点批三个数不达标怎么办？
A：no-go。先修 Skills 配置/提示词/plan 粒度（比如 plan 拆得太粗导致返工多），重跑试点批，不带病放行（阈值见 AGENTS.md §8.3）。

---

## 关键原则

1. **你不写代码**：所有代码由子 Agent 写
2. **你不跑命令**：所有命令由主 Agent 跑，你只审批 RunCommand
3. **你做决策**：架构变更/合并 main/push 远程/新依赖引入，Agent 会问你
4. **你做 3 个人工 review 卡点**：契约（真读 OpenAPI）/ 试点批（逐文件 + 三个数）/ plan-05/06 事务代码（逐行）
5. **你做验收**：Demo 风格选择、最终验收报告确认
6. **你做部署**：确认部署、配置域名/证书

---

## 文件清单（换电脑必带）

```
liteshop/
├── 操作手册.md                  ← 本文件（你看的，4 条指令完成全流程）
├── 提示词.md                    ← 主 Agent 入口提示词（备用，操作手册已含）
├── AGENTS.md                    ← 项目宪法（调度机制唯一权威 §八）
├── .env.example                 ← 环境变量模板
├── .gitignore
├── .codex/skills/project-radar/SKILL.md  ← project-radar 安装源（主 Agent 初始化时复制到全局）
└── docs/
    ├── PRD-v1.3.md              ← 产品需求 + 详细设计
    ├── 工作流文档-v2.1.md       ← IDE 多 Agent 调度特有内容（机制引用 AGENTS.md §八）
    ├── 环境准备清单.md          ← 电脑环境/软件/依赖
    ├── 设计规范.md              ← Design System 完整规范
    ├── Skills安装指南.md        ← Skills 市面分析 + 安装/使用时机（常驻清单以 AGENTS.md §8.7 为准）
    ├── CI-CD规范.md             ← CI/CD 流水线 + 质量门禁 + Hotfix
    ├── 数据库迁移规范.md         ← Alembic 迁移命名/评审/回滚 SOP
    ├── 可观测性规范.md           ← SLI/SLO + Prometheus 指标 + 告警
    ├── 部署发布规范.md           ← 部署架构 + 灰度 + 回滚 SOP
    ├── API版本管理.md            ← 版本演进 + 弃用策略
    ├── 故障响应手册.md           ← 故障分级 + BCP + postmortem 模板
    └── verify-commands.md       ← 各端验证命令
```

---

**文档版本**：v1.1（4 条指令 + 试点校准 + 人工 review 卡点）
**创建日期**：2026-09-01（v1.0）/ 2026-09-04（v1.1）
**适用工具**：CodeX IDE
**使用者**：你（产品经理/项目负责人）
