/**
 * PC 子路径出口。
 * 当前 Phase 1 只提供平台无关原子件；Radix 驱动的 PC 组件在出现真实调用方后按方案逐步加入。
 */
export { Button, EmptyState, Input, Skeleton } from '../index.js';
export type {
  ButtonProps,
  ButtonVariant,
  EmptyStateProps,
  InputProps,
  SkeletonProps,
  SkeletonVariant,
} from '../index.js';
