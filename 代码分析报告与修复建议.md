# LiteShop 代码优化建议（2026-09-09 复查版）

> 复查日期：2026-09-09
> 基线：2026-09-07 首轮全量分析 + 2026-09-08 修复提交（7ed6cca / bb1b964，+5546 行 / 110 文件）
> 复查方式：git 快照对比 + 逐条问题代码级重验（grep / read / git ls-files）
> 本文档**取代** 09-07 版报告——旧版 3 Critical + 9 High 已全部修复归档，本文只保留当前仍然成立的问题

---

## 一、复查总结

### 1.1 修复成果确认（09-07 报告条目现状）

**全部 Critical（3/3）与 High（9/9）已修复**，且多数实现质量优于原方案：

| 条目 | 修复验证 |
|---|---|
| C1 验证码后门 | ✅ `code_generator()` 注入 + 格式校验，`123456` 移除（auth.py:82-94） |
| C2 购物车回滚 | ✅ store 新增 `upsertLine`，remove 失败可恢复 |
| C3 401 会话 | ✅ 单飞刷新 + `_liteshopRetried` 防循环 + 排除 login/refresh/logout（http.ts:43-108） |
| H1 RBAC 后门 | ✅ 纯 DB 权限校验，非数字 subject 直接拒绝（admin.py:49-56） |
| H2 幽灵包 httpx2 | ✅ 已换 `httpx>=0.27,<1` |
| H3 幂等键 | ✅ 拦截器仅缺省时设置 `X-Request-Id`；order-confirm 用 `requestIdRef` 绑定逻辑动作 |
| H4 清车误导 | ✅ `Promise.allSettled`，清车失败不再误报"下单失败" |
| H5 queryKey 冲突 | ✅ 死文件已从 git 删除（磁盘残留见问题 N1） |
| H6 UI 库决策 | ✅ 已定案：不引 antd，自建 ui-kit 单包三层（见《UI组件库设计方案.md》） |
| H7 tokens 包入口 | ✅ `exports` 含 `./tokens.css` |
| H8 幽灵依赖 | ✅ vite / plugin-react / typescript / eslint 显式声明 |
| H9 E2E | ✅ 4 → 6 spec（新增 a11y、admin-operations、site-publish、交易主链路登录→下单→订单列表） |

**功能缺口同步大幅收敛**（详见《功能缺口核对报告.md》）：售后全链路（表+8 状态机+前后端页面+测试）、运费模板 UI、评价审核、RBAC 管理页、H5 搜索页/设置页/通知入口/省市下拉、SchemaRenderer 真实数据渲染、搭建器官网 1200px 画布——均已落地。

### 1.2 当前质量评分（更新）

| 维度 | 09-07 | 09-09 | 变化 |
|---|---|---|---|
| 并发/数据正确性 | ★★★★★ | ★★★★★ | 保持 |
| 安全 | ★★★☆☆ | ★★★★☆ | ↑ C1/H1 修复 |
| 前端交易链路正确性 | ★★★☆☆ | ★★★★☆ | ↑ C2/C3/H3/H4 修复 |
| 测试 | ★★☆☆☆ | ★★★☆☆ | ↑ E2E 6 spec + 后端新增 5 个测试文件 |
| **架构（文件规模）** | ★★★☆☆ | **★★☆☆☆** | **↓ 恶化**：三个上帝文件继续膨胀 |
| 代码卫生 | ★★★★★ | ★★★★☆ | ↓ 磁盘残留 + commit 规范违例 |

---

## 二、当前问题清单

### 🔴 P0 — 立即处理（10 分钟内可完成）

#### N1. 磁盘残留 16 个未跟踪死文件

git 已删除全部兼容层死代码，但磁盘副本仍在（`git status` untracked），tsc / eslint / 后续 Agent 仍会扫到，且随时可能被误提交。

清单（DeleteFile 一次清完）：

```
packages/h5-app/src/{main-page.tsx, routes.tsx, useDebounceAction.ts, service/index.ts, trade-pages.tsx, utils/}
packages/admin-app/src/{admin-pages.tsx, main-page.tsx, product-editor.tsx, routes.tsx, useDebounceAction.ts, service/index.ts, service/products.ts, utils/index.ts}
packages/site-app/src/page.tsx
```

（h5 utils/ 目录下 format-price.ts 一并删除，功能已迁入 utils/ 新结构）

#### N2. 售后状态枚举与 PRD/契约偏差

实现（`backend/app/enums/after_sale.py`）与 PRD D1.1.2 / OpenAPI 契约命名不一致：

| PRD/契约 | 实现 | 语义 |
|---|---|---|
| GOODS_RETURNED | WAITING_RETURN → RETURNED（拆成两态） | 退货流转 |
| REFUNDED | COMPLETED | 退款完成 |
| REFUND_FAILED | ❌ 缺失 | 退款失败 |
| CLOSED | CANCELLED | 关闭 |

实现的 8 态比 PRD 的 7+1 态**语义更完整**（WAITING_RETURN/RETURNED 拆分合理），但 REFUND_FAILED 缺失意味着退款失败无法显式建模。
**建议**：保留实现命名，补 `REFUND_FAILED` 状态（REFUNDING → REFUND_FAILED → REFUNDING 重试）；同步修订 PRD D1.1.2 与 after-sale.yaml。**属契约变更，需人工确认。**

### 🟠 P1 — 架构债务（唯一持续恶化的指标，plan-26 主体）

#### N3. 三个上帝文件不降反升

| 文件 | 09-07 | 09-09 | 变化 |
|---|---|---|---|
| backend/app/api/admin.py | 661 行 | **789 行** | +132（新增售后审核/审计端点继续堆入） |
| backend/app/services/admin.py | 567 行 | **738 行** | +177（同上） |
| admin-app page-builder/index.tsx | 645 行 | **784 行** | +169（官网画布功能又塞进同一文件） |

**拆分方案**（保持路由路径与契约快照不变，纯内部重构）：

1. **api/admin.py → 按域 router**：`admin/catalog.py`（商品/分类/SPU-SKU）、`admin/orders.py`（订单/物流/改价）、`admin/after-sales.py`（已存在独立文件，聚合端点迁出）、`admin/rbac.py`、`admin/audit.py`、`admin/dashboard.py`、`admin/freight.py`、`admin/marketing.py`；main.py 挂载路径不变
2. **services/admin.py → 同域 service**：拆分后 AdminService 只留 `require_permission` + `audit` 通用能力；售后/评价/运费逻辑已在各自 service（after_sale.py 186 行 / review.py / freight.py），把 admin.py 里对它们的编排调用迁走即可
3. **page-builder → 3 hooks + 3 子组件**：`usePageHistory`（撤销/重做栈）、`useAutoSave`（30s 草稿）、`useChannelCanvas`（商城/官网画布切换）；`ComponentPalette` / `Canvas` / `PropsPanel` 子组件。数据流顺手改 React Query（对应 M3 原方案）

#### N4. 残余越层调用（1 处）

[admin.py:378](file:///d:/work/liteshop-main/backend/app/api/admin.py#L378)：`review_service.repository.list_for_audit(session)` —— API 层直接穿透 service 摸 repository。
**修复**：ReviewService 增加 `list_for_audit()` 方法（logistics.py 同类问题已修，照搬其模式）。

### 🟡 P2 — 工程规范（小改动，可并入 N1 同一批提交）

#### N5. 金额字段后缀不统一

`priceCents` ✅（5 处）与 `totalAmount / total / totalSpent` ❌ 并存（shared-types/models.ts:30,132,181,328,492）。
**方案**：按宪法 §4.4.2 SemVer 两步走——本 MINOR 版先加 `totalAmountCents` 等新字段 + 旧字段 `@deprecated`，下一版删除。**契约变更需同步 OpenAPI + 人工确认。**

#### N6. 错误码复用过度

40901 被订单流转、购物车、页面 slug 冲突等互不相干场景共用（admin.py:98 / cart.py:23 / pages.py:140,296,325 / orders.py:99,307,332）。前端无法按码精准提示。
**方案**：限流拆 `42902`；slug 冲突拆 `40904`；订单/购物车状态冲突保留 40901。**契约变更需同步 error-codes.md。**

#### N7. tokens.css 遗留问题

- `--font-size-lg` 与 `--font-size-2xl` 同为 20px（tokens.css:56,59）——去重命名
- `--gallery-*` 仍混在基础 `:root`——迁 `[data-gallery]` 作用域
- 缺 `--z-index-*` 层级 token（与 ui-kit 方案 Phase 0 合并执行）

#### N8. eslint 缺 react-hooks 插件

h5 / admin 均未装 `eslint-plugin-react-hooks`（rules-of-hooks: error / exhaustive-deps: warn）。现有代码 useEffect 依赖写错不会被拦截。**与 ui-kit Phase 0 同批。**

#### N9. 依赖与脚本杂项

- `passlib[bcrypt]` 仍在 requirements.txt:10（无密码登录体系，纯死依赖）
- `deploy.ps1` 名实不符（实际是启动基础设施，无应用容器）→ 改名 `start-infra.ps1`
- `verify-plan.ps1` 仍非真实验证命令 → 按 docs/verify-commands.md 分端分发

#### N10. 提交规范违例

修复提交 `7ed6cca` / `bb1b964` message 均为 `"1"`，违反宪法 §4.5 Conventional Commits。已入库无法改写，**后续提交必须规范**（例：`fix(auth): replace fixed SMS code with injected generator`）。

---

## 三、修复路线图（更新版）

| 批次 | 内容 | 规模 | 验证 |
|---|---|---|---|
| **批次 A（立即）** | N1 删磁盘残留 + N10 备注 | 10 分钟 | `git status` 干净 |
| **批次 B** | N4 越层修复 + N6 错误码拆分 + N5 金额字段第一步（加新字段标 deprecated） | 半天 | `ruff + mypy + pytest` / `tsc + test` + 契约文档同步 |
| **批次 C** | **N3 域拆分（plan-26 主体）** | 1-2 天 | 路由路径不变，用 `pytest` + 契约快照测试守护 |
| **批次 D** | N7 tokens + N8 eslint 插件（= ui-kit Phase 0）+ N9 杂项 | 半天 | 各端 build + lint |
| **批次 E（决策）** | N2 售后枚举对齐（补 REFUND_FAILED + 改文档） | 人工确认后半天 | 契约测试 |

**未动的历史条目去向**：C/H 系列已修复归档；M1/M3 → N3；M4 → N4；M5 → N6；M6 → N5；M7 → N7；M9 → N8；M10 → N9。功能缺口类（改地址等）见《功能缺口核对报告.md》不再重复。

---

## 四、亮点（本轮修复值得肯定）

1. **修复质量高于原方案**：C3 的 `_liteshopRetried` 防循环、C1 的生成器注入可测试性，都比建议方案更完善
2. **测试意识提升**：新增售后 180 行 / RBAC 78 行 / 评价回复 45 行 / 运费 65 行 / 订单更新 117 行测试——新功能全部带测试交付
3. **功能补齐与债务修复同步**：没有因补功能而牺牲已修复的安全项

---

*本文档为 09-09 复查后的唯一有效版本；09-07 原报告的 C/H/M 条目编号在《CRM项目借鉴分析.md》《UI组件库设计方案.md》中的引用仍然有效（问题本体已修复或已映射到 N 系列新编号）。*
