# 阶段交接历史

> 2026-09-19 合并归档。以下保留原文，状态和测试数字仅对记录时版本有效；当前状态见 ../交接文档-当前阶段.md。



---

来源：docs/交接文档-1a阶段.md

# LiteShop 1a 阶段交接文档

> 本文保留 1a 历史快照；当前一期 1a/1b 状态以 `docs/交接文档-一期阶段.md` 为准。

> 交接版本：1a 阶段收尾版
>
> 记录日期：2026-09-06
>
> 适用范围：下一次新聊天、后续开发 Agent、阶段复盘与发布前整改

## 1. 项目概况

LiteShop 是面向中小商家的轻量级商城系统，当前采用 pnpm + Turborepo monorepo。

技术栈已经按项目宪法确定：

- H5：React 18、TypeScript、Vite、React Router 6、Zustand、TanStack Query
- 后台：React 18、TypeScript、Vite、React Router 6
- 官网：Next.js App Router
- 后端：Python 3.12、FastAPI、SQLAlchemy 2.0 async、Alembic、Redis、Celery
- 基础设施：PostgreSQL 16、Redis 7、Docker Compose

当前项目范围是 PRD v1.3 的一期 1a，13 个计划均已完成初版实现和自动化验收。1b 功能不应在没有重新拆计划和确认范围前混入 1a。

## 2. 本阶段已完成

### 2.1 计划完成情况

| 计划 | 交付内容 | 状态 |
|---|---|---|
| plan-01~03 | shared-types、Design Tokens、共享组件 | 已完成 |
| plan-04 | 后端骨架、认证、日志、基础设施 | 已完成 |
| plan-05 | 商品与三层库存模型、库存流水 | 已完成初版 |
| plan-06 | 订单状态机、支付创建、回调验签、后端幂等 | 已完成初版，仍需安全整改 |
| plan-07 | H5 应用骨架、加载/空/错误状态 | 已完成 |
| plan-08 | 商品列表、分类、详情、搜索、SKU 抽屉 | 已完成 |
| plan-09 | 购物车、订单确认、支付入口、前端防抖 | 已完成初版，仍需一致性整改 |
| plan-10 | 后台骨架、RBAC、审计日志 | 已完成初版 |
| plan-11 | 后台商品、分类、订单管理 | 已完成初版，搜索契约仍需修复 |
| plan-12 | 库存、运费模板、数据看板、系统设置 | 已完成初版，趋势/持久化仍不完整 |
| plan-13 | 集成验证、1a 验收复核 | 已完成初版 |

### 2.2 当前代码结构

前端按以下方式组织，后续不要退回到组件内裸请求或通过 `window.location.pathname` 分支：

```text
packages/h5-app/src/
├── pages/       页面视图
├── components/  页面共享组件
├── hooks/       React Query 与交互 Hook
├── service/     API 管理层
├── store/       Zustand 客户端状态
├── utils/       格式化与工具函数
└── router/      React Router 6 集中路由
```

后台保持同样的视图、组件、hooks、service、store、utils、router 分层。

后端保持：

```text
api -> services -> repositories -> models
```

请求/响应使用 Pydantic DTO，写操作使用异步事务，金额使用整数分，类型从 `@liteshop/shared-types` 复用。

### 2.3 已实现的用户交互

- H5 首页：搜索、清除搜索、搜索结果播报、轮播上一张/下一张/圆点切换、分类与活动区路由跳转。
- 商品详情：商品图轮播、SKU 抽屉、Escape 关闭、焦点恢复、Tab 焦点循环、滚动锁定、收藏反馈。
- 购物车：全选、单项选择、数量增减、删除、结算、防抖和操作状态反馈。
- 订单确认：地址、商品金额、运费、备注、提交状态和错误状态。
- 后台商品列表：搜索、清除搜索、刷新状态、结果数量播报。
- 所有主要写操作按钮均使用 `useDebounceAction` 或等价防抖。

## 3. 视觉和编码标准

### 3.1 已确认视觉标准

用户已确认采用 `outputs/miniprogram_ui_gallery_full.html` 的画廊风格，具体表现为：

- 主色基于珊瑚红 `#ff6b6b`，通过 Design Tokens 使用，业务代码不得直接散落硬编码颜色。
- H5 使用窄屏画布、留白、柔和浅色背景、圆角面板、毛玻璃导航和底部 Tabbar。
- 后台沿用同一套 Token，但信息密度更高，优先保证扫描效率和操作效率。
- 色彩、字号、间距、圆角、阴影均从 `packages/shared-tokens/` 和 `docs/设计规范.md` 读取。
- UI 交互遵循 WCAG AA：语义 HTML、键盘可操作、可见焦点、错误/加载/空状态完整。

### 3.2 已确认编码习惯

- 注释使用中文，且只写必要的上下文说明。
- 喜欢清晰的主流分层架构：前端有视图层、组件层、状态数据层、API 管理层、工具层；后端使用 API、Service、Repository、Model 分层。
- 路由使用 React Router 6 集中配置，不使用 `window.location.pathname` 作为页面路由实现。
- 禁止 `any`、禁止重复定义共享类型、禁止组件内裸 Axios。
- 写操作按钮必须防抖，loading 时必须 disabled。
- 金额全链路使用整数分，展示前统一 `formatPrice()`。
- 颜色、间距、字号、圆角、阴影使用 CSS 变量，禁止硬编码视觉值。
- API 必须遵循 `docs/api-contracts/v1/`，统一错误信封和枚举值。
- 代码完成后由主 Agent 主动 review、跑验证、修复问题，再进入下一步。
- 当前更偏好主 Agent 直接推进；过去子 Agent 调度曾出现停滞和交接不完整，因此后续除非明确要求，不要为了形式强行拆子 Agent。

## 4. 已执行验证

以下是目前已经执行过的验证，不能把“构建通过”误认为“可以生产上线”：

- Backend：ruff、format、mypy、pytest 通过，21 项测试通过。
- H5：TypeScript、ESLint、Vitest、Vite build 通过。
- Admin：TypeScript、ESLint、Vitest、Vite build 通过。
- Workspace：串行 Turbo typecheck、lint、test 通过；并发执行曾触发 Windows Node 原生内存不足。
- E2E：Playwright 冒烟测试 2 项通过。
- OpenAPI：14 个 YAML 可解析，1a 路由未发现契约遗漏。
- Impeccable detector：对 H5/Admin 返回 `[]`。
- H5 首屏 JS gzip：约 105KB，低于 200KB 门禁。

## 5. 当前没有完成的内容

### 5.1 基础设施

- Docker Desktop Engine 当前曾返回 500/无响应，PostgreSQL 16 和 Redis 7 尚未完成本机健康检查。
- 真实生产数据库仓储、真实微信/支付宝 SDK、真实支付沙箱和对象存储尚未接入。
- 当前部分开发路径仍支持内存/演示数据，不能直接作为生产行为。

### 5.2 产品功能边界

- 收藏仍为浏览器本地存储，服务端收藏持久化不属于当前完整交付。
- settings/page schema 仍是开发进程内存存储，重启丢失，多实例不一致。
- 后台销售趋势真实数据尚未完整实现，当前 trend 可能为空。
- 退款只有申请和初始状态，尚未完成渠道退款、Celery worker、成功/失败回写和库存回滚闭环。
- 1b 功能，包括售后、会员、通知中心、评价、商城主题持久化、低代码搭建器、收藏服务化、推荐等，不应假设已经完成。

## 6. 复审发现的生产阻塞项

以下内容来自 2026-09-06 复审结论。结论是：项目目前仍不建议进入生产环境。

### 6.1 高风险问题

1. 支付回调签名仍未覆盖 `providerTradeNo` 和支付渠道，且微信/支付宝共用回调密钥。
2. 创建支付单先返回幂等记录，后校验订单归属，存在跨用户支付单信息泄露风险。
3. 下单商品/SKU 查询未统一限制商品未删除、商品 `ON_SHELF`、SKU `ACTIVE`。
4. 数据库幂等事务无法覆盖 Redis 购物车写入，可能出现接口失败但购物车已修改或重复加购。
5. 未登录用户仍可能被前端显示为订单提交成功，但后端没有创建订单。
6. 下单成功只清理本地 Zustand 购物车，没有删除 Redis 购物车。
7. API 异常时后台和 H5 仍可能静默显示演示数据，生产环境会误导运营人员。
8. 主题配置只写进程内 `_theme`，重启和多实例场景不一致。

### 6.2 中风险问题

1. 短信验证码消费不是原子 GET + DELETE。
2. refresh token 没有真正轮换撤销和重放检测。
3. 未配置可信代理时直接信任 `X-Forwarded-For`。
4. 购物车累计数量没有上限，重复请求可无限累加。
5. 购物车接口大量使用 `HTTPException`，不符合统一错误响应契约。
6. 后台商品搜索参数没有真正传给后端服务层。
7. 后台库存分页缺少 `total` 和 `hasNext`。
8. 后台设置保存失败时仍可能显示“已保存”。
9. 退款状态没有完整推进闭环。
10. 首次并发登录创建用户可能触发唯一索引冲突且未统一转换。

### 6.3 测试与工程不足

- 当前 backend 测试主要是内存模式和 API 冒烟测试。
- `pnpm test` 曾在 Windows Vitest worker 中出现 `VirtualAlloc` 原生崩溃；串行 Turbo 可以通过，但根因仍应评估 CI 资源配置。
- shared-types、shared-components、shared-tokens 缺少有效行为测试。
- 缺少真实 PostgreSQL/Redis 集成测试。
- 缺少并发下单、库存竞争、支付回调竞态、Redis 成功但数据库提交失败、验证码并发消费、商品下架后下单等测试。

## 7. 下一步执行顺序

下一次开发不要直接做 UI 扩展，先按以下顺序清理生产阻塞项：

1. 先补支付回调签名字段、渠道独立密钥、支付单归属校验顺序和支付幂等测试。
2. 补齐商品/SKU 状态校验，覆盖商品下架、删除、SKU 禁用后的下单拒绝。
3. 重新设计 Redis 购物车与数据库幂等的一致性策略，并在订单成功后清理服务端购物车。
4. 生产环境关闭静默演示数据回退；未登录用户必须阻止提交或明确跳转登录。
5. 实现验证码原子消费、refresh token 撤销/重放检测、可信代理 IP 解析和购物车数量上限。
6. 统一购物车错误响应信封，修复后台商品搜索和库存分页契约。
7. 修复设置保存成功判断，主题配置落库，补齐真实看板趋势数据。
8. 完成退款 worker、渠道调用、状态回写、失败重试和库存回滚。
9. 建立真实 PostgreSQL/Redis 集成测试和并发测试，再重新进行商业级验收。
10. Docker 健康检查恢复后，执行完整的迁移、部署、冒烟和回滚演练。

涉及支付、鉴权、金额、数据库迁移、生产配置、架构调整或新依赖时，必须先向用户说明并请求确认，不能默默修改。

## 8. 已知错误与规避措施

### 错误一：过早把“构建通过”当成“功能完成”

**表现**：类型、Lint、构建和冒烟测试通过，但复审仍发现支付归属、Redis 一致性、生产回退等问题。

**规避**：以后每个计划至少同时检查业务不变量、失败路径、并发路径、真实数据路径和权限边界；支付、库存、订单不能只看 happy path。

### 错误二：开发回退逻辑没有严格隔离

**表现**：API 异常时使用演示数据或内存状态，容易把“开发体验”带进生产行为。

**规避**：所有 demo fallback 必须由显式开发开关控制，生产环境默认关闭；接口异常必须显示错误、重试和数据不可用状态。

### 错误三：只清理前端状态，没有核对服务端状态

**表现**：下单后只清理 Zustand，没有同步清理 Redis 购物车。

**规避**：每个跨存储写流程都要列出本地状态、Redis 状态、数据库状态和失败补偿路径，至少有集成测试覆盖部分成功。

### 错误四：交互改动引入了 effect 无限更新

**表现**：购物车未登录分支中，临时空数组每次渲染引用变化，导致 `Maximum update depth exceeded`。

**规避**：effect 依赖必须使用稳定引用；状态更新前比较新旧值；浏览器控制台必须作为 UI 验收的一部分，而不是只看页面是否渲染。

### 错误五：子 Agent 调度和交接信息不稳定

**表现**：子 Agent 曾出现执行停滞、正式摘要缺失、需要用户反复询问进度。

**规避**：当前阶段优先由主 Agent 直接执行；如果未来重新使用子 Agent，必须限制单批范围、明确完成回执、主 Agent 主动验收并自动推进，不能依赖用户轮询。

### 错误六：全量验证并发导致 Windows Node 原生内存耗尽

**表现**：并行 Turbo 执行时出现 `VirtualAlloc`、V8 OOM 和 worker 崩溃。

**规避**：Windows 本地默认使用 `turbo --concurrency=1` 或分包验证；CI 再根据机器资源配置并发度，不能把并发崩溃误判为代码失败，也不能忽略资源问题。

## 9. 下一次新聊天启动清单

新聊天开始后按这个顺序恢复上下文：

1. 读取本文件。
2. 读取 `AGENTS.md`、`.codex/project-context.md`、`plans/INDEX.md`。
3. 查看 `git status --short`，不要清理用户已有删除或未跟踪文件。
4. 先确认 Docker、PostgreSQL、Redis 当前状态。
5. 先处理第 6 节生产阻塞项，不要直接增加新页面。
6. 改动前读取对应 Skill；UI 改动读取 `frontend-ui-ux` 和 `impeccable`。
7. 每完成一个计划：主 Agent review、跑验证、更新本交接文档或新增下一版交接文档。
8. 只有在支付、鉴权、金额、迁移、架构、生产部署等红线事项完成用户确认后，才允许进入发布流程。

## 10. 重要文件索引

- 项目宪法：[AGENTS.md](../AGENTS.md)
- 项目雷达上下文：[.codex/project-context.md](../.codex/project-context.md)
- 计划索引：[plans/INDEX.md](../plans/INDEX.md)
- 架构说明：[架构说明.md](架构说明.md)
- 设计规范：[设计规范.md](设计规范.md)
- 验证命令：[verify-commands.md](verify-commands.md)
- API 契约：[api-contracts/v1/](api-contracts/v1/)
- 错误码：[error-codes.md](error-codes.md)

## 11. 交接结论

1a 阶段已经完成可运行的产品骨架和主要业务链路，但当前仍属于开发/验收状态，不是生产就绪状态。下一阶段的首要目标不是继续堆功能，而是完成支付、订单、库存、购物车一致性、认证撤销、持久化和真实基础设施测试，直到第 6 节阻塞项逐项关闭并重新通过商业级验收。


---

来源：docs/交接文档-一期阶段.md

# LiteShop 一期交接文档

> 交接版本：一期 1a/1b 代码复核版
>
> 记录日期：2026-09-06
>
> 用途：下一次新聊天快速恢复项目状态、约束、剩余工作和验收结论。

## 1. 一期完成内容

- 完成 1a 的 plan-01 至 plan-13：共享类型与令牌、H5 商城交易链路、Admin 基础运营、FastAPI 分层、订单状态机、三层库存、支付回调验签、Redis 购物车、操作日志和 Playwright 冒烟。
- 完成 1b 的 plan-14 至 plan-23 核心实现：支付/订单安全、验证码与 refresh token、购物车幂等、主题/看板/搜索/库存分页、退款核心处理、会员、通知、评价、低代码 Schema、收藏服务化和页面入口。
- plan-24 已完成代码级验收；本轮已恢复本地 Docker 基础设施并完成 Alembic 迁移往返，真实 PostgreSQL/Redis 并发覆盖仍待补齐。

## 2. 当前代码架构

前端两端均采用：

```text
pages（视图）
  -> components（跨页面组件）
  -> hooks/features（查询、交互和领域模型）
  -> service（Axios/API 管理）
  -> store（Zustand 客户端状态）
  -> utils（纯函数）
  -> router（React Router 6 集中路由）
```

领域能力只在确有复用或独立规则时进入 `features`：H5 的 `features/catalog`、`features/cart`，Admin 的 `features/dashboard` 均有真实实现；商品详情的轮播/评价/SKU 抽屉和购物车商品行属于页面私有组件，分别位于 `pages/product-detail/components`、`pages/cart/components`。初始骨架中遗留的空 `src/app`、`src/shared/*` 目录没有业务含义，不应继续向其中添加代码。

本轮补齐了此前空的 `features`：H5 的 `features/catalog`、`features/cart` 和 Admin 的 `features/dashboard` 已有真实领域模块。`useDebounceAction` 两端语义统一为动作立即执行，执行及冷却期间锁定按钮，唯一真实实现位于 `shared-components`，应用包只保留兼容导出。认证状态已统一到两端 `store/session.ts`，页面和 HTTP 拦截器不再直接读写认证 `localStorage`；Admin 布局和需要登录的 H5 页面增加了守卫。商品详情和购物车完成了第一批局部组件拆分，加载/错误反馈抽成共享组件。

2026-09-06 再审确认：前端第一批架构收口已完成。共享 `ProductCard`/`formatPrice`/`useDebounceAction` 已统一，H5/Admin 路由已页面级懒加载，购物车/地址/结算与后台领域查询已有 feature API 出口；H5/Admin typecheck、lint、test、build 全绿。真正的 i18n 资源层、Admin E2E/axe-core 和部分页面进一步拆分仍待后续。

后端保持：

```text
api -> services -> repositories -> models
```

领域异常已集中到 `backend/app/errors/domain.py`，主题设置由 `services/settings.py` 编排，API 不直接调用系统设置仓储。

2026-09-06 后端整改进展：收藏、会员、页面、通知、评价 Service 的数据库访问已下沉到对应 Repository，Service 仅保留领域校验、编排和响应映射；`api/admin.py`、`services/admin.py`、`api/orders.py` 仍是历史聚合文件，真实 PostgreSQL/Redis 并发路径仍待补齐。

2026-09-06 环境恢复记录：Docker Desktop Engine 已恢复，Docker Hub 直连因本机无可用 HTTPS proxy 失败，开发 Compose 暂使用 `docker.m.daocloud.io/library/{postgres:16-alpine,redis:7-alpine}` 镜像；两个容器均为 healthy。Alembic 已完成 `upgrade head → downgrade base → upgrade head` 往返，当前版本为 `20260906_150000`，数据库已创建 26 张业务表。

## 3. 视觉与编码偏好

- 视觉标准是 `outputs/miniprogram_ui_gallery_full.html` 的画廊风格：珊瑚红主色、浅色背景、留白、圆角面板、毛玻璃导航、H5 窄屏画布；后台同 Token 但信息密度更高。
- 颜色、字号、间距、圆角、阴影必须使用 CSS 变量；不要在业务 CSS 或 JSX 散落视觉常量。
- React 函数组件 + Hooks，页面只编排视图；服务端状态用 TanStack Query，客户端状态用 Zustand。
- 路由必须使用 React Router 6，禁止 `window.location.pathname` 分支。
- 所有写操作按钮使用 `useDebounceAction`，Mutation `retry: 0`；金额全链路使用整数分并通过 `formatPrice()` 展示。
- 禁止 `any`、组件内裸 Axios、重复定义共享领域类型；注释使用中文且只保留必要上下文。
- JSX 必须由 Prettier 格式化为多行可读结构，不能把整个视图压在一行。

## 4. 验证结果

已通过：

```text
backend: ruff format --check / ruff check / mypy app / pytest -q
         33 passed，1 个 Starlette 弃用警告
workspace: pnpm -r typecheck / pnpm -r lint / pnpm -r test / pnpm -r build
h5/admin: tsc、ESLint、Vitest（各 2 tests）、Vite build、Prettier check
e2e:      Playwright 2 passed
impeccable detector: []
```

本轮环境复核：Docker Compose 两个服务均为 `healthy`；`alembic upgrade head → downgrade base → upgrade head` 通过；PostgreSQL `pg_isready` 返回 `accepting connections`，Redis 返回 `PONG`；Python `3.12.10`、pnpm `9.15.0`（通过 `npx pnpm@9.15.0`）可用。当前 Node 为 `v26.8.1`，满足项目 `>=22` 约束，无需切换版本。

`pnpm -r test` 中 shared-types/shared-tokens/shared-components/shared-3d-components 当前只有 0 测试的 Node test 脚本，命令成功不代表行为覆盖充分。

## 5. 未完成与生产阻塞

- 本地 Docker 与迁移演练已通过；Compose 使用 DaoCloud 镜像代理是当前网络环境的开发机适配，换机或 CI 应通过镜像代理/缓存恢复官方镜像可达性。
- 真实微信/支付宝 SDK、生产支付沙箱、OSS/COS 和真实短信供应商尚未接入；当前支付与开发数据路径是本地/沙箱实现。
- plan-18 售后仍缺后台审核 UI、退货地址/物流和真实 Celery worker；plan-20 业务事件自动通知、plan-21 图片签名/DOMPurify、plan-22 搭建器保存/预览/模板、plan-23 搜索历史/热门/分页仍需补齐。
- plan-25 前端架构收口已完成主要项：领域 Hook/API 拆分、组件复用、路由懒加载、金额唯一事实源和防抖测试已落地；全面 i18n、Admin E2E/axe-core 与页面进一步拆分仍未完成。
- plan-26 后端分层已完成 Repository 收口的第一批：当前 33 条测试通过，总覆盖率约 60%，订单工作流 37%、库存仓储 26%、支付仓储 36%；API/Service 大文件拆分及真实 PostgreSQL/Redis 并发测试仍未完成。
- AGENTS.md 固定的 Ant Design Mobile、Ant Design、Tailwind、lucide、react-hook-form、zod 等前端依赖尚未落地；依赖安装和迁移需要用户明确确认。
- 低代码官网、多页面 SEO、18 个官网组件属于二期；3D、营销工具、多租户、小程序/APP、AI 和智能补货属于三期。
- 当前工作树包含用户已有删除/未跟踪文件，不能擅自清理或回滚；本轮仅包含迁移兼容性修复与 Compose 镜像源调整，提交前需单独检查 `git diff`。

## 6. 下一步顺序

1. 继续 plan-26：拆分 `api/admin.py`、`api/orders.py`、`services/admin.py`，补真实数据库/Redis 并发测试。
2. 补齐前端真正 i18n、Admin E2E/axe-core 与页面级组件拆分。
3. 完成 1b 余项及 plan-24/25/26 验收记录，重新跑全部验证和商业验收。
4. 由主 Agent 拆二期官网低代码计划：Next.js 动态路由、SchemaRenderer、SEO、导航/Footer、18 个官网组件、动画和联系表单。
5. 二期通过后再拆三期 3D/营销/多租户计划；引入 Three.js、R3F、第三方支付/OSS、生产部署或新重型依赖前必须先取得用户确认。

## 7. 本轮发现的错误与规避

- 过去把“构建通过”当成“功能完成”。以后必须同时检查业务不变量、权限、失败补偿、并发和真实数据路径。
- 过去创建了空 `features` 目录和重复 Hook，导致架构名存实亡。以后新增领域逻辑必须有真实模块和 index 导出，重复能力只保留一个语义实现。
- 过去 API 异常可能回退演示数据。现在前端回退仅在 Vite 开发模式启用，生产/预发布后端强制启用数据库；新增回退必须显式受环境开关控制。
- 过去 API 层出现仓储直连。以后异常和持久化都由 Service 编排，API 只处理输入、鉴权和响应映射。
- 过去局部按钮绕过防抖。以后用 grep 检查所有 `mutateAsync` 和写操作入口，确保 loading 期间 disabled。
- 过去把短信验证码发送仅做成 1 秒防抖，未实现 E16.2 要求的 60 秒冷却；当前 H5 登录页已增加 60 秒倒计时，后续新增验证码入口必须同时检查前端倒计时和后端限流。
- 过去把 `features` 目录名当作架构完成标志，实际没有领域代码。以后只有存在跨页面复用或独立领域规则时才创建 feature；页面私有视图放到 `pages/*/components`，共享反馈和基础控件放到 `shared-components`。
- 过去各页面直接读取认证 `localStorage`，容易出现登录状态分叉。以后只通过 `store/session.ts` 读写令牌，服务层通过 `getAccessToken()` 注入请求头。
- 过去全量 Turbo 并发在 Windows 出现原生内存不足。以后本机默认串行或低并发，CI 再按机器资源提高并发。
- 本轮迁移演练暴露 `TEXT` 默认值阻塞 PostgreSQL 类型转换：以后 Alembic 修改列类型时，先显式移除旧 `server_default`，完成 `USING` 转换后再设置目标类型默认值，并必须执行 upgrade/downgrade 往返。

## 8. 当前结论

一期代码可以继续开发和验收；Docker 基础设施与迁移演练已恢复，但在真实并发测试、真实第三方基础设施和 1b 余项完成前，明确不允许进入生产环境。下一次新聊天先读本文件、`AGENTS.md`、`.codex/project-context.md` 和 `plans/INDEX.md`，再从 plan-26 的并发测试或一期未完成项继续。


---

来源：docs/交接文档-二期阶段.md

# LiteShop 二期交接文档

> 交接版本：二期官网基础能力与三期 3D 基础工具
>
> 记录日期：2026-09-06

## 已完成

- `packages/site-app` 已从最小占位页升级为 App Router 官网：首页、`/about`、`/products`、`/contact` 动态页面、静态参数、Metadata、sitemap、robots 和 404。
- 官网 Schema 数据与渲染器已建立，覆盖导航、Footer、Hero、HeroSplit、Features、Stats、LogoWall、Testimonials、Pricing、FAQ、图文、CTA、联系表单及 3D 占位等 18 类组件。
- 官网视觉复用 `shared-tokens` 和画廊风格变量，响应式覆盖 320px，支持移动端导航抽屉、表单提交状态、键盘焦点和减少动画偏好。
- 联系表单后端新增 DTO、Service、Repository、幂等 API 与 PostgreSQL `form_submissions` 迁移；开发模式内存存储，数据库模式真实落库。
- `shared-3d-components` 新增场景预设、速度限制和设备降级工具，暂不引入 Three.js/R3F。

## 验证

- 官网 `typecheck`、`lint`、`test`、`next build` 全部通过，生成 `/`、`/about`、`/products`、`/contact`、sitemap、robots 和 404。
- Backend Ruff、mypy、35 条 pytest 全部通过；真实 PostgreSQL 联系表单写入通过。
- shared-3d-components 构建与 3 条工具测试通过。

## 未完成与阻塞

- 官网页面目前使用本地 Schema 数据，尚未从后台页面 API 读取，也未接入 ISR revalidate webhook。
- 导航菜单、Footer 配置、官网全局设置和页面 SEO 字段尚未落库；联系表单后台查询、邮件/企微通知和更严格限流待补。
- 商城低代码编辑器仍缺模板、预览、A/B 变体和完整 10 组件扩展。
- Three.js/R3F/drei、GSAP、Lenis、Framer Motion 尚未接入。它们属于新增依赖，按 AGENTS.md 需先确认依赖与包体积预算；当前使用 CSS/原生降级实现，不阻塞官网构建。
- 三期多租户、优惠券/拼团/分销、物流轨迹、AI 文案和智能补货尚未实现，需要独立数据模型与基础设施决策。

## 下一步

1. 完成 plan-28：导航、Footer、官网全局设置落库与后台权限 API。
2. 完成 plan-29：商城搭建器持久化、模板、预览、A/B 变体和相关推荐。
3. 补官网 API/ISR、联系表单后台处理与集成 E2E。
4. 取得依赖确认后接入 Three.js/R3F 及官网动画三库，跑包体积和移动端性能验证。
5. 再拆三期多租户、营销、物流、AI 和智能补货计划。

## 规避事项

- 不在页面组件内散落 API 请求；官网数据通过 Schema/feature 层进入渲染器。
- 不把 CSS 颜色、间距、圆角和阴影写成业务常量，统一使用 Token。
- 新增 3D/动画依赖前先记录依赖决策、首屏体积和降级策略。
- 联系表单写入必须带请求 ID，重复提交返回原结果，不能依赖前端按钮禁用作为唯一防线。


---

来源：docs/交接文档-二期三期阶段.md

# LiteShop 二期与三期交接文档

> 记录日期：2026-09-07

## 本轮报告复核结论

- 已复核《代码分析报告与修复建议.md》《CRM项目借鉴分析.md》《UI组件库设计方案.md》《功能缺口核对报告.md》并统一口径。
- 上一版报告中的 C1/C2/C3、H1-H5 已有代码修复和回归验证；不再把它们列为当前线上 Critical。
- plan-18、plan-22、plan-29 均改为“部分完成”：退款执行基础不等于售后闭环，Schema/编辑器外壳不等于真实数据消费，官网 API/ISR 不等于官网画布闭环。
- 功能缺口报告移除未经统一权重定义的完成度百分比，改为 P0/P1/P2/P3 逐项清单；后续排期以 `plans/INDEX.md` 的 plan-32~37 为准。
- CRM 借鉴中的 endpoint 字典不再放入 `shared-types`，TTL storage 需绑定用户/租户/token 指纹；`useUpdateEffect` 和 service `.then` 仅按真实调用方与可读性采用。
- ui-kit 仅保留为待确认方案：`shared-tokens` 是唯一业务 token 源，Radix 官方包按组件选择；创建新包或引入 Radix/Framer Motion/RHF/zod 前必须先确认依赖、包体积、a11y 和迁移范围。

## 本阶段完成内容

- 官网已具备 Next.js App Router、多页面 SSG、SEO Metadata、动态 sitemap、robots、404、18 类组件渲染器与联系表单。
- 官网导航、全局设置与页面 Schema 已接入公开读取 API；后台发布可触发 ISR 路径与标签失效，页面渠道按 `store/site` 隔离。
- Admin 已增加官网联系表单列表、状态筛选、详情和跟进状态更新，写操作使用防抖与请求幂等。
- 官网页面已区分 `DRAFT/PUBLISHED`；后台发布动作才会让官网公开读取并触发 ISR，商城页不会污染官网缓存。
- 商城搭建器支持 13 类组件、三栏布局、50 步撤销/重做、快捷键、自动保存、模板、拖拽添加、复制/删除/排序和 Token 属性编辑。
- 商品聚合已增加标签和相关推荐字段；H5 SchemaRenderer 已补商品横滑、优惠券与公告栏。
- 3D 共享包提供场景预设、设备降级和速度限制；官网通过动态导入接入轻量 3D 回退组件。
- 三期基础 API 已增加优惠券、领取幂等、物流轨迹和 AI 商品文案本地 Provider。

## 验证结果

- Backend：Ruff、格式检查、47 条 pytest 通过，Alembic 已验证 `upgrade → downgrade → upgrade`，当前 head 为 `20260907_094000`；mypy 在本机 Windows Python 工具链出现 DLL 加载失败，不能记为通过。
- Admin/H5：typecheck、ESLint、Vitest 和 Vite build 通过。
- 官网：typecheck、ESLint、Vitest 和 Next.js production build 通过，首屏共享 JS 约 87.3 KB。
- shared-3d-components：构建与 3 条测试通过。
- PostgreSQL 16、Redis 7：Compose healthcheck 均为 healthy。

## 2026-09-07 收口记录

- Admin 前端已把聚合 `useAdminQueries.ts` 拆为商品、分类、订单、库存、看板、会员、RBAC 七个领域 Hook；旧文件只保留兼容导出。
- Admin API 已按商品、订单、库存、权限、设置、联系表单拆成 `service/admin/<domain>.ts`，`service/admin.ts` 只做兼容聚合出口。
- 路由继续使用 React Router 6 的集中配置和页面级懒加载；未发现 `window.location.pathname` 分流。
- 串行验证通过：`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build`、backend Ruff/pytest、Playwright 5/5、18 个 OpenAPI YAML 解析、`npx impeccable detect`；mypy 因 Windows DLL 工具链阻塞待重跑。
- 新增 `backend/integration_tests/test_postgres_redis.py` 与 `scripts/test.ps1 -Integration`；在本机 PostgreSQL 16/Redis 7 上真实运行，库存竞争与 Redis NX 幂等共 `2 passed`。标准单元测试默认跳过该目录，避免开发机缺少基础设施时误报。
- 复核发现的构建失败是并发启动两个 Next/Turbo 构建竞争 `.next/export` 清理造成的环境竞态；改为串行后稳定通过。
- 修复首页切换的渠道隔离：`clear_home` 只更新当前 `store/site` 渠道，并新增回归测试；强制串行 `turbo run build --force --concurrency=1` 真实通过。
- 复核《代码分析报告与修复建议.md》并完成高风险整改回归：短信验证码改为随机一次性凭据并增加生产 Provider 门控；RBAC 删除硬编码管理员后门；`httpx2` 更换为主流 `httpx`；H5/Admin HTTP 客户端增加 Cookie 刷新、401 单飞重放和稳定请求 ID；购物车回滚与订单清车失败提示修正；删除无调用方且会造成 queryKey 冲突的 Admin 商品旧查询实现。
- 新增 H5 交易主链路 Playwright 测试，覆盖登录、加购、购物车、下单、支付创建和订单列表；修复订单列表 mock 的方法分流后，全量 E2E 为 5/5 通过。
- 本轮报告复核结论：H6（UI 库迁移）、M1/M3（大文件拆分）、M5/M6（错误码/金额字段契约变更）属于独立架构或 MAJOR 变更，暂不在本次直接执行；真实支付、短信、物流供应商和多租户仍需外部凭证或产品决策。

## 未完成与环境/决策阻塞

- Admin 页面发布真实数据库种子 E2E 仍需稳定测试环境；当前已覆盖 Admin 联系表单接口桩流程、H5 草稿预览与官网动态读取单测。
- 邮件/企微通知尚未接入，需要外部渠道凭证和失败重试策略。
- 搭建器预览已打开 H5 草稿预览窗口，官网发布后的浏览器级 ISR 验证仍待补齐。
- Three.js/R3F/drei、GSAP、Lenis、Framer Motion 未引入。它们属于重型新增依赖，需要先确认包体积、版权和性能预算。
- 多租户遵循独立部署优先；共享数据库租户隔离不能只加 `tenant_id` 就声称完成，需要完整 RLS/唯一索引/缓存键/任务上下文设计。
- 拼团、秒杀、分销、发票、真实物流供应商、外部 AI 模型、智能补货仍需产品规则、供应商凭证和独立状态机。
- 集成测试清理必须先删除 `stock_logs` 流水再删除 SKU；该外键无级联是审计数据保留约束，禁止在生产迁移中直接改为级联删除。
- `mypy` 在当前 Windows Python 安装中出现 DLL 加载失败；此前同版本检查曾通过，本轮 Ruff、pytest、TypeScript、Lint、Prettier 和构建均通过，后续应修复 Python 工具链后再执行生产门禁。

## 商业化前必须完成

1. 接入真实微信/支付宝 SDK 和支付沙箱，完成商户证书、验签密钥、回调重试及对账演练。
2. 为订单、库存、支付回调、验证码、购物车和收藏补充真实 PostgreSQL/Redis 并发 fixture，并对关键模块建立覆盖率门槛。
3. 将 `backend/app/api/admin.py`、`backend/app/services/admin.py`、`backend/app/api/orders.py` 按领域继续拆分，保持 API 契约不变。
4. 补齐 H5/Admin 全量 i18n、axe-core 键盘测试，并在安装 Ant Design、Tailwind、lucide、表单依赖前确认包体积和迁移范围。
5. 完成多租户隔离（或确认独立部署）、真实物流/邮件/企微/AI 供应商与生产灰度、回滚和冒烟验证。

## 下一步建议

1. 先完成 P0：Admin 订单改地址和运费模板管理 UI，并补契约级测试。
2. 再完成 P1：售后完整闭环（plan-32），从模型/迁移到 H5 申请、Admin 审核、退货入库和幂等集成测试。
3. 完成 plan-33/34 的低代码真实数据消费和官网画布/动画配置，再补官网发布浏览器 E2E。
4. 按 plan-35/36 补齐 Admin/H5 体验缺口；ui-kit（plan-37）在依赖与宪法确认后再实施。
5. 三期高级业务按优惠券、拼团/秒杀、物流、AI、多租户分别拆计划，不把互相独立的状态机塞进一个提交。

## 本阶段犯错与规避

- 页面复制最初只改了数据库 slug，响应 Schema 的旧 slug 会覆盖新值。已改为复制时同步 Schema，且响应由模型字段覆盖 Schema。以后所有“数据库列 + JSON 快照”双存字段都必须加一致性测试。
- 首轮模型使用了 SQLAlchemy 保留属性 `metadata`，导致启动失败。以后新增 ORM 字段先核对 SQLAlchemy 声明式保留名，并先跑最窄 import/模型检查。
- 初次真实导航测试用了非管理员 subject，得到 403。以后数据库闭环测试明确准备角色/权限，开发超级管理员测试使用约定 subject `admin`。
- 前端新增 workspace 依赖后只更新 package.json，包缺少 exports 导致 TypeScript 找不到声明。以后共享包必须同时声明 `main/types/exports`，再跑消费者 typecheck。
- 渠道字段首次只加了索引，未同步调整旧的全局 slug 唯一约束；以后涉及渠道隔离时必须同时检查 ORM 约束、迁移兼容和同 slug 跨渠道测试。
- 页面最初是“保存即公开”，没有发布状态，难以支持预览和回滚；以后低代码内容必须把草稿、发布和公开读取过滤作为同一条验收链路。

## 用户偏好与代码习惯

- 前端按视图层、组件层、features 状态数据层、API/service、store、hooks、utils、router 分层；页面不能把所有 JSX 挤在一行。
- 后端保持 `api → services → repositories → models`，写操作走事务和幂等，不允许 API 直接写 ORM。
- 注释使用中文；禁止 `any`；金额使用整数分；写操作按钮使用共享 `useDebounceAction`。
- 视觉沿用确认的珊瑚红画廊风格，颜色、字号、间距、圆角和阴影全部走 Design Tokens。
- 主 Agent 每完成一个计划主动 review、修复、验证并继续，不等待用户逐次追问。
- 当前工作流由主 Agent 串行执行，子 Agent 不作为进度阻塞；每批完成后记录真实命令输出和未决外部依赖。


---

来源：docs/交接文档-当前阶段.md

# LiteShop 当前阶段交接文档

> 记录日期：2026-09-10
> 范围：一期 1a/1b 收口、二期/三期缺口继续推进

## 本阶段完成

- 售后闭环基础实现：售后模型、迁移、申请、用户查询、审核、退货物流、退款/换货完成、Redis/数据库幂等、订单项锁定和退货库存回补。
- 售后契约已重写为结构化 OpenAPI：补齐用户列表/详情/退货物流、后台列表/审核/完成、状态枚举、金额整数分、错误信封和幂等请求头。
- 售后关键边界测试新增 7 项：越权/不存在订单项、金额超过订单项、重复请求、进行中重复申请、非法状态流转、重复提交物流、已完成订单申请。
- H5 SchemaRenderer 已真实消费首页 Schema、商品、分类，并覆盖加载、错误、空数据和未知组件降级。
- Admin 售后审核工作区已接入状态筛选、同意/拒绝、退款/换货完成确认，写操作统一防抖且 mutation `retry: 0`。
- 官网搭建器已区分商城/官网模式，支持 375px 商城画布和 1200px 官网画布，官网组件面板、SEO 标题/描述、组件动画字段已接入。
- SiteRenderer 已监听 `prefers-reduced-motion`，组件卸载时移除媒体查询监听器；未引入 GSAP/Lenis/Framer Motion 等新依赖。
- H5 已补支付倒计时与过期禁用、订单状态步骤条、独立搜索页、热门搜索、本地搜索历史、通知未读角标、账户设置和客服入口。
- 后端订单响应已暴露 `expiredAt`，内存演示响应也保持字段一致。
- Admin RBAC 已支持角色创建/改名/权限绑定、角色删除保护和管理员角色分配，写入统一走数据库事务、幂等和操作日志。
- 运费模板编辑器已支持多个地区计费项的新增、修改和删除，金额仍统一为整数分。
- 商品评价已增加商家回复字段、迁移、后台回复接口和 H5 展示，只有审核通过的评价可回复。
- H5 地址级联、评价提交、浏览足迹和相关推荐已接入现有 service/features 分层；Schema 预览 E2E 已修复并通过。
- 评价与售后后台列表已收口到 Service 边界，API 不再直接访问 Repository；两处均补回归测试。
- shared-tokens 字号梯度与设计规范对齐，补齐完整 z-index 语义 token，并保留旧命名兼容别名。
- `verify-plan.ps1` 已支持按 plan/target 运行真实 Backend/H5/Admin/Site/Shared/E2E 门禁。
- 本地基础设施入口已独立为 `scripts/start-infra.ps1`；未配置生产目标时 `deploy.ps1` 会明确拒绝执行，防止把本地 Compose 当作生产发布。
- 独立 UI Kit `packages/ui-kit` 已创建并纳入 workspace，提供可迁移的 Button、Input、EmptyState、Skeleton 基础层及 `pc`/`mobile` 子路径出口。
- H5 首页已补下拉刷新、首页专用骨架屏和刷新状态提示；Schema 商品板块已支持 `productIds`、`sort`、`count` 配置，并新增发布 Schema 消费 Playwright 测试。
- 工程门禁已补齐根级 `Dockerfile`、`.dockerignore`、GitHub Actions CI 和 `scripts/check_enum_sync.py`；共享 9 个枚举与后端定义同步通过。
- UI Kit token 桥接已接入 H5、Admin、Site 三个宿主入口，业务侧不会再产生第二套颜色/间距事实源；组件迁移仍按真实调用方逐步进行。
- H5 SchemaRenderer 的分类/商品板块已拆到 `packages/h5-app/src/components/schema/`，通过 `React.lazy + Suspense` 按需加载，构建产物可见独立 chunk；Schema 属性读取与 URL 白名单也集中到该目录。
- 官网搭建器的默认商城/官网 Schema、组件分组、模板、草稿恢复和预览工具已移入 `packages/admin-app/src/pages/page-builder/model/page-builder-model.ts`，并补充 2 项模型回归测试。
- 新增 `backend/tests/test_memory_trade_paths.py`，覆盖内存沙箱订单归属/非法状态、支付金额/验签/回调幂等和库存失败不变更；后端全量测试从 72 项增至 75 项。

## 尚未完成

- 关键业务闭环已具备本地验证，但后端覆盖率为 60%，低于 CI 规范要求的 80%；需要继续补充订单、库存、支付、认证和售后数据库级测试，不能用配置排除或删断言规避。
- 真实微信/支付宝 SDK、渠道退款失败重试、短信/邮件/企微和物流供应商仍未接入，需要外部沙箱凭证与产品决策。
- plan-26 的 Admin 与订单 API/Service 领域拆分已完成：`backend/app/api/admin.py`、`backend/app/api/orders.py` 与 `backend/app/services/admin.py` 现为兼容聚合入口，具体实现位于 `backend/app/api/admin_routes/`、`backend/app/api/order_routes/` 和 `backend/app/services/admin_*.py`；其他 Service 的 Repository 下沉仍待继续。
- plan-29 的真实数据库种子发布 E2E 尚未补齐；官网 ISR 鉴权和发布入口已有浏览器测试。
- 完整 i18n 资源化、axe-core 自动化审查、多租户隔离、生产灰度和真实发布流程仍需后续工作。
- plan-33 仍缺真实数据库发布 E2E 和请求瀑布性能深测；非首屏商品/分类板块已使用 lazy/Suspense，当前新增 E2E 使用隔离响应验证发布 Schema 消费，不等同于真实种子数据验收。
- plan-34 已完成 page-builder 模型、ComponentPalette、Canvas、PropsPanel、History/AutoSave hooks 拆分；真实数据库种子发布/回滚 E2E 仍未完成。
- `packages/ui-kit` 已创建并纳入 workspace：Phase 1 提供 Button、Input、EmptyState、Skeleton 与 `pc`/`mobile` 薄出口；包零 `@liteshop/*` 业务依赖，未引入 Radix、Framer Motion、RHF、Zod。现有应用迁移、AGENTS.md 宪法调整和平台专属重依赖仍保留确认关卡。

- UI Kit 调用方已盘点：H5/Admin 主要使用 shared-components 的反馈状态与 ProductCard，Site 仅使用防抖行为；暂不并行替换同名业务组件，后续按单页面、低风险顺序迁移。

## 验证记录

- Backend：`ruff check .`、`ruff format --check .`、`python -m mypy .`、`python scripts/check_enum_sync.py` 通过；`pytest -q` 为 75 passed，1 个 Starlette 弃用警告；`pytest -q --cov=app --cov-report=term-missing` 总覆盖率 60%；Alembic 当前 head 为 `20260908_110000`。
- Backend 集成：Docker Compose 启动后设置 `LITESHOP_RUN_INTEGRATION=1` 和 `LITESHOP_USE_DATABASE=true`，`pytest -q integration_tests/test_postgres_redis.py` 为 3 passed，覆盖 PostgreSQL 库存竞争、Redis NX 幂等和售后请求幂等。
- Workspace：`pnpm -r build`、`pnpm -r typecheck`、`pnpm -r lint`、`pnpm -r test` 均通过；shared-types/shared-components 在消费者检查前先完成 build。
- UI Kit：`pnpm --dir packages/ui-kit build`、`typecheck`、`lint`、`test` 全部通过（2 tests passed）；根级 `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 均通过，Turbo 已识别 8 个 workspace 包。
- H5/Admin/Site：各自 typecheck、lint、Vitest、Vite/Next production build 均通过；H5 首屏 gzip 仍低于 200KB 门禁。
- OpenAPI：`docs/api-contracts/v1` 下 18 个 YAML 均可由 PyYAML 解析；RBAC、售后和评价回复契约与 FastAPI 路由同步。
- E2E：`cd tests/e2e; pnpm test` 为 13 passed，覆盖 H5 Schema 发布消费、RBAC 403/409、运费多计费项、评价回复、SKU 键盘焦点、320px/reduced-motion 和官网 ISR 鉴权。
- 设计质量：`npx impeccable detect` 退出码 0；本轮新增交互使用 Design Tokens、语义按钮、焦点恢复和 Escape 关闭。
- 基础设施：本轮复查时 Docker Desktop Engine 未运行，因此未能重验 PostgreSQL/Redis；上轮记录为两者均 `healthy`，本轮不能沿用旧状态声称仍在运行。
- `git diff --check` 无空白错误；Windows 换行提示不影响验证。

## 本阶段发现并修复的问题

- 售后 OpenAPI 初版把 `schemas` 错放在 `components` 外层，且遗漏退货物流和后台审核接口；已按真实 FastAPI 路由重写并结构化解析验证。
- 新增 `expiredAt` 后，测试用的 `SimpleNamespace` 没有该属性导致订单地址测试失败；已用 `getattr` 兼容测试替身，并补齐内存响应字段。
- 售后单测初版假仓储缺少 `get_for_update`，导致退货状态测试无法进入业务判断；已补足最小仓储协议并通过全量测试。
- H5 首次 typecheck 使用了未重新构建的共享类型 dist，导致 `expiredAt` 类型暂时不可见；以后修改共享包后必须先 build 再跑消费者检查。
- 搭建器官网模式初次只改变文案，没有隔离组件面板和画布宽度；已改为 channel 驱动的组件分组和 375/1200 Token 宽度。
- RBAC/评价回复首轮实现后，mypy 暴露 SQLAlchemy `ScalarResult` 推断为 `Any`；通过仓储边界的精确 `cast` 修复，避免向服务层泄漏 Any。
- OpenAPI 评价契约首次插入商家回复字段时缩进错误，结构化解析立即捕获并修正；以后每次契约编辑必须先跑全目录 YAML 解析。
- 集成测试首次直接设置 `LITESHOP_RUN_INTEGRATION=1`，但遗漏 `LITESHOP_USE_DATABASE=true`，导致幂等操作拿到 `None` 会话；以后统一使用 `scripts/test.ps1 -Integration` 或同时设置两个变量。
- Docker Engine 曾在复跑前暂时不可用，启动 Compose 后服务恢复；基础设施门禁必须先检查 `docker compose ps` 再判断集成测试结果。
- `deploy.ps1` 原先实际只启动本地 Compose，名称与生产发布语义冲突；已拆出 `start-infra.ps1`，并让部署入口在缺少生产参数时失败关闭。
- 根级 Prettier 脚本依赖 `npx --yes prettier@3.6.2`，本地未安装 `prettier`，因此直接执行 `pnpm exec prettier` 会失败；后续格式化应使用项目既有 `pnpm format`/`pnpm format:check`。
- 官网 Next.js 构建曾因旧 `.next` 缓存引用不存在的 `/_document` 失败；清理生成目录后构建通过。以后遇到 PageNotFoundError 先清理对应框架生成缓存，再判断源码是否有问题。
- UI Kit 方案文档曾在包创建后仍标记为“不得创建”；本轮已同步方案、plan-37 和计划索引。后续需保持“代码状态变更后立即更新交接/计划文档”的习惯，避免下一轮 Agent 依据过期阻塞信息工作。

## 后续开发约定

- 前端按视图层、组件层、features 状态数据层、service API 层、store、hooks、utils、router 分层；页面 JSX 必须分行格式化。
- 后端保持 `api → services → repositories → models`，写操作事务化、Redis/DB 双层幂等，API 不直接操作 ORM。
- 注释使用中文，禁止 `any`；金额统一整数分；所有写操作按钮使用共享 `useDebounceAction`；React Query mutation 设置 `retry: 0`。
- 类型优先从 `@liteshop/shared-types` 导入；API 与 `docs/api-contracts/v1/` 同步；不重复定义组件、枚举和错误码。
- UI 继续沿用确认的珊瑚红画廊风格，颜色/字号/间距/圆角/阴影只用 Design Tokens。
- 主 Agent 完成每个阶段后主动 review、修复、验证并继续，不等待用户逐次追问；子 Agent 不作为进度阻塞。
- sync/await 用于事务、轮询、补偿和多分支；简单一次性解包可用 `.then`，同一函数不混用且完整传播 rejection。

## 下一步顺序

1. 继续 plan-26：推进其他 Service 的 Repository 下沉，并补订单、库存、支付、认证、售后真实数据库并发测试。
2. 将后端覆盖率从 60% 提升到 CI 要求的 80% 以上，优先补当前低覆盖的 API、Repository 和订单工作流分支。
3. 补完整 i18n 资源化、axe-core 键盘/触摸自动化和真实数据库种子发布 E2E；每轮变更后复跑 workspace、Playwright、契约解析和 Impeccable detector。
4. UI Kit 后续按真实调用方逐组件迁移；LiteShop 宿主先完成 `shared-tokens → --ui-*` 桥接，再评估是否引入平台专属底座和表单依赖。
5. 真实支付/短信/物流/邮件供应商、多租户隔离和生产部署必须在外部凭证、架构与发布红线确认后进行。

## 2026-09-10 增量交接

- Admin 页面构建器已从单文件拆成编排层、组件库、画布、属性面板和两个 hooks，Admin typecheck/lint/test/build 全部通过。
- 后端后台路由与服务已按领域拆分，原有 39 个后台 path+method 保留，OpenAPI 后台组合 49 条且无重复；Ruff、mypy、pytest 75 passed。
- 本轮未改变 API 契约、数据库模型或依赖；Admin 与订单 API/Service 已完成领域拆分，覆盖率 80%、真实数据库并发和 Docker Engine 仍是后续工作/环境阻塞。

## 2026-09-12 增量交接

### 本轮完成

- plan-26 的后端分层继续收口：`admin_access`、`admin_trade`、`after_sale`、`refund`、`contact`、`freight`、`marketing`、`page` 和 `order_workflow` 中的 ORM 会话持久化操作均已下沉到对应 Repository。
- 新增订单工作流仓储边界测试，覆盖取消、确认收货、改价、支付超时取消和支付回调顺序；补充售后审核、RBAC、退款和运费仓储委托测试。
- 临时 PostgreSQL 数据库完成 `upgrade head → downgrade base → upgrade head`，最终 head 为 `20260908_110000`；开发库没有执行降级或清空。
- Docker Compose PostgreSQL 16/Redis 7 均为 `healthy`；真实集成测试保持 `3 passed`。

### 当前验证

- Backend：`ruff format --check .`、`ruff check .`、`python -m mypy .` 通过；`pytest -q` 为 `84 passed`，仅有 Starlette 第三方弃用警告。
- Workspace：根级测试和构建通过；H5/Admin/Site/ui-kit/shared 包验证保持通过。
- Service ORM 扫描：未发现 `session.add/add_all/delete/flush/get/scalar/scalars/execute/commit/rollback/refresh` 直接调用。

### 尚未完成

- 后端覆盖率约 62%，低于 CI 80% 门禁；需要继续补订单、支付、认证、购物车、收藏和验证码/Token 轮换的真实数据库级并发测试。
- 真实微信/支付宝 SDK、渠道退款失败重试、短信/邮件/物流供应商和生产数据库仓储仍未接入，不能宣称生产就绪。
- plan-33/34 的真实数据库种子发布 E2E、请求瀑布性能基准仍待补；完整 i18n/axe-core 深度覆盖仍待补。

### 下一步

1. 在现有 Docker 环境中补订单创建并发、支付回调竞态、验证码消费、refresh token 轮换、购物车和收藏幂等的真实集成 fixture。
2. 按覆盖率报告优先补低覆盖 API/Repository/OrderWorkflow 分支，目标达到 CI 80%，不得通过排除、跳过或删除断言规避。
3. 真实数据库种子发布 E2E 与请求瀑布基准完成后，再进行一期/二期商业级验收审查。

## 2026-09-12 增量交接

### 本轮完成

- 补齐 `backend/integration_tests/test_postgres_redis.py` 的 6 类真实基础设施场景：并发下单库存竞争、支付回调与支付超时取消竞态、验证码原子消费、refresh token 原子轮换与重放拒绝、购物车请求幂等/Redis 故障、收藏 Redis+数据库幂等。
- 真实集成测试在 Docker PostgreSQL 16/Redis 7 下为 `9 passed`，并连续 5 轮复跑通过；标准后端测试仍为 `84 passed`。
- 修复 `ProductRepository` 和 `InventoryRepository` 的 SKU 商品关系预加载：库存原子更新刷新 SKU 后继续访问 `sku.spu` 不再触发异步 `MissingGreenlet`。
- 为跨 `asyncio.run` 集成测试隔离 Redis 客户端和事务连接，避免全局连接池绑定已关闭事件循环。

### 尚未完成

- 后端覆盖率约 62%，仍低于 CI 80% 门禁；下一轮应按覆盖率报告补订单工作流、库存/支付仓储、认证 Token、购物车/收藏和后台 API 分支。
- plan-24 的下架下单、退款失败重试、主题一致性测试仍待补；真实微信/支付宝、短信、物流和邮件供应商未接入，不能宣称生产就绪。
- plan-33/34 的真实数据库种子发布 E2E、请求瀑布性能基准、完整 i18n 和 axe-core 深度覆盖仍待完成。

### 验收记录

- `ruff format --check .`：198 个文件已格式化。
- `ruff check .`：通过。
- `python -m mypy .`：196 个源文件通过。
- `pytest -q`：84 passed，1 个 Starlette 第三方弃用警告。
- `LITESHOP_RUN_INTEGRATION=1 LITESHOP_USE_DATABASE=true pytest -q integration_tests/test_postgres_redis.py`：9 passed；竞态套件连续 5 轮通过。
- `docker compose ps`：PostgreSQL 16 与 Redis 7 均 `healthy`。

### 下一步

1. 按覆盖率报告补真实行为测试，目标达到 80%，不使用排除规则或弱化断言。
2. 补下架下单、退款重试和主题一致性测试，并在每轮后复跑标准/集成门禁。
3. 完成真实数据库种子发布 E2E 与请求瀑布基准，再进入商业级验收和生产红线评估。
## 2026-09-13 覆盖率收口

- 后端新增 16 项真实行为/分支测试，覆盖订单超时、ISR、页面/导航/营销 API 及后台/用户/运费/退款仓储。
- `ruff format --check .`、`ruff check .`、`python -m mypy .` 全通过；全量测试 `133 passed`，覆盖率 `83%`，已超过 CI 80% 门禁。
- 仍未完成：真实微信/支付宝、短信、物流供应商接入；完整 i18n、axe-core 深度覆盖及官网真实数据库种子发布。

## 2026-09-13 真实接口与边界联调

### 本轮完成

- 新增 `backend/integration_tests/test_real_http_api.py`，在 `LITESHOP_USE_DATABASE=true` 下通过 HTTP ASGI 链路验证商品分页、空搜索、非法分页、商品 404 错误码、分类空数组和匿名订单鉴权。
- 新增 `tests/e2e/specs/real-api.spec.ts`，可在后端启动时从 Playwright request 直连真实 API，验证分页边界和 404，不以 H5 Demo fallback 作为通过条件。

### 验收记录

- `LITESHOP_RUN_INTEGRATION=1 LITESHOP_USE_DATABASE=true python -m pytest -q integration_tests/test_real_http_api.py`：3 passed。
- 专用端口真实页面联调：`E2E_H5_PORT=5176 E2E_ADMIN_PORT=5177 E2E_SITE_PORT=5178 E2E_API_BASE=http://127.0.0.1:8000/api/v1 pnpm --dir tests/e2e exec playwright test specs/real-api.spec.ts -g 'H5 分类页'`：1 passed；确认浏览器页面实际请求后端 `/categories` 和 `/products`。
- H5：`pnpm --dir packages/h5-app typecheck`、`lint`、`test` 全通过（14 tests）。

### 尚未完成

- 浏览器页面级请求渲染验证仍需在本机同时启动 FastAPI 与 H5 Vite，并设置 `VITE_API_BASE` 后执行；当前 Playwright 联调用例已就绪但后端未启动时会跳过。
- 本机 Docker Desktop 引擎当前不可用（`docker compose ps` 无法连接 `dockerDesktopLinuxEngine`），因此本轮无法重新启动 PostgreSQL/Redis；上一轮真实 PostgreSQL/Redis 集成结果仍为 9 passed。
- 订单创建/支付回调的真实 HTTP 边界仍主要由既有 PostgreSQL/Redis 服务级测试覆盖，后续可增加带测试用户和唯一 `X-Request-Id` 的端到端夹具。
