# LiteShop 轻店 — 轻量级低代码商城与官网系统 PRD

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.3 |
| 撰写日期 | 2026-08-30（v1.1）/ 2026-08-31（v1.2 修订）/ 2026-08-31（v1.3 闭环补强） |
| 文档状态 | 评审通过（v1.3 增补附录 E：工程约束 + 闭环设计补强） |
| 产品定位 | 面向中小商家的轻量级、低代码、ERP 化商城 + 官网一体化系统 |
| 端构成 | H5 商城端 + 官网端（PC/移动响应式）+ 管理后台（含低代码页面搭建器） |

---

## 0. 修订日志

## 0.1 v1.2 修订背景（2026-08-31）

v1.1 经评审识别出 35+ 项设计缺陷、需求缺口与潜在困难。v1.2 在不改变整体架构与技术栈的前提下，补齐详细设计，作为开发实施的唯一依据。

### 0.2 v1.2 新增内容

- **附录 D1：数据模型详细设计** —— 订单/售后状态机拆分、金额单位规约、购物车一致性、RBAC、物流编码、主题配置等表结构与状态流转
- **附录 D2：接口契约详细设计** —— 金额单位、错误码三层结构、i18n、枚举管理、通知中心、限流策略、健康检查
- **附录 D3：低代码 Schema 版本管理与缓存策略** —— ComponentSchema 加版本字段、迁移函数、缓存失效、ISR 事件链路
- **附录 D4：架构与性能详细设计** —— 多级缓存、ISR revalidate 事件、Vite+Next.js 双构建兼容、3D 隔离方案
- **附录 D5：安全合规详细设计** —— 联系表单字段级加密、文件上传白名单、支付环境隔离、a11y 合规
- **附录 D6：新增功能详细设计** —— 消息通知中心、商品评价、搜索系统、库存预警自动化、运费模板、优惠券预留
- **附录 D7：可观测性与 DevOps 详细设计** —— 健康检查分层、Prometheus、Sentry、Source Map、蓝绿部署、备份恢复演练、CI 体积门禁
- **附录 D8：范围调整与排期修订** —— 一期拆 1a/1b 两阶段、3D 移出主线、多租户明确独立部署、P0/P1/P2 分级落地

### 0.3 v1.2 关键决策一览

| 编号 | 决策 | 类别 |
|---|---|---|
| D-01 | 订单状态机与售后状态机拆分，orders.status 只承担正向交易 | P0 数据模型 |
| D-02 | API 金额一律整数分（Cent），后端 Decimal↔int 在序列化层转换 | P0 契约 |
| D-03 | 购物车 Redis 为主 + DB 异步落库，下单时从 DB 取 SKU 快照锁定 | P0 数据模型 |
| D-04 | RBAC 一期只做菜单/按钮权限，数据权限后移二期 | P0 权限 |
| D-05 | 物流公司枚举 + 配置表，一期硬编码主流公司 | P0 主数据 |
| D-06 | ComponentSchema 加 version 字段 + 迁移函数注册表 | P0 低代码 |
| D-07 | Schema 缓存 Cache-Aside + 事件失效（page.updated 事件） | P0 缓存 |
| D-08 | 错误码 + i18n key + 默认中文 message 三层结构 | P0 契约 |
| D-09 | 统一通知中心 + 多渠道适配器（SMS/邮件/站内信/模板消息） | P0 通知 |
| D-10 | 商品评价系统一期基础版（评分+文字+图片+审核） | P0 新增 |
| D-11 | 运费模板表 + 模板项表（按地区/重量/件数） | P0 新增 |
| D-12 | H5 Schema 多级缓存（Service Worker + IndexedDB + 版本号对比） | P1 性能 |
| D-13 | ISR revalidate 异步事件链路（page.updated → Celery → 重试 → 死信） | P1 架构 |
| D-14 | 共享组件全部 "use client"，Vite + Next.js 双构建 e2e 烟雾测试进 CI | P1 架构 |
| D-15 | 3D 作为可选插件包，不进 componentMap 默认注册 | P1 范围 |
| D-16 | 一期拆 1a（10 周）+ 1b（5 周）两阶段交付 | P1 范围 |
| D-17 | 多租户明确独立部署，一期表结构不加 tenant_id | P1 架构 |
| D-18 | 联系表单字段级 AES-256-GCM + 90 天保留（一期文件密钥 FIELD_ENCRYPTION_KEY，KMS 为增长期，见 D5.1 分期降级说明） | P1 安全 |
| D-19 | 支付资质第 1 周前置申请，与开发并行 | P1 排期 |
| D-20 | 文件上传白名单 + Magic Number 双校验 + OSS 数据处理流水线 | P1 安全 |
| D-21 | i18n 一期用 i18next + 共享语言包，只做中文 | P2 i18n |
| D-22 | 发票一期不实现，三期对接第三方电子发票 SaaS | P2 合规 |
| D-23 | a11y 按 WCAG 2.1 AA 合规，CI 跑 axe-core | P2 合规 |
| D-24 | CI 跑 bundle-analyzer + 首屏 JS gzip 200KB 门禁 | P2 性能 |
| D-25 | 一期连接配置预留读写分离开关，主库单点 | P2 架构 |
| D-26 | Source Map 上传私有 OSS bucket + Sentry 关联 | P2 DevOps |
| D-27 | 蓝绿部署 + Alembic downgrade 演练 + 每月恢复演练 | P1 DevOps |
| D-28 | 限流分层（接口级/用户级/IP 级）+ 滑动窗口 Redis ZSET | P1 安全 |
| D-29 | 商城与官网主题统一 site_themes 表 + CSS 变量 setProperty | P1 架构 |
| D-30 | 低代码 Schema 多版本 + 流量分配支持 A/B 测试 | P2 营销 |

### 0.4 与 v1.1 的兼容性

- v1.1 主体章节（一~十一章）保持不变，作为产品需求基线
- 所有 v1.2 增补内容统一放在"附录 D：详细设计"章节，不改动原章节
- 若附录 D 与 v1.1 主体冲突，以附录 D 为准（例如金额单位、订单状态枚举）

### 0.5 v1.3 修订背景（2026-08-31）

v1.2 评审后做闭环性复审，识别出 29 项未闭环设计点 + 17 项行业对照缺口 + 工程约束缺失。v1.3 增补附录 E（工程约束 + 闭环补强），不改变 v1.2 的整体架构与决策。

### 0.6 v1.3 新增内容

- **附录 E0：闭环性补强清单** —— 对照 v1.2 的 29 项未闭环点逐一补强
- **附录 E1：代码约束** —— 命名/目录/分层/异步/事务/状态/样式等硬约束
- **附录 E2：前端组件化与设计系统** —— L0-L4 五层组件分层、组件规约、Storybook 集成
- **附录 E3：完整数据模型补遗** —— 库存/SPU/SKU/order_items/users/payments/pages/operation_logs 等缺失 DDL
- **附录 E4：代码注释规约** —— 注释原则、模板、语言、不需要注释场景
- **附录 E5：行业对照补充设计** —— 17 项行业缺口对照与落地建议
- **附录 E15：1a 期验收清单** —— 一期第一阶段完成判定标准

### 0.7 v1.3 关键补强决策一览

| 编号 | 决策 | 类别 |
|---|---|---|
| E-01 | 补齐库存三层模型 DDL + 锁定/扣减/回滚 SQL | 闭环补强 |
| E-02 | 补齐 SPU/SKU 规格表完整 DDL | 闭环补强 |
| E-03 | 补齐 order_items 快照表 DDL | 闭环补强 |
| E-04 | 补齐 payments/refunds 表 DDL 与状态机 | 闭环补强 |
| E-05 | 补齐 pages 表 DDL（低代码核心表） | 闭环补强 |
| E-06 | 补齐 operation_logs 表 DDL + 查询 API | 闭环补强 |
| E-07 | 补齐 users/addresses/favorites 表 DDL | 闭环补强 |
| E-08 | 画清订单状态机与库存联动时序图 | 闭环补强 |
| E-09 | 给出 OpenAPI 文件清单 + 核心端点契约 | 闭环补强 |
| E-10 | 规定鉴权 Header 与 Token 刷新接口 | 闭环补强 |
| E-11 | 给出文件上传签名接口契约 | 闭环补强 |
| E-12 | 给出 10 个低代码组件完整 Schema 示例 | 闭环补强 |
| E-13 | 规定组件 props 与样式字段类型 | 闭环补强 |
| E-14 | 设计 SSE 鉴权 + 断线重连 | 闭环补强 |
| E-15 | 定义 Outbox worker 轮询频率与分布式锁 | 闭环补强 |
| E-16 | 补 CSRF 防护 + XSS 白名单工具 + JWT 算法 + bcrypt cost | 闭环补强 |
| E-17 | 画清支付下单/回调完整时序图 | 闭环补强 |
| E-18 | 设计订单超时取消与支付回调竞态处理 | 闭环补强 |
| E-19 | 规定结构化日志字段格式 | 闭环补强 |
| E-20 | 规定 Sentry 初始化与敏感字段过滤 | 闭环补强 |
| E-21 | 规定命名/目录/分层/异步/事务/状态/样式硬约束 | 工程约束 |
| E-22 | 规定 L0-L4 五层组件分层与设计系统 | 组件化 |
| E-23 | 规定 Storybook 集成与低代码组件 Story | 组件化 |
| E-24 | 规定代码注释原则与模板 | 注释规约 |
| E-25 | 补商品多图主图/SKU 级图/SEO 字段预留 | 行业对照 |
| E-26 | 补购物车跨端 token cookie | 行业对照 |
| E-27 | 补批量操作与导入导出框架 | 行业对照 |
| E-28 | 补 feature_flags 灰度开关表 | 行业对照 |
| E-29 | 补 Turbo Monorepo 配置规约 | 行业对照 |
| E-30 | 补 SQLAlchemy 连接池配置 | 行业对照 |
| E-31 | 给出 1a 期验收清单 | 闭环补强 |
| E-32 | 前端防抖 + 后端幂等双层防护（订单/支付/加购等写操作） | 闭环补强 |

### 0.8 与 v1.2 的兼容性

- v1.2 附录 D 内容保持不变，作为设计基线
- v1.3 增补内容统一放在"附录 E：工程约束与闭环补强"章节
- 若附录 E 与附录 D 冲突，以附录 E 为准（附录 E 是对 D 的补强）

---

## 一、产品概述

### 1.1 产品定位

LiteShop 是一款面向中小商家（单店/品牌方/个体工商户）的轻量级独立商城与官网一体化系统，核心特点：

- **低代码装修**：商家通过可视化拖拽搭建 H5 商城页面和官网页面，无需写代码
- **ERP 化后台**：后台不只是商品上下架，而是覆盖商品、订单、库存、采购、财务、售后的轻量进销存
- **官网即开即用**：内置官网低代码搭建，支持多页面路由、导航菜单、SEO 配置、滚动动画、3D 效果
- **轻量部署**：单服务器 Docker Compose 一键部署，中小商家可私有化使用，也可 SaaS 化

### 1.2 目标用户

| 用户类型 | 特征 | 核心诉求 |
|---|---|---|
| 中小商家/个体工商户 | 单店经营，SKU 50-2000，日订单 < 500 | 低成本开店、自己能装修页面、管清楚库存和订单 |
| 品牌方/区域经销商 | 有自有品牌，需要独立商城沉淀私域 + 官网展示品牌 | 品牌化官网、商城交易、会员管理、多渠道库存 |
| 技术服务商/代运营 | 帮多个客户搭建商城和官网 | 可复用、低代码快速交付、多租户（二期） |

### 1.3 核心价值主张

> 让中小商家用一台服务器、一个下午，拥有一个自己能装修、能管进销存、能展示品牌的独立商城与官网。

### 1.4 产品愿景与版本规划

- **一期**：跑通商城"装修 → 上架 → 下单 → 支付 → 发货 → 进销存"完整闭环 + 商城低代码搭建
- **二期**：官网低代码搭建（多页面路由 + 18 个官网组件 + GSAP 滚动动画 + SEO）
- **三期**：3D 动画实验、多租户 SaaS 化、营销工具（优惠券/拼团/分销）、多端（小程序/APP）、AI 商品文案生成、智能补货建议

---

## 二、需求背景与目标

### 2.1 问题背景

1. 现有 SaaS 商城（有赞/微盟）年费高（数千至数万），且数据不在自己手里
2. 开源商城（CRMEB/萤火）功能臃肿，二次开发成本高，后台不是 ERP 思路
3. 中小商家不会写代码，但希望自己能改页面、调样式
4. 商家最痛的不是"能不能下单"，而是"库存准不准、订单乱不乱、钱对不对得上"
5. 品牌方需要独立官网展示品牌形象，但官网和商城往往是两套系统，数据不通、维护成本高
6. 现有官网搭建工具（凡科/建站之星）模板僵化，无法和商城数据打通，且动画/3D 效果差

### 2.2 业务目标

| 指标 | 一期目标 | 二期目标（官网） |
|---|---|---|
| 核心交易闭环 | 商品→购物车→下单→支付→发货→确认收货，全流程可用 | — |
| 低代码搭建 | 支持 10 个商城基础组件，商家可独立搭建首页 | 支持 18 个官网组件，多页面路由，商家可独立搭建整站 |
| 部署成本 | 单台 4C8G 云服务器可承载日订单 500 | 官网 SSG + CDN，首屏 LCP < 2s |
| 首屏加载 | H5 首页首屏 < 2s（4G 网络） | 官网首页 LCP < 2.0s，CLS < 0.1 |
| 库存准确率 | 订单扣减/取消回滚零超卖 | — |
| SEO | — | 官网页面自动生成 sitemap、结构化数据、Meta 标签 |

### 2.3 成功指标

- 商家从部署到上架第一个商品 < 30 分钟
- 商家独立完成商城首页装修 < 1 小时（无需技术支持）
- 商家独立完成官网整站搭建 < 半天（首页 + 关于 + 产品 + 联系）
- 订单状态流转零人工干预错误
- 支付回调处理零重复发货
- 官网 Core Web Vitals 全部达标（LCP < 2s, INP < 200ms, CLS < 0.1）

---

## 三、产品整体架构

### 3.1 系统架构（四层）

```
┌──────────────────────────────────────────────────────────────────┐
│                         接入层 (Nginx / CDN)                        │
│              反向代理 / HTTPS / 静态资源 / CDN回源 / SSG分发        │
├──────────────────────────┬───────────────────────────────────────┤
│       前端应用层           │            后端服务层                    │
│                          │                                       │
│  ┌────────────────────┐  │  ┌─────────────────────────────────┐  │
│  │  H5 商城端          │  │  │   FastAPI 后端服务                │  │
│  │  (Vite + React 18) │  │  │   - 用户认证 / 商品 / 订单 / 库存  │  │
│  │  - 商品浏览/购物车   │  │  │   - 支付回调 / 退款 / 售后         │  │
│  │  - 下单支付          │  │  │   - 低代码 Schema 存取（商城+官网） │  │
│  │  - Schema渲染       │  │  │   - 文件上传签名 / 短信 / 表单提交   │  │
│  └────────────────────┘  │  │   - ISR revalidate webhook        │  │
│                          │  └─────────────────────────────────┘  │
│  ┌────────────────────┐  │  ┌──────────────┐ ┌────────────────┐  │
│  │  官网端             │  │  │  PostgreSQL  │ │     Redis      │  │
│  │  (Next.js App Router)│ │  │  业务数据     │ │ 缓存/分布式锁   │  │
│  │  - SSG/ISR 静态生成 │  │  │  Schema JSON │ │ 购物车/超时队列  │  │
│  │  - 多页面路由渲染    │  │  │  官网页面     │ │ 限流/表单去重   │  │
│  │  - SEO Meta 注入    │  │  │              │ │                │  │
│  │  - 动画/3D          │  │  └──────────────┘ └────────────────┘  │
│  └────────────────────┘  │                                       │
│                          │                                       │
│  ┌────────────────────┐  │  ┌─────────────────────────────────┐  │
│  │  管理后台           │  │  │  Celery / ARQ 异步任务队列        │  │
│  │  (Vite + React 18) │  │  │  - 订单超时取消 / 退款 / 通知      │  │
│  │  - ERP模块          │  │  │  - ISR 页面重新生成                │  │
│  │  - 低代码搭建器      │  │  │  - 邮件/短信通知                  │  │
│  │  (商城+官网双画布)   │  │  └─────────────────────────────────┘  │
│  └────────────────────┘  │                                       │
├──────────────────────────┴───────────────────────────────────────┤
│                          基础设施层                                  │
│      OSS/COS 对象存储 / CDN / 微信支付 / 支付宝 / 短信 / 物流API    │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 端划分

| 端 | 技术选型 | 渲染方式 | 为什么 |
|---|---|---|---|
| **商城 H5 端** | Vite + React 18 + React Router | CSR（客户端渲染） | 商城是登录后应用，SEO 需求弱，CSR 足够 |
| **官网端** | Next.js 14+ App Router | SSG/ISR（静态生成/增量再生） | 官网核心需求是 SEO 和首屏性能，SSG 是 2026 年标准方案；React 语法和商城端一致 |
| **管理后台** | Vite + React 18 + antd 5 | CSR | 后台是内部工具，CSR 即可 |
| **共享层** | shared-components / shared-tokens / shared-types | — | 商城低代码组件和官网低代码组件共享同一套 SchemaRenderer 渲染引擎和 Design Tokens |

**为什么官网端单独用 Next.js 而不是全部迁移**：商城 H5 不需要 SEO，Next.js 的 App Router 对商城这种强交互应用反而增加复杂度；官网是内容展示型，SSG 性能极好。两者共享 React 组件库，构建工具不同但组件本身是构建无关的。

### 3.3 前端 Monorepo 结构

```
liteshop/
├── packages/
│   ├── h5-app/                  # H5 商城端（Vite + React 18）
│   ├── site-app/                # 官网端（Next.js App Router）
│   ├── admin-app/               # 管理后台 + 低代码搭建器（Vite + React 18）
│   ├── shared-components/        # 共享低代码组件（商城组件 + 官网组件）
│   ├── shared-3d-components/     # 共享 3D 组件（R3F，仅官网端引用）
│   ├── shared-tokens/            # 共享 Design Tokens（CSS变量/Tailwind/antd主题）
│   └── shared-types/             # 共享 TypeScript 类型
├── backend/                      # FastAPI 后端
├── docker-compose.yml
└── pnpm-workspace.yaml
```

### 3.4 低代码渲染引擎（共享）

所有低代码页面（商城页 + 官网页）共享同一套 Schema 格式和渲染引擎：

```typescript
// 页面 Schema 统一格式
interface PageSchema {
  pageStyle: PageStyle;           // 页面级样式（背景/内边距）
  seo?: PageSEO;                  // SEO 配置（仅官网页）
  animation?: PageAnimation;      // 页面级动画配置（仅官网页）
  components: ComponentSchema[];  // 组件列表
}

interface ComponentSchema {
  id: string;                     // 组件唯一 ID
  type: string;                   // 组件类型（如 'hero'、'product-grid'、'features'）
  props: Record<string, any>;     // 组件内容配置（商品来源/标题/图片等）
  style: ContainerStyle;          // 容器样式（背景/边框/圆角/内边距/外边距/阴影）
  elementStyle?: ElementStyle;    // 元素级样式（文字颜色/字号等，由组件自定义）
  animation?: ComponentAnimation; // 滚动进入动画（仅官网组件）
}
```

**SchemaRenderer**：遍历 components 数组，根据 type 从 componentMap 查找组件，注入 props/style/elementStyle/animation 渲染。商城端和官网端共用同一个 SchemaRenderer 组件，只是 componentMap 中注册的组件不同（商城端注册商城组件，官网端注册官网组件）。

---

## 四、功能需求详细说明

### 4.1 H5 商城端

#### 4.1.1 首页（低代码渲染）`[1b]`

> 1b 期交付（依赖低代码搭建器，见 §7.1 / D8.1.2 第 4-5 周）；1a 期首页为固定布局兜底页。

| 功能点 | 说明 |
|---|---|
| 动态渲染 | 根据后台保存的页面 Schema JSON 动态渲染页面组件 |
| 顶部搜索栏 | 固定顶部，毛玻璃背景，点击跳转搜索页 |
| 轮播图 | 支持多张图片、自动播放、指示器、点击跳转 |
| 分类导航 | 图标+文字网格，点击跳转分类列表页 |
| 商品列表 | 支持网格/横滑/列表三种样式，数据来源可选指定分类/指定商品/热销 |
| 活动专区 | 图片广告位，点击跳转指定页面或商品 |
| 底部 Tabbar | 首页/分类/购物车/我的，图标+文字，选中态用全局主色 |
| 下拉刷新 | 下拉刷新页面数据 |
| 骨架屏 | 页面加载时显示骨架屏占位 |

#### 4.1.2 商品模块

| 功能点 | 说明 |
|---|---|
| 分类页 | 左侧一级分类，右侧二级分类+商品预览 |
| 商品列表 | 支持综合/销量/价格排序，筛选（价格区间/分类），分页加载 |
| 商品详情 | 顶部图片轮播、价格区（现价+原价划线+促销标签）、标题、销量、SKU选择弹窗、详情图长图、相关推荐（`[1b]`，随 D6.7 手动推荐交付，见 §7.1） |
| SKU 选择 | 底部抽屉弹出，规格按钮选中态，显示价格/库存，加入购物车/立即购买 |
| 搜索 | 关键词搜索、搜索历史、热门搜索、搜索结果列表 |
| 收藏 | 商品收藏/取消收藏，收藏列表（`[1b]`，见 §7.1） |

#### 4.1.3 购物车

| 功能点 | 说明 |
|---|---|
| 商品列表 | 左滑删除、复选框、商品图/标题/SKU/价格/数量步进器 |
| 数量调整 | 步进器加减，库存上限校验，库存不足提示 |
| 全选/反选 | 底部结算栏全选复选框 |
| 合计计算 | 实时计算选中商品总价 |
| 去结算 | 跳转订单确认页 |
| 空状态 | 购物车为空时显示插画+去逛逛按钮 |
| 购物车动画 | 商品详情页点击加入购物车，图片抛物线飞入底部购物车图标 |

#### 4.1.4 订单模块

| 功能点 | 说明 |
|---|---|
| 订单确认 | 收货地址选择、商品清单、运费计算、优惠券选择（二期）、订单备注、提交订单 |
| 支付 | 支持微信支付（H5支付/JSAPI）、支付宝（手机网站支付），支付倒计时（30分钟） |
| 订单列表 | 按状态 Tab 筛选（全部/待付款/待发货/待收货/已完成/退款），订单卡片 |
| 订单详情 | 订单状态步骤条、商品清单、收货信息、订单编号/下单时间/支付时间/物流信息 |
| 订单操作 | 取消订单（待付款）、提醒发货（待发货，`[1b]` 随通知中心交付——1a 期无消费者，仅落库记录无实际效果，见 D6.1）、确认收货（待收货）、申请退款/售后（`[1b]`，见 §7.1 售后管理） |
| 物流查询 | 接入物流查询 API（一期可显示物流单号，二期接入轨迹） |

#### 4.1.5 用户中心

| 功能点 | 说明 |
|---|---|
| 个人信息 | 头像、昵称、手机号（登录即注册） |
| 收货地址 | 地址列表、新增/编辑/删除、设为默认、地址选择器（省市区） |
| 我的订单 | 订单入口，各状态订单数量角标 |
| 我的收藏 | 收藏商品列表（`[1b]`，见 §7.1） |
| 浏览足迹 | 最近浏览商品（`[1b]`，与收藏同模块交付，见 §7.1） |
| 联系客服 | 客服电话/在线客服入口（一期用电话+企微二维码） |
| 设置 | 关于我们、隐私政策、用户协议、退出登录 |

#### 4.1.6 登录与认证

| 功能点 | 说明 |
|---|---|
| 手机号登录 | 手机号+短信验证码登录（需接入短信服务） |
| 微信授权登录 | H5 环境下微信 OAuth 授权登录（可选，一期可用手机号） |
| Token 机制 | JWT Access Token + Refresh Token，前端存 localStorage，请求头携带 |

---

### 4.2 管理后台（ERP 化）

> **分期说明**：本节各功能的交付分期以 **§7.1 为准**——售后管理（4.2.7）、会员管理（4.2.8）、评价审核、商城主题配置（4.2.10 部分）为 `[1b]`；采购/财务管理（4.2.5/4.2.6）为 `[二期]`；官网全局设置（4.2.10 部分）为 `[二期]`。

#### 4.2.1 工作台/数据看板

| 功能点 | 说明 |
|---|---|
| 核心指标 | 今日/昨日销售额、订单量、客单价、新增用户数、退款金额 |
| 趋势图 | 近 7 天/30 天销售额与订单量折线图（ECharts） |
| 商品排行 | 销量 TOP10、销售额 TOP10 商品列表 |
| 待办提醒 | 待付款订单数、待发货订单数、退款待审核数、库存预警商品数 |
| 库存预警 | 低库存商品清单（低于安全库存的 SKU） |

#### 4.2.2 商品管理

| 功能点 | 说明 |
|---|---|
| 商品分类 | 两级分类树，分类名称/排序/图标，启用/禁用 |
| 商品列表 | SPU 列表，支持按分类/名称/状态筛选，上下架、批量操作 |
| 新增/编辑商品 | SPU 信息（名称/分类/主图/详情图/详情描述）+ 规格（颜色/尺寸等，可自定义规格名）+ SKU 列表（每个规格组合对应价格/库存/SKU编码/重量） |
| 商品上下架 | 单个/批量上下架 |
| 商品删除 | 软删除（已有关联订单的商品不可硬删） |
| 库存调整 | 单个 SKU 库存手动调整（记录调整原因，生成库存流水） |

#### 4.2.3 订单管理

| 功能点 | 说明 |
|---|---|
| 订单列表 | 按订单号/手机号/商品名/状态/时间筛选，订单卡片/表格 |
| 订单详情 | 订单完整信息（状态/商品/金额/收货/支付/物流/操作日志） |
| 订单操作 | 改价（待付款）、改地址（待发货）、备注、发货（填写物流公司+单号）、取消订单、关闭订单 |
| 批量发货 | 导出待发货订单→填写物流单号→批量导入发货（`[1b]`——与订单导出为成套批量能力，整体后移；1a 期只做单个发货，见 §7.1） |
| 订单导出 | 按筛选条件导出订单 Excel（`[1b]`，见 §7.1） |
| 订单状态机 | 待付款→已取消/待发货→已发货→已完成→退款中→已退款，状态流转严格校验 |

#### 4.2.4 库存管理

| 功能点 | 说明 |
|---|---|
| 实时库存 | 按 SKU 查看当前库存、锁定库存（已下单未发货）、可用库存 |
| 库存流水 | 所有库存变动记录（下单锁定/发货扣减/取消回滚/采购入库/手动调整），含变动数量、变动后库存、关联单号、时间、操作人 |
| 库存预警 | 设置安全库存阈值，低于阈值的 SKU 在看板预警 |
| 库存盘点 | 盘点单（一期可选，二期完善） |
| 多仓库 | 一期单仓库，二期支持多仓库调拨 |

#### 4.2.5 采购管理 `[二期]`

> 原「一期基础版」口径已移除（2026-09 裁决：§7.1 无采购一期条目，整体归二期），供应商/采购单/入库全部二期交付。

| 功能点 | 说明 |
|---|---|
| 供应商管理 | 供应商档案（名称/联系人/电话/地址/备注） |
| 采购单 | 创建采购单（选择供应商+商品+数量+采购价），采购单状态（待入库/已入库/已取消） |
| 采购入库 | 采购单到货入库，自动增加库存，生成库存流水 |
| 采购退货 | 退货给供应商，扣减库存（二期） |

#### 4.2.6 财务管理 `[二期]`

> 原「一期基础版」口径已移除（2026-09 裁决：§7.1 无财务一期条目，整体归二期）；1a/1b 期订单退款记录由订单管理（4.2.3）的退款字段承载，独立财务模块二期交付。

| 功能点 | 说明 |
|---|---|
| 收款记录 | 订单支付成功记录（订单号/金额/支付方式/支付时间） |
| 退款记录 | 退款单（关联订单/退款金额/退款原因/状态/时间） |
| 收支流水 | 按时间查看收入（订单收款）与支出（退款）汇总 |
| 利润核算 | 按商品/订单计算毛利（销售额-商品成本-运费），成本价取采购价或手动设置 |
| 对账单 | 按日/周/月导出对账单 Excel |
| 运费统计 | 按订单/物流公司统计运费支出 |

#### 4.2.7 售后管理 `[1b]`

| 功能点 | 说明 |
|---|---|
| 退款/退货申请 | 用户在 H5 端发起，后台待审核列表 |
| 售后审核 | 同意/拒绝退款，同意退货时填写退货地址 |
| 退货入库 | 收到退货后确认入库，库存回滚 |
| 退款执行 | 调用支付渠道退款接口，退款状态同步 |
| 售后单详情 | 完整售后流程记录（申请/审核/退货/退款/完成） |

#### 4.2.8 会员管理 `[1b]`

| 功能点 | 说明 |
|---|---|
| 用户列表 | 注册用户（头像/昵称/手机号/注册时间/订单数/消费总额） |
| 用户详情 | 用户基本信息、收货地址、订单历史、收藏 |
| 用户标签 | 给用户打标签（如"高价值""活跃""沉睡"），二期可用于精准营销 |
| 会员等级 | 一期简单等级（普通/会员），二期完善成长值体系 |

#### 4.2.9 营销管理（二期，一期预留）

| 功能点 | 说明 |
|---|---|
| 优惠券 | 满减券/折扣券/无门槛券，创建/发放/核销统计 |
| 满减活动 | 订单满 X 元减 Y 元 |
| 拼团/秒杀/分销 | 三期 |

#### 4.2.10 系统设置

| 功能点 | 说明 |
|---|---|
| 店铺设置 | 店铺名称/Logo/客服电话/营业时间/店铺公告 |
| 运费模板 | 按地区/重量/件数设置运费，包邮条件 |
| 支付配置 | 微信支付/支付宝商户号配置（证书/密钥） |
| 短信配置 | 短信服务商 API 配置 |
| OSS 配置 | 对象存储配置（AccessKey/Bucket/域名） |
| 管理员与权限 | 管理员列表、角色管理（RBAC）、菜单权限、操作日志 |
| 商城主题配置 | 全局主题（主色/导航栏/Tabbar 样式），保存后 H5 端生效（`[1b]`，见 §7.1） |
| 官网全局设置 | 见 4.4.3 节（网站基础信息/SEO/主题/动画/自定义脚本/域名）（`[二期]`，随官网低代码交付） |

---

### 4.3 商城低代码页面搭建平台 `[1b]`

> 整体为 1b 期交付（见 §7.1 / D8.1.2 第 4-5 周），1b 交付 5 个核心组件，二期补齐至 10 个。

#### 4.3.1 搭建器整体布局

三栏布局：

| 区域 | 宽度 | 功能 |
|---|---|---|
| 左侧组件库 | 240px | 组件分类折叠面板，每个组件可拖拽到画布 |
| 中间画布 | 自适应 | 顶部工具栏（页面选择/画布尺寸切换/预览/保存/撤销重做）+ 设备模型画布（移动端 375px / PC端 1200px 居中）+ 底部缩放控制 |
| 右侧属性面板 | 300px | 选中组件后显示，分"内容""样式""动画"三个 Tab（动画 Tab 仅官网组件显示） |

**画布尺寸切换**：搭建器顶部可切换"商城页面（移动端 375px）"和"官网页面（PC 端 1200px）"，切换后组件库自动切换为对应类型的组件列表。

#### 4.3.2 商城组件库（1b 期 5 个核心组件，二期补齐至 10 个）

> 1b 交付：搜索框、轮播图、商品网格、图片广告、辅助空白（对齐 D8.1.2）；其余 5 个（分类导航、商品横滑、优惠券占位、公告栏、富文本）二期补齐。

| 组件名 | 功能 | 可配置内容 |
|---|---|---|
| 搜索框 | 顶部搜索栏 | 占位文字、圆角、背景色、是否显示 |
| 轮播图 | 图片轮播 | 图片列表、高度、自动播放、指示器样式、点击跳转 |
| 图片广告 | 单张/多张图片广告 | 图片、圆角、跳转链接、上下间距 |
| 分类导航 | 图标+文字网格 | 分类数据来源、列数、图标、文字颜色 |
| 商品网格 | 商品列表网格 | 商品来源（指定分类/指定商品/热销）、展示数量、列数、是否显示价格/销量 |
| 商品横滑 | 横向滑动商品列表 | 商品来源、标题、展示数量 |
| 优惠券 | 优惠券展示区 | 优惠券列表、样式、领取按钮 |
| 公告栏 | 滚动公告文字 | 公告文字、滚动速度、图标、背景色 |
| 富文本 | 自定义文字内容 | 文字内容、字号/颜色/对齐、背景 |
| 辅助空白 | 占位间距 | 高度、背景色 |

二期扩展组件：视频、秒杀、拼团、倒计时、店铺信息、客服入口、Tabbar 自定义。

#### 4.3.3 画布交互

| 功能点 | 说明 |
|---|---|
| 拖拽添加 | 从左侧组件库拖拽组件到画布，画布显示插入位置指示线 |
| 拖拽排序 | 画布内组件上下拖拽排序，dnd-kit SortableContext 实现 |
| 选中态 | 点击组件选中，显示 2px 蓝色虚线边框+四角手柄+右上角删除/复制按钮 |
| Hover 态 | 鼠标悬停未选中组件显示 1px 灰色虚线 |
| 删除 | 选中后点删除按钮或按 Delete 键 |
| 复制 | 选中后点复制按钮，在下方插入副本 |
| 撤销/重做 | 操作历史栈，Ctrl+Z/Ctrl+Y，最多 50 步 |
| 预览 | 新窗口打开对应端真实渲染效果（商城页打开 H5 端，官网页打开官网端） |
| 保存 | 保存页面 Schema 到后端，自动保存（每 30 秒）+ 手动保存 |
| 缩放 | 画布 50%/75%/100% 缩放，CSS transform 实现 |

#### 4.3.4 属性面板

**"内容"Tab**（组件数据配置）：
- 根据组件类型动态显示配置表单
- 商品来源选择器（分类树/商品搜索/热销）
- 图片上传（直传 OSS）
- 链接选择器（商品/分类/自定义 URL/官网页面/锚点）
- 数字输入（展示数量/列数/高度）

**"样式"Tab**（组件样式配置，所有组件通用）：
- 容器样式：背景色/背景图、边框（颜色/宽度/圆角）、内边距、外边距、阴影、透明度
- 元素样式：根据组件类型显示（文字字号/字重/颜色、图片圆角/比例、按钮颜色/尺寸）
- 预设样式：每个组件提供 2-3 套预设（简约风/卡片风/渐变风），一键套用

**"动画"Tab**（仅官网组件显示）：
- 动画开关
- 动画类型选择（fade-up/zoom-in/blur-in 等 10 种）
- 高级参数（时长/延迟/距离/缓动函数/是否只播放一次）

#### 4.3.5 页面管理

| 功能点 | 说明 |
|---|---|
| 页面列表 | 已创建页面，按类型分组（商城页面/官网页面） |
| 新建页面 | 选择页面类型（商城页/官网页）、选择空白页或模板、输入页面名称 |
| 页面模板库 | 商城模板 3 套（通用/服装/生鲜）；官网模板 5 套（企业官网/产品展示/科技公司/服务机构/个人作品集） |
| 设为首页 | 商城首页和官网首页分别指定 |
| 路由配置（仅官网页） | 配置页面路由路径（如 /about、/products），见 4.4.1 节 |
| 页面删除 | 删除自定义页面（首页不可删） |
| 页面 Schema | 每个页面存为 JSON Schema，存在 PostgreSQL JSON 字段 |

---

### 4.4 官网低代码搭建（二期核心）

#### 4.4.1 多页面路由系统

| 功能 | 说明 |
|---|---|
| **页面管理** | 后台创建官网页面，每个页面配置：页面名称、路由路径（如 `/about`、`/products`、`/contact`）、页面 Schema、SEO 信息 |
| **首页设置** | 指定哪个页面作为网站首页（路由 `/`） |
| **404 页面** | 可自定义 404 页面内容 |
| **动态路由** | Next.js 用 `[slug]` 动态路由，根据后台页面 Schema 渲染；SSG 构建时生成所有静态页面 |
| **锚点导航** | 页面内组件可配置锚点 ID，导航菜单可链接到页面内锚点（如 `/#features`） |
| **路由冲突处理** | 系统保留路由（如 `/api`、`/admin`）不可占用；自定义路由唯一性校验 |
| **ISR 增量再生** | 后台修改页面后，调用 Next.js revalidate API 触发该页面重新生成，不影响其他页面 |

#### 4.4.2 导航菜单配置

| 功能 | 说明 |
|---|---|
| **顶部导航栏** | 可配置菜单项（名称、链接类型：内部页面/外部URL/锚点、打开方式：当前页/新标签）、支持多级下拉菜单（最多 2 级） |
| **导航样式** | 导航栏背景（透明/纯色/毛玻璃）、滚动时变色、Logo 位置、CTA 按钮（如"立即咨询""开始使用"） |
| **Footer 配置** | 多列链接组（每列可配置标题+链接列表）、版权信息、社交图标、备案号 |
| **移动端菜单** | 自动适配汉堡菜单，导航项在移动端折叠为抽屉 |
| **锚点跳转** | 点击导航锚点时，用 Lenis 的 `lenis.scrollTo('#anchor')` 平滑滚动，不用原生跳转 |

#### 4.4.3 全局网站设置

| 分类 | 配置项 |
|---|---|
| **基础信息** | 网站名称、Logo（桌面/移动）、favicon、默认分享图（OG Image） |
| **SEO 默认** | 默认 title 模板、默认 description、keywords、OG 标签默认值、Twitter Card、结构化数据（JSON-LD：Organization/Product/Breadcrumb） |
| **主题** | 主色、辅助色、文字色、背景色、字体（支持 Google Fonts/自定义字体 URL）、圆角基准、是否开启暗色模式、暗色模式配色 |
| **动画全局开关** | 全站动画开关、滚动动画强度（无/轻/强）、3D 效果开关（移动端自动关闭）、`prefers-reduced-motion` 自动检测 |
| **自定义脚本** | 头部 HTML（如 Google Analytics、百度统计、Google Tag Manager）、底部 HTML（客服代码、广告像素）——需做 XSS 过滤和权限控制，仅超级管理员可配置 |
| **域名** | 绑定自定义域名、SSL 证书配置、301 重定向规则 |

#### 4.4.4 单页 SEO 配置

每个低代码页面可单独覆盖全局 SEO 默认值：
- 页面 title、description、keywords
- OG title、OG description、OG image
- canonical URL
- noindex/nofollow 开关
- 结构化数据（选择页面类型：Article/Product/FAQ/Service 等，自动生成对应 JSON-LD）

**SEO 实现**：
- Next.js `generateMetadata({ params })` 中根据 slug 从 API/数据库获取页面 SEO 配置，返回 Metadata 对象
- 结构化数据（JSON-LD）在页面组件中根据页面类型注入
- 语义化 HTML：低代码组件渲染时用语义化标签（`<header>`/`<nav>`/`<main>`/`<section>`/`<article>`/`<footer>`）
- 自动生成 `sitemap.xml`（所有页面路由+最后更新时间）
- 可配置 `robots.txt`，默认允许所有爬虫

#### 4.4.5 官网组件库（二期 18 个）

所有官网组件共享 Schema 格式，额外支持 `animation` 字段。

**导航与布局类（4 个）**

| 组件 | 功能 | 可配置项 |
|---|---|---|
| **Navbar** | 顶部导航栏 | Logo、菜单数据、CTA 按钮、背景样式（透明/纯色/毛玻璃）、滚动变色、高度、固定/静态 |
| **Footer** | 页脚 | 多列链接组、版权文字、社交图标、备案号、背景色 |
| **Section** | 区块容器 | 内边距、背景色/背景图/渐变、最小高度、内容对齐、锚点 ID |
| **Divider** | 分割线 | 样式（实线/虚线/渐变）、颜色、粗细、上下间距 |

**Hero 类（3 个）**

| 组件 | 功能 | 可配置项 |
|---|---|---|
| **Hero** | 首屏大标题区 | 大标题（支持高亮词）、副标题、CTA 按钮组（1-2 个）、背景图/渐变、文字对齐、上下间距 |
| **HeroSplit** | 左右分栏 Hero | 左侧文字（标题+副标题+按钮）、右侧媒体（图片/视频/3D）、分栏比例、反转方向 |
| **Hero3D** | 3D 背景 Hero | 3D 场景类型（粒子/几何体/产品模型）、标题/副标题叠加、3D 动画参数、移动端降级图 |

**内容展示类（6 个）**

| 组件 | 功能 | 可配置项 |
|---|---|---|
| **Features** | 特性网格 | 特性列表（图标+标题+描述）、列数（2/3/4）、卡片样式、图标背景 |
| **Stats** | 统计数字 | 数字列表（数值+单位+标签+前缀）、计数动画（滚动到视口时从 0 动画到目标值）、列数、数字字号 |
| **LogoWall** | 客户/合作伙伴 Logo 墙 | Logo 列表（图片+名称+链接）、展示样式（网格/跑马灯滚动）、灰度/彩色、行数 |
| **Testimonials** | 用户评价 | 评价列表（头像+姓名+职位+公司+评价内容+评分）、展示样式（轮播/网格/跑马灯）、自动播放 |
| **Pricing** | 价格表 | 套餐列（名称+价格+周期+特性列表+CTA按钮+推荐标签）、列数、年付/月付切换 |
| **FAQ** | 常见问题 | 问题列表（问题+答案）、手风琴折叠样式、默认展开第一项、搜索框（可选） |

**媒体与交互类（3 个）**

| 组件 | 功能 | 可配置项 |
|---|---|---|
| **ImageWithText** | 图文混排 | 图片（位置左/右/上）、文字（标题+描述+按钮）、图片样式（圆角/阴影/边框）、分栏比例 |
| **ContactForm** | 联系表单 | 字段配置（姓名/邮箱/电话/公司/留言/自定义字段）、每个字段是否必填、提交按钮文字、提交后行为（显示成功提示/跳转/发送邮件通知）、后端提交到 FastAPI |
| **CTA** | 行动召唤区块 | 标题、描述、按钮（1-2 个）、背景（纯色/渐变/图片）、文字对齐 |

**3D 类（2 个，实验性，三期）**

| 组件 | 功能 | 可配置项 |
|---|---|---|
| **Hero3DBackground** | 3D 粒子/几何体背景 | 场景类型（浮动几何体/粒子网络/波浪平面/旋转环）、颜色（主色/辅助色）、粒子数量、动画速度、鼠标交互（跟随/旋转）、透明度 |
| **Product3DViewer** | 3D 产品展示 | 上传 GLB/GLTF 模型、自动旋转、拖拽旋转、缩放、背景色、环境光、热点标注（可点击弹出信息）、移动端降级为 360° 图片序列 |

#### 4.4.6 组件动画配置（所有官网组件通用）

每个官网组件的 Schema 中增加 `animation` 字段：

```json
{
  "animation": {
    "enabled": true,
    "type": "fade-up",
    "duration": 0.8,
    "delay": 0,
    "distance": 40,
    "once": true,
    "ease": "power3.out"
  }
}
```

支持的动画类型：`fade-up`、`fade-down`、`fade-left`、`fade-right`、`zoom-in`、`zoom-out`、`flip`、`slide-up`、`blur-in`、`none`。
属性面板中提供动画预设选择器（无/淡入上移/缩放/模糊）+ 高级参数（时长/延迟/距离）。

#### 4.4.7 3D 动画实验系统（三期）

**技术栈**：
- three.js r184+（WebGL/WebGPU 3D 引擎）
- @react-three/fiber v9.6+（Three.js 的 React 渲染器，v9 稳定支持 WebGPU 后端，GPU 使用降低约 40%）
- @react-three/drei v10.7+（R3F 辅助库：OrbitControls、GLTF 加载、环境光、Float 浮动等）
- @react-three/postprocessing（可选，后期处理：辉光、景深、噪点）

**3D 低代码组件实现**：
- 3D 组件通过 `componentMap` 注册到 SchemaRenderer，内部用 R3F 的 `<Canvas>` 渲染
- `<Canvas frameloop="demand">` 按需渲染，只在 props 变化或动画需要时渲染帧
- 全部 `React.lazy(() => import(...))`，首屏不加载 three.js（约 600KB），3D 组件进入视口附近时才加载
- 移动端自动降级（检测 `navigator.hardwareConcurrency < 4` 或 `deviceMemory < 4GB` 或移动端），替换为 2D 图片/视频/纯 CSS 动画
- 组件卸载时 dispose 所有 geometry、material、texture，防止内存泄漏
- WebGPU 优先：R3F v9 支持 `gl="webgpu"`，浏览器支持时自动用 WebGPU，不支持时回退 WebGL

**3D 场景预设**：

| 预设 | 效果 | 适用场景 |
|---|---|---|
| 粒子网络 | 漂浮的粒子+连线，鼠标交互 | Hero 背景、科技感页面 |
| 浮动几何体 | 多个几何体缓慢浮动旋转 | Hero、产品展示区 |
| 波浪平面 | 起伏的波浪平面，颜色渐变 | 数据/科技主题 |
| 旋转环 | 同心圆环旋转，带粒子 | 产品/技术展示 |
| GLTF 模型 | 上传自定义 3D 模型（产品/Logo） | 产品 3D 展示 |
| 3D 文字 | 3D 立体标题文字，旋转动画 | Hero 大标题 |

**3D 性能优化清单**：
- `frameloop="demand"` 按需渲染
- `dpr={[1, 2]}` 限制设备像素比（移动端 dpr 不超过 1.5）
- 几何体用 `instancedMesh`（重复物体）
- 纹理用 2 的幂尺寸（512/1024/2048），压缩为 KTX2/Basis
- 模型用 Draco 压缩的 GLB，控制在 2MB 以内
- 后期处理（辉光/景深）默认关闭，只在高性能设备开启
- 页面不可见时（`document.hidden`）暂停渲染
- 纯背景 3D Canvas 设 `style={{ pointerEvents: 'none' }}`，不拦截滚动

---

### 4.5 动画系统（三库分工）

官网对动画要求高，采用**三库分工**，各管一段，避免重复和性能浪费：

| 库 | 职责 | 为什么用它 | 包体积(gzip) |
|---|---|---|---|
| **Lenis** | 平滑滚动（momentum scrolling） | 替代原生滚动，提供丝滑的惯性滚动，和 GSAP ScrollTrigger 完美集成，不破坏 `position: sticky` | ~6KB |
| **GSAP + ScrollTrigger** | 滚动驱动动画（scrollytelling、视差、元素进入视口、数字计数、时间线动画） | 滚动动画的事实标准，性能比 Framer Motion 好（不触发 React reconciler，直接操作 DOM），2026 年所有插件免费（含 SplitText/MorphSVG） | core 23KB + ScrollTrigger 10KB |
| **Framer Motion** | UI 交互动画（弹窗、抽屉、hover、按钮点击、页面转场、AnimatePresence 退出动画） | React 声明式，和 React 状态天然集成，布局动画（layout prop）是 GSAP 做不到的 | ~34KB（tree-shaking 后按需加载可降到 ~15KB） |

**动画实现要点**：

1. **Lenis + GSAP 集成**：Lenis 的 raf 挂到 GSAP ticker 上，`gsap.ticker.add((time) => lenis.raf(time * 1000))`，`gsap.ticker.lagSmoothing(0)` 防止延迟。
2. **ScrollTrigger 与低代码渲染**：组件挂载后（`useLayoutEffect`）再注册 ScrollTrigger，组件卸载时 `ScrollTrigger.kill()` 清理；低代码页面切换时全局 `ScrollTrigger.refresh()` 重新计算位置；图片加载完成后再次 refresh。
3. **数字计数动画**：Stats 组件用 GSAP 的 `gsap.to(obj, { value: target, onUpdate })` 配合 ScrollTrigger，滚动到视口时从 0 动画到目标值。
4. **跑马灯/无限滚动**：LogoWall、Testimonials 的跑马灯用 GSAP 或 CSS animation，不用 ScrollTrigger。
5. **`prefers-reduced-motion`**：全局检测用户系统设置，开启"减少动画"时自动禁用所有非必要动画（Lenis 关闭、GSAP 动画跳过、Framer Motion 动画 duration 设 0、3D 关闭）。
6. **性能预算**：GSAP 动画只操作 `transform` 和 `opacity`（GPU 加速），不操作 `width/height/top/left`（触发重排）。

---

## 五、非功能需求

### 5.1 性能需求

| 指标 | 商城 H5 | 官网端 |
|---|---|---|
| 首屏加载 | < 2s（4G 网络） | LCP < 2.0s（桌面/移动） |
| 页面切换 | < 300ms（SPA 内路由） | < 200ms（客户端导航） |
| 接口响应 | 普通查询 < 200ms，下单 < 500ms | 表单提交 < 500ms |
| 并发能力 | 单台 4C8G 支撑 50 QPS，日订单 500 | SSG + CDN，理论无上限 |
| 低代码渲染 | 20 个组件页面渲染 < 500ms | 30 个组件页面 SSG 直出，水合 < 300ms |
| 图片加载 | 商品图懒加载，首屏图 < 200KB/张 | Next.js Image 自动压缩/WebP/AVIF，Hero 图 preload |
| Core Web Vitals | — | LCP < 2.0s, INP < 200ms, CLS < 0.1 |

### 5.2 安全需求

| 项 | 要求 |
|---|---|
| 传输安全 | 全站 HTTPS，API 强制 HTTPS |
| 认证 | JWT Token，Access Token 2 小时过期，Refresh Token 7 天 |
| 权限 | 后台 RBAC，每个 API 校验角色权限，前端菜单/按钮级权限控制；官网自定义脚本仅超级管理员可配置 |
| 支付安全 | 支付回调验签，金额校验，幂等处理 |
| 数据安全 | 敏感数据（手机号/地址）脱敏展示，密码 bcrypt 加密，数据库定时备份；官网表单数据加密存储 |
| 防刷 | 登录/下单/表单提交接口限流（Redis 计数器），短信验证码防刷 |
| XSS/CSRF | 输入过滤，输出转义；官网自定义脚本做白名单过滤 + CSP 限制脚本来源 |
| SQL 注入 | ORM 参数化查询（SQLAlchemy），禁止拼接 SQL |

### 5.3 兼容性需求

| 项 | 要求 |
|---|---|
| 商城 H5 浏览器 | iOS Safari 14+、Android Chrome 80+、微信内置浏览器、支付宝内置浏览器 |
| 官网浏览器 | Chrome 90+、Edge 90+、Safari 14+、Firefox 88+；移动端 iOS Safari 14+、Android Chrome 80+ |
| 屏幕适配 | 商城：320px-430px 宽度自适应，vw 单位，安全区域适配；官网：响应式 320px-1920px，断点（sm 640/md 768/lg 1024/xl 1280） |
| 后台浏览器 | Chrome 90+、Edge 90+、Safari 14+（后台主要 PC 使用） |
| 后台分辨率 | 支持 1280px 及以上宽度 |
| 3D 兼容 | WebGPU（Chrome/Edge/Safari 17.4+）优先，回退 WebGL；移动端自动降级为 2D |

### 5.4 可用性需求

| 项 | 要求 |
|---|---|
| 系统可用性 | 99.5%（单服务器，允许维护窗口） |
| 数据备份 | 数据库每日全量备份，保留 7 天，备份文件存 OSS；官网静态页面 CDN 多节点 |
| 错误监控 | 前端 Sentry 错误上报（商城+官网+后台），后端日志分级（ERROR/WARN/INFO） |
| 降级方案 | 支付渠道异常时提示用户稍后重试；3D 加载失败时显示降级图；官网 ISR 失败时回退到上一次成功的静态页面 |

### 5.5 官网性能与 SEO 策略

**渲染策略**：

| 页面类型 | 渲染方式 | 说明 |
|---|---|---|
| 官网所有页面 | SSG（静态站点生成） | 构建时获取所有页面 Schema，生成静态 HTML，CDN 分发 |
| 页面内容更新 | ISR（增量静态再生） | 后台修改页面后，通过 revalidate API 触发该页面重新生成 |
| 联系表单提交 | API Route（SSR） | Next.js API Route 接收表单提交，转发到 FastAPI 后端 |
| 商城 H5 | CSR（不变） | 商城端保持 Vite CSR |

**资源优化**：
- **图片**：Next.js `<Image>` 组件，自动 WebP/AVIF、响应式 `srcset`、懒加载、模糊占位；CDN 图片处理（按需裁剪/压缩）
- **字体**：`next/font/google` 或 `next/font/local`，预加载、`display: swap`、无 FOIT、自托管不依赖第三方
- **代码分割**：按路由分割；GSAP/ScrollTrigger/Lenis/three.js 全部动态导入（`import()`），只在用到的页面加载
- **3D 懒加载**：3D 组件用 `IntersectionObserver` + `React.lazy`，进入视口才加载 three.js（约 600KB）
- **动画库按需**：GSAP 只注册用到的插件，不用 SplitText/MorphSVG 就不导入
- **Schema 缓存**：页面 Schema 存 Redis，SSG 构建时从 Redis 读取，ISR revalidate 时更新缓存

---

## 六、技术栈选型

### 6.1 前端技术栈

| 分类 | 技术选型 | 版本/说明 | 适用端 |
|---|---|---|---|
| **框架** | React 18 | 函数组件 + Hooks | 商城/后台 |
| **官网框架** | Next.js 14+ App Router | SSG/ISR/API Route/RSC | 官网 |
| **构建工具** | Vite 5+ | 开发热更新快，构建快 | 商城/后台 |
| **语言** | TypeScript 5+ | 类型安全 | 全部 |
| **包管理** | pnpm + workspace | Monorepo 管理 | 全部 |
| **H5 UI 库** | Ant Design Mobile 5 | 移动端组件 | 商城 |
| **后台 UI 库** | Ant Design 5 | 企业级后台组件 | 后台 |
| **官网 UI** | Tailwind CSS v4 + shadcn/ui（可选） | 官网组件用 Tailwind 写 | 官网 |
| **样式方案** | Tailwind CSS v4 | 静态页面开发，零运行时 | 全部 |
| **动态样式** | CSS 变量 + 内联 style | 低代码运行时商家配置的样式 | 全部 |
| **状态管理** | Zustand | 轻量全局状态 | 全部 |
| **服务端状态** | React Query (TanStack Query) | API 请求缓存/重试/失效 | 商城/后台 |
| **拖拽库** | dnd-kit | 低代码搭建器拖拽排序 | 后台 |
| **不可变状态** | Immer | 低代码编辑器 Schema 不可变更新 | 后台 |
| **UI 交互动画** | Framer Motion | 弹窗/转场/hover/micro-interaction | 商城/官网 |
| **平滑滚动** | Lenis | momentum scrolling | 官网 |
| **滚动动画** | GSAP + ScrollTrigger | 滚动驱动动画，所有插件 2026 年免费 | 官网 |
| **3D** | three.js + @react-three/fiber v9 + @react-three/drei v10 | WebGPU 优先，按需渲染 | 官网（实验性） |
| **图表** | ECharts | 后台数据看板 | 后台 |
| **图标** | lucide-react | 轻量树摇图标库 | 全部 |
| **路由** | React Router 6 | 前端路由 | 商城/后台 |
| **HTTP** | Axios | 请求拦截/响应拦截/错误处理 | 商城/后台 |
| **表单** | react-hook-form + zod | 表单验证 | 官网联系表单/后台 |
| **代码规范** | ESLint + Prettier + Stylelint + Husky + lint-staged | 提交前检查 | 全部 |

### 6.2 后端技术栈

| 分类 | 技术选型 | 版本/说明 |
|---|---|---|
| **语言** | Python 3.12 | |
| **Web 框架** | FastAPI | 异步、自动 OpenAPI 文档、性能好 |
| **ORM** | SQLAlchemy 2.0 | 异步支持，类型安全 |
| **数据库迁移** | Alembic | 数据库版本管理 |
| **数据库** | PostgreSQL 16 | 支持 JSON 字段（Schema 存储）、事务强 |
| **缓存/锁** | Redis 7 | 页面缓存、购物车、分布式锁、超时队列、限流、表单去重 |
| **任务队列** | Celery + Redis（或 ARQ） | 异步任务（订单超时取消、退款、通知、ISR 页面重新生成） |
| **认证** | python-jose（JWT）+ passlib（bcrypt） | Token 认证 + 密码加密 |
| **支付** | 微信支付 SDK + 支付宝 SDK | 支付下单/回调/退款 |
| **文件存储** | 阿里云 OSS / 腾讯云 COS | 商品图/详情图/官网图片/3D 模型，服务端签名直传 |
| **短信** | 阿里云短信 / 腾讯云短信 | 登录验证码 |
| **邮件** | SMTP（或阿里云邮件推送） | 表单提交通知、售后通知 |
| **物流** | 快递100 / 快递鸟 API（二期） | 物流轨迹查询 |
| **数据校验** | Pydantic v2 | 请求/响应模型校验 |
| **日志** | loguru | 结构化日志 |
| **部署** | Docker + Docker Compose | 容器化部署 |
| **Web 服务器** | Nginx | 反向代理/HTTPS/静态资源 |
| **ASGI 服务器** | Uvicorn / Gunicorn | FastAPI 运行 |

### 6.3 基础设施

| 项 | 选型 |
|---|---|
| 云服务器 | 阿里云/腾讯云 4C8G 起步（可弹性升配） |
| 官网部署 | Vercel（推荐，SSG 性能最好）或自托管 Node.js + CDN |
| 对象存储 | 阿里云 OSS / 腾讯云 COS + CDN |
| 域名 | 商家自有域名，需 ICP 备案（商城域名 + 官网域名可分开或共用） |
| HTTPS | Let's Encrypt 免费证书或云厂商证书 |
| 监控 | Sentry（前端错误，三端统一）+ 云厂商监控（服务器） |
| 性能监控 | web-vitals（官网 Core Web Vitals 上报） |
| 日志 | 本地文件 + logrotate（一期），二期可上 ELK |

---

## 七、MVP 范围与版本规划

### 7.1 一期必须实现（P0）— 商城核心 + 商城低代码

> **分期标注（依据 D8.1 分阶段交付）**：每项末尾标注交付分期——`[1a]` = 1a 期核心交易闭环（10 周），`[1b]` = 1b 期完善与扩展（5 周）。project-radar 提取 MVP 清单时**只取 [1a] 项**（提取规则见 AGENTS.md §8.5.1），[1b] 项进入 1b 期计划，不计入 1a 期遗漏。

**H5 商城端**：
- [ ] 手机号+短信验证码登录 `[1a]`
- [ ] 首页（低代码 Schema 渲染，支持 10 个基础组件）`[1b]`（1b 交付 5 个核心组件，二期补齐至 10 个，见 D8.1.2/D8.2）
- [ ] 商品分类页、商品列表页、商品详情页 `[1a]`
- [ ] SKU 选择弹窗 `[1a]`
- [ ] 购物车（增删改查、全选、合计）`[1a]`
- [ ] 订单确认、提交订单 `[1a]`
- [ ] 微信支付 + 支付宝（H5 支付）`[1a]`
- [ ] 订单列表、订单详情 `[1a]`
- [ ] 取消订单、确认收货 `[1a]`
- [ ] 提醒发货（待发货订单催促，依赖通知中心，D6.1）`[1b]`
- [ ] 收货地址管理（增删改查、默认地址）`[1a]`
- [ ] 我的收藏 `[1b]`
- [ ] 浏览足迹（与收藏同模块）`[1b]`
- [ ] 用户中心 `[1a]`
- [ ] 搜索（关键词搜索商品）`[1a]`
- [ ] 站内消息中心（我的消息：未读/已读/按类型筛选，SSE 实时推送，D6.1.4）`[1b]`
- [ ] 商品评价（已完成订单提交评价：评分+文字+图片；商品详情页评价列表与评分摘要，D6.2）`[1b]`

**管理后台**：
- [ ] 管理员登录、RBAC 权限（角色/菜单/按钮权限）`[1a]`（1a 交付基础版；RBAC 完整版 D-04 为 1b，见 D8.1.2）
- [ ] 数据看板（核心指标+趋势图+商品排行+待办提醒）`[1a]`
- [ ] 商品分类管理 `[1a]`
- [ ] 商品 SPU/SKU 管理（增删改查、上下架）`[1a]`
- [ ] 订单管理（列表/详情/单个发货/取消/备注/改价/改地址）`[1a]`
- [ ] 订单批量发货与导出 Excel（成套批量能力）`[1b]`
- [ ] 库存管理（实时库存/锁定库存/库存流水/安全库存预警）`[1a]`
- [ ] 会员管理（用户列表/详情）`[1b]`
- [ ] 售后管理（退款审核/退货入库/退款执行）`[1b]`
- [ ] 评价审核管理（待审核/通过/拒绝、商家回复，D6.2）`[1b]`
- [ ] 商品标签与手动推荐（商品编辑页「相关推荐」配置，D6.7）`[1b]`
- [ ] 系统设置（店铺设置/运费模板/支付配置/OSS配置/短信配置）`[1a]`
- [ ] 商城主题配置（主色/导航栏/Tabbar）`[1b]`

**商城低代码搭建平台**（整体为 1b 期，见 D8.1.2 第 4-5 周）：
- [ ] 三栏布局搭建器（组件库/画布/属性面板）`[1b]`
- [ ] 10 个商城基础组件 `[1b]`（1b 交付 5 个：搜索框/轮播图/商品网格/图片广告/辅助空白）
- [ ] 拖拽添加/排序/删除/复制 `[1b]`
- [ ] 组件选中态/Hover 态 `[1b]`
- [ ] 内容配置 Tab + 样式配置 Tab `[1b]`
- [ ] 预设样式（每套组件 2-3 套）`[1b]`
- [ ] 撤销/重做（50 步）`[1b]`
- [ ] 保存/自动保存 `[1b]`
- [ ] 预览（新窗口真实渲染）`[1b]`
- [ ] 页面管理（新建/列表/设为首页/删除）`[1b]`
- [ ] 3 套商城页面模板（通用/服装/生鲜）`[1b]`
- [ ] 画布缩放（50%/75%/100%）`[1b]`

**后端**：
- [ ] 用户认证（注册/登录/Token 刷新）`[1a]`
- [ ] 商品 CRUD（SPU/SKU/分类）`[1a]`
- [ ] 订单状态机（6 个状态流转）`[1a]`
- [ ] 库存原子扣减/回滚（防超卖）`[1a]`
- [ ] 支付下单/回调（验签+幂等+金额校验）`[1a]`
- [ ] 退款接口 `[1a]`（支付退款 API 随 D8.1.1 第 5-7 周交付；售后退款审核流程为 1b）
- [ ] 购物车（Redis 存储）`[1a]`
- [ ] 低代码页面 Schema 存取 `[1b]`
- [ ] 文件上传签名（OSS 直传）`[1a]`
- [ ] 短信验证码 `[1a]`
- [ ] 订单超时自动取消（延迟队列）`[1a]`
- [ ] 操作日志 `[1a]`
- [ ] 通知中心（多渠道适配器 SMS/邮件/站内信/微信模板消息 + 模板变量 + 用户通知偏好，D-09/D6.1）`[1b]`

### 7.2 一期不做（P2，后续版本）

- 官网低代码搭建（二期）
- 3D 动画（三期实验性）
- 多租户 SaaS 化
- 小程序/APP 端
- 优惠券/拼团/秒杀/分销
- 多仓库/库存调拨/盘点单
- 采购管理（整体归二期，原「一期基础版」口径已移除，见 4.2.5）
- 财务管理（整体归二期，原「一期基础版」口径已移除，见 4.2.6）
- 会员等级/成长值/积分
- 直播
- AI 商品文案生成
- 智能补货建议
- 物流轨迹实时查询（一期只显示物流单号）

### 7.3 二期规划（P0）— 官网低代码

**官网端**：
- [ ] Next.js 官网端项目初始化（App Router + SSG/ISR）
- [ ] 多页面路由渲染（[slug] 动态路由 + generateStaticParams）
- [ ] SEO Meta 注入（generateMetadata + JSON-LD + sitemap + robots）
- [ ] 官网 SchemaRenderer（共享渲染引擎 + 官网组件注册）
- [ ] Lenis 平滑滚动 + GSAP ScrollTrigger 集成
- [ ] Framer Motion UI 交互动画
- [ ] 联系表单提交（API Route → FastAPI）
- [ ] ISR revalidate webhook（后台修改页面后触发重新生成）
- [ ] 404 页面自定义
- [ ] 响应式布局（移动端/平板/桌面）

**官网组件库（18 个）**：
- [ ] 导航与布局：Navbar、Footer、Section、Divider
- [ ] Hero：Hero、HeroSplit（Hero3D 放三期）
- [ ] 内容展示：Features、Stats（含计数动画）、LogoWall（含跑马灯）、Testimonials、Pricing、FAQ
- [ ] 媒体与交互：ImageWithText、ContactForm、CTA
- [ ] 每个组件支持动画配置（fade-up/zoom-in 等 10 种）
- [ ] 每个组件支持容器样式 + 元素样式 + 预设样式

**后台搭建器扩展**：
- [ ] 画布尺寸切换（商城移动端 375px / 官网 PC 端 1200px）
- [ ] 组件库按类型分组（商城组件 / 官网组件）
- [ ] 属性面板增加"动画"Tab（仅官网组件）
- [ ] 官网页面管理（路由路径配置 / SEO 配置 / 设为首页）
- [ ] 导航菜单配置（顶部导航 + Footer）
- [ ] 全局网站设置（基础信息 / SEO 默认 / 主题 / 动画开关 / 自定义脚本 / 域名）
- [ ] 5 套官网页面模板（企业官网/产品展示/科技公司/服务机构/个人作品集）
- [ ] 官网页面预览（新窗口打开官网端真实渲染）

**后端扩展**：
- [ ] 官网页面 CRUD（pages 表，含路由/SEO/Schema）
- [ ] 导航菜单存储（nav_menus 表）
- [ ] 网站全局设置存储（site_settings 表）
- [ ] 联系表单提交 API + 存储（form_submissions 表）+ 邮件/企微通知
- [ ] 表单反垃圾（蜜罐字段 + 限流 + 频率限制）
- [ ] ISR revalidate API（调用 Next.js revalidatePath）
- [ ] 官网表单提交后台查看/导出

### 7.4 三期规划（P1 实验性）— 3D 与高级功能

- [ ] 3D 组件：Hero3DBackground（6 种场景预设）、Product3DViewer（GLTF 模型上传 + 热点标注）
- [ ] React Three Fiber v9 + drei 集成（WebGPU 优先）
- [ ] 3D 组件低代码可配置（场景预设 + 关键参数 + 实时预览）
- [ ] 3D 性能优化（按需渲染 / dpr 限制 / 移动端降级 / 资源 dispose / 懒加载）
- [ ] 高级滚动动画（scrollytelling 时间线 / 视差 / 3D 场景滚动驱动）
- [ ] 营销工具（优惠券 / 满减 / 拼团）
- [ ] 多租户 SaaS 化
- [ ] 小程序端
- [ ] AI 商品文案生成
- [ ] 智能补货建议
- [ ] 物流轨迹实时查询

---

## 八、技术难点

### 8.1 商城端难点

#### 难点 1：订单状态机与并发控制

**问题**：订单状态多（待付款/待发货/已发货/已完成/已取消/退款中/已退款），状态流转规则复杂，并发下容易出现状态错乱（如用户同时取消订单和支付成功）。

**解决方案**：
- 定义明确的状态流转图，每个状态只能流向指定的下一个状态
- 用数据库乐观锁（`UPDATE orders SET status=:new WHERE id=:id AND status=:old`），检查影响行数，为 0 则抛出状态冲突异常
- 关键操作（支付回调、取消、发货）加 Redis 分布式锁（按订单号加锁）
- 所有状态变更记录操作日志（谁/什么时间/从什么状态到什么状态/原因）

#### 难点 2：库存超卖

**问题**：高并发下多个用户同时下单同一 SKU，先查库存再扣减会导致超卖。

**解决方案**：
- 原子扣减：`UPDATE sku SET stock = stock - :qty WHERE id = :id AND stock >= :qty`，检查影响行数
- 库存分三层：可用库存（展示）、锁定库存（已下单未发货）、实际库存
- 下单时锁定库存（stock-=qty, locked+=qty），发货时扣减锁定库存（locked-=qty），取消时回滚（stock+=qty, locked-=qty）
- 所有库存变动生成库存流水，可追溯
- 高并发场景用 Redis 预扣库存 + 异步落库（一期日订单 500，数据库原子扣减足够）

#### 难点 3：支付回调幂等与验签

**问题**：微信/支付宝回调可能重复发送，伪造回调，回调金额与订单金额不一致。

**解决方案**：
- 验签：用平台公钥验证回调签名，验签失败直接拒绝
- 幂等：回调处理前先查订单状态，已支付则直接返回成功，不重复处理
- 金额校验：回调金额必须与订单金额一致，不一致记录异常并人工处理
- 用订单号做幂等键，加唯一索引
- 回调处理用数据库事务，要么全成功要么全回滚
- 主动查询：支付后前端轮询+后端定时主动查单（防止回调丢失）

#### 难点 4：低代码 Schema 设计与动态渲染（所见即所得）

**问题**：后台搭建器中拖拽的组件，H5 端必须完全一致地渲染；组件的内容配置和样式配置要能序列化存储并动态还原。

**解决方案**：
- 统一 Schema 格式：`{ pageStyle, components: [{ type, props, style, elementStyle }] }`
- 共享组件库：H5 端和后台画布用**同一套 React 组件**（放在 shared-components 包），保证渲染一致
- SchemaRenderer：遍历 components 数组，根据 type 从 componentMap 查找组件，注入 props/style/elementStyle 渲染
- 容器样式用外层 div 内联 style，元素样式用组件内部 style 属性或 CSS 变量
- 全局样式用 CSS 变量，商家改主题时 setProperty 全站生效
- 画布中渲染时加一层选中态包装（不影响组件本身）

#### 难点 5：SPU/SKU 数据模型

**问题**：商品规格（颜色/尺寸/容量）组合生成 SKU，规格可自定义，SKU 关联价格/库存/编码，模型设计错了后面全返工。

**解决方案**：
- SPU 表：商品基础信息（名称/分类/主图/详情/品牌）
- 规格表：规格定义（规格名，如"颜色"），规格值表（规格值，如"红色"）
- SPU 规格关联表：SPU 关联哪些规格
- SKU 表：每个规格组合对应一条 SKU（SPU_ID + 规格值组合 JSON + 价格 + 库存 + SKU编码 + 重量）
- 规格值组合用 JSON 字段存储，同时存一个唯一索引（SPU_ID + 规格值排序后的哈希）防重复
- 购物车/订单/库存全部按 SKU 维度操作

#### 难点 6：低代码组件的样式配置体系

**问题**：商家要能配置每个组件的样式，但配置项太多商家会懵，太少又不够用；动态样式不能用 Tailwind 类。

**解决方案**：
- 四层样式继承：全局 CSS 变量 → 页面级 → 组件级容器 → 元素级
- 组件级容器样式统一（所有组件都有：背景/边框/圆角/内边距/外边距/阴影/透明度），在属性面板"样式"Tab 统一配置
- 元素级样式由每个组件自己定义，只暴露最常用的 3-5 个配置项
- 预设样式：每个组件 2-3 套预设，一键套用，降低商家配置门槛
- 动态样式全部用内联 style 或 CSS 变量，**不用 Tailwind 动态类**（JIT 扫描不到）
- 元素级默认值用 CSS 变量，商家改全局主色时自动跟随

#### 难点 7：移动端性能优化

**问题**：H5 商城首屏加载慢、商品列表滚动卡顿、图片加载导致布局抖动。

**解决方案**：
- 首屏：路由懒加载 + 组件懒加载，首屏只加载首页必要组件
- 图片：WebP 格式 + CDN 按需裁剪 + 懒加载 + 固定宽高比占位防布局抖动
- 列表：商品列表超过 50 个用虚拟滚动（react-window），上拉加载分页
- 状态：React Query 缓存 API 结果，避免重复请求
- 构建：Vite 构建优化，代码分割，Tree Shaking，gzip/brotli 压缩
- 低代码页面：Schema 缓存到 Redis，组件用 React.memo 优化重渲染
- 动画：Framer Motion 用 transform/opacity（GPU 加速）

#### 难点 8：金额精度

**问题**：float 计算导致金额误差，财务对账对不上。

**解决方案**：
- 数据库用 `NUMERIC(12,2)`（精确到分）
- Python 用 `Decimal` 类型，所有金额计算用 Decimal
- 四舍五入用 `ROUND_HALF_UP`
- 优惠计算、运费、退款都用 Decimal，最后统一保留 2 位
- 订单金额 = 商品总价 + 运费 - 优惠，计算后与前端传的金额校验，不一致拒绝下单

#### 难点 9：后台 RBAC 权限

**问题**：多角色（超级管理员/运营/财务/仓管），不同角色看到不同菜单和按钮，数据权限不同。

**解决方案**：
- RBAC 模型：用户 → 角色 → 权限（菜单权限 + 按钮权限 + 数据权限）
- 菜单权限：前端路由表根据角色过滤，后端每个 API 校验角色
- 按钮权限：前端用自定义组件 `<Permission code="order:delete">` 控制按钮显示，后端 API 同样校验
- 数据权限：**二期预留**（基于 D1.4.5 数据范围字段设计，一期只做菜单/按钮权限，见 D8.2）
- 操作日志：所有写操作记录（谁/什么时间/操作了什么/修改前后值）

#### 难点 10：订单超时自动取消

**问题**：用户下单后 30 分钟未支付，需要自动取消订单并回滚库存，不能漏也不能重复取消。

**解决方案**：
- 下单时往 Redis 有序集合（ZSET）写入 `{订单号: 过期时间戳}`
- 后台定时任务（每分钟）扫描 ZSET 中过期的订单，执行取消（回滚库存+标记已取消；优惠券释放为二期后增加，见 D6.6 预留）
- 取消操作加分布式锁（按订单号），防止定时任务和用户手动取消并发
- 取消后从 ZSET 删除
- 支付回调时先检查订单是否已取消，已取消则退款
- 一期可用 Celery/ARQ 的延迟任务，或简单的 APScheduler 定时扫描

### 8.2 官网端难点

#### 难点 11：低代码 Schema 在 Next.js SSG 中的渲染

**问题**：商城端的 SchemaRenderer 是 CSR 的（运行时从 API 拉 Schema 渲染），官网端需要 SSG（构建时生成静态 HTML），两者渲染时机不同。

**解决方案**：
- SchemaRenderer 组件本身是构建无关的（纯 React 组件，接收 schema prop 渲染），CSR 和 SSG 都能用
- SSG 时：Next.js `generateStaticParams` 获取所有页面 slug，`generateMetadata` 生成 SEO，页面组件在构建时通过 API（或直接读数据库）获取 Schema，传给 SchemaRenderer 渲染为静态 HTML
- ISR 时：后台修改页面后调用 Next.js revalidate API，触发该页面重新构建
- 客户端水合后：Schema 不变，直接复用静态 HTML，不需要重新拉取

#### 难点 12：共享组件库同时支持 Vite 和 Next.js

**问题**：shared-components 包同时被商城端（Vite）和官网端（Next.js）引用，需要确保组件不依赖特定构建 API。

**解决方案**：
- 共享组件只依赖 React 和通用库，不直接用 `import.meta.env`（Vite）或 `process.env`（Next.js），环境变量通过 props 传入
- 包用 TypeScript 编写，构建为 ESM + CJS 双格式（tsup 或 rollup），两个构建工具都能消费
- CSS（Tailwind）通过共享的 `shared-tokens` 包注入，两个端都引入同一个 CSS 文件
- 3D 组件单独打包（`@liteshop/3d-components`），只在官网端引用，不污染商城端包体积

#### 难点 13：3D 组件的低代码可配置性

**问题**：3D 场景参数多，全部暴露给商家配置会 overwhelm，且 3D 场景调试需要实时预览。

**解决方案**：
- 提供 5-6 个预设场景，商家选预设后只暴露 3-5 个关键参数（颜色、粒子密度、动画速度、鼠标交互开关）
- 后台画布中 3D 组件用真实 R3F Canvas 实时预览（和官网端一致），不是模拟图
- 高级参数（光照、相机、后期处理）折叠在"高级"Tab，默认不展示
- GLTF 模型上传后自动生成缩略图，支持在画布中拖拽旋转预览

#### 难点 14：滚动动画与低代码动态渲染的时机

**问题**：GSAP ScrollTrigger 需要在 DOM 渲染完成后计算元素位置，但低代码页面是动态渲染的（Schema 异步加载后才渲染组件），时机不对会导致 ScrollTrigger 计算位置错误。

**解决方案**：
- 页面 Schema 加载完成、所有组件挂载后，统一调用 `ScrollTrigger.refresh()` 重新计算所有触发点
- 图片加载完成后（`window.onload` 或图片 `onLoad`）再次 `ScrollTrigger.refresh()`（图片加载会改变页面高度）
- 用 `useLayoutEffect`（不是 useEffect）注册 ScrollTrigger，确保在浏览器绘制前完成
- 低代码页面切换时（客户端路由），先 `ScrollTrigger.killAll()` 清理旧页面的触发器，新页面渲染后再注册
- Lenis 初始化在 `useEffect` 中，确保 DOM 已挂载

#### 难点 15：SEO Meta 与低代码页面的结合

**问题**：每个低代码页面的 SEO 配置存在数据库 Schema 里，Next.js 需要在构建时（SSG）获取这些配置生成 Meta 标签。

**解决方案**：
- Next.js `generateMetadata({ params })` 中根据 slug 从 API/数据库获取页面 SEO 配置，返回 Metadata 对象
- 全局默认 SEO 存在 site_settings 表，页面级 SEO 覆盖全局默认
- 结构化数据（JSON-LD）在页面组件中根据页面类型注入
- OG Image 支持自动生成（用 `@vercel/og` 或后端 puppeteer 截图），商家也可上传自定义 OG 图

#### 难点 16：性能预算平衡（3D + GSAP + 低代码渲染）

**问题**：官网同时有 3D（600KB+）、GSAP（33KB）、Lenis（6KB）、低代码渲染，首屏性能容易超标。

**解决方案**：
- 首屏关键内容（Hero 文字+按钮+首屏图片）SSG 直出 HTML，不依赖 JS
- 3D 组件：`React.lazy` + IntersectionObserver，进入视口才加载；移动端直接降级为 2D，不加载 three.js
- GSAP/ScrollTrigger/Lenis：动态导入，只在有动画的页面加载
- 低代码 Schema：SSG 时已渲染为 HTML，客户端水合只做事件绑定
- 图片：Next.js Image 自动压缩+懒加载，Hero 图 preload
- 字体：next/font 自托管，`display: swap`
- 性能监控：用 `web-vitals` 库上报 Core Web Vitals，后台看板展示

#### 难点 17：联系表单的后端处理与反垃圾

**问题**：官网联系表单提交到后端，需要存储、通知商家、防垃圾提交（机器人刷表单）。

**解决方案**：
- 后端 FastAPI API 接收表单提交，校验（zod），存 form_submissions 表
- 通知：提交后发邮件给商家（SMTP）或发飞书/企微 webhook（可配置通知方式）
- 反垃圾：① 蜜罐字段（隐藏字段，机器人会填）② 提交频率限制（Redis 限流，同一 IP/邮箱 1 分钟 1 次）③ 可选 Turnstile/reCAPTCHA v3 ④ 内容检测（敏感词过滤）
- 后台可查看表单提交列表、标记已读/已处理、导出 Excel

---

## 九、坑点与风险

### 9.1 技术坑点

| # | 坑点 | 说明 | 规避方案 |
|---|---|---|---|
| 1 | **低代码动态样式用 Tailwind 类** | Tailwind v4 JIT 只扫描源码，商家运行时配置的 `bg-[#ff6b6b]` 不会被编译 | 动态样式用 CSS 变量或内联 style，静态页面才用 Tailwind 类 |
| 2 | **antd 与 Tailwind preflight 冲突** | Tailwind 的 preflight 会重置 button/input 等元素样式，导致 antd 组件样式异常 | `tailwind.config.js` 中 `corePlugins: { preflight: false }`，或用 CSS 层隔离 |
| 3 | **移动端滚动穿透** | 弹窗（SKU 抽屉）打开时，背景页面还能滚动 | 弹窗打开时给 body 加 `overflow:hidden` + `position:fixed`，记录 scrollTop，关闭时恢复 |
| 4 | **1px 边框高清屏发虚** | `border:1px` 在 2x/3x 屏上显得粗，`0.5px` 在部分安卓机不显示 | 用伪元素 `::after` + `transform:scaleY(0.5)` 实现真 1px，或统一用 1px 接受视觉差异 |
| 5 | **安全区域适配遗漏** | 底部固定栏在 iPhone 刘海屏被底部横条遮挡 | 封装 `<SafeAreaBottom>` 组件，所有底部固定栏加 `padding-bottom: env(safe-area-inset-bottom)` |
| 6 | **图片加载布局抖动（CLS）** | 商品图无固定高度，加载时把下方内容顶下去 | 所有商品图容器用 `aspect-[4/3]` 固定宽高比 + 灰色占位，图片 absolute 填充 |
| 7 | **支付回调重复通知** | 微信/支付宝会多次发送回调，重复处理导致重复发货 | 回调先查订单状态，已处理直接返回成功；用订单号做幂等键加唯一索引 |
| 8 | **库存并发超卖** | 先查库存再判断再扣减，并发下两个请求都查到有库存 | 用原子 SQL `UPDATE ... SET stock=stock-:qty WHERE stock>=:qty`，检查影响行数 |
| 9 | **金额用 float** | float 精度丢失，财务对账对不上 | 数据库 NUMERIC(12,2)，Python Decimal，统一 ROUND_HALF_UP |
| 10 | **订单超时取消遗漏** | 定时任务漏扫，订单一直待付款占库存 | Redis ZSET 存过期时间，每分钟扫描；支付时主动检查订单状态 |
| 11 | **数据库不备份** | 服务器故障/误操作导致数据丢失 | 每日 pg_dump 全量备份，存 OSS，保留 7 天，定期验证备份可恢复 |
| 12 | **低代码画布与端渲染不一致** | 画布中写了一套模拟组件，端是另一套，样式/行为不一致 | 共享组件库，画布和端用同一套组件，画布只加选中态包装层 |
| 13 | **大量内联样式性能差** | 低代码页面 20 个组件都有内联 style，React 重渲染时重新计算样式 | 全局样式用 CSS 变量，组件 style 用 useMemo 缓存，元素级只在配置了才注入 |
| 14 | **微信支付/支付宝资质不全** | 个人无法申请微信支付商户号，需要个体户/企业资质 | 提前注册个体工商户，申请微信支付商户号和支付宝商家号，配置回调域名 |
| 15 | **CORS 跨域** | 前后端分离部署，前端请求后端 API 被浏览器拦截 | FastAPI 配置 CORS 中间件，允许指定域名，生产环境不允许 `*` |
| 16 | **OSS 直传签名泄露** | 前端直传 OSS 用永久 AccessKey，泄露后被滥用 | 后端生成临时签名（STS Token 或 PostObject 签名），前端用临时凭证上传 |
| 17 | **短信验证码被刷** | 攻击者批量调用短信接口，产生大量短信费用 | 接口限流（Redis 计数器），图形验证码，IP 限流 |
| 18 | **商品详情图过长加载慢** | 详情页长图几 MB，加载慢 | 详情图切片上传，CDN 压缩，懒加载，限制单张图大小 |
| 19 | **SKU 规格组合爆炸** | 商品规格太多（5 色 × 10 尺寸 = 50 SKU），管理困难 | 限制规格数量（最多 3 个规格，每个规格最多 20 个值），SKU 批量编辑 |
| 20 | **退款与库存回滚不一致** | 退款成功但库存没回滚，或库存回滚了但退款失败 | **支付渠道退款调用必须在 DB 事务外**：①事务内更新售后单为 REFUNDING 并提交；②事务外调用渠道退款 API；③成功→事务内落 REFUNDED + 库存回滚，失败→落 REFUND_FAILED 重试任务（对齐 D1.1.2 状态机，禁止在事务内同步等渠道返回——渠道超时会长时间持有行锁） |
| 21 | **GSAP 在 Next.js SSR 中 hydration 错误** | GSAP 操作 DOM，SSR 时没有 window，直接导入会报错 | 所有 GSAP 代码用 `dynamicImport(() => import('gsap'), { ssr: false })`，在 `useEffect` 中注册 |
| 22 | **R3F/Three.js 在 SSR 中 window 未定义** | Three.js 依赖 window/document，SSR 构建时报错 | 3D 组件全部 `React.lazy` + `ssr: false`，只在客户端渲染 |
| 23 | **Lenis 与 CSS position:sticky 冲突** | Lenis 用 transform 模拟滚动，会导致 sticky 元素不跟随 | Lenis 官方已修复 sticky 支持，需确保初始化在 sticky 元素渲染后 |
| 24 | **ScrollTrigger 与 React 严格模式双重挂载** | React 18 严格模式下组件挂载两次，ScrollTrigger 注册两次 | 开发环境关闭严格模式，或在 `useEffect` cleanup 中 `ScrollTrigger.kill()` |
| 25 | **3D 资源未 dispose 导致内存泄漏** | 低代码页面切换/组件卸载时，Three.js 资源未释放 | 组件卸载时遍历 scene dispose 所有资源；自定义 GLTF 模型需手动 traverse dispose |
| 26 | **移动端 3D 性能差/发热/耗电** | 移动端 GPU 弱，3D 场景持续渲染导致发热、FPS 骤降 | 移动端自动降级（检测 hardwareConcurrency/deviceMemory/UA），`frameloop="demand"`，dpr 限制 1.5 |
| 27 | **低代码页面 Schema 更新后 SSG 缓存未失效** | 后台改了页面内容，但 Next.js SSG 页面还是旧的 | 后台保存页面后调用 Next.js `revalidatePath('/slug')`；设置合理 revalidate 间隔兜底 |
| 28 | **自定义脚本注入 XSS 风险** | 商家在"自定义 HTML/JS"中注入恶意脚本 | 自定义脚本只允许超级管理员配置；白名单过滤；CSP 限制脚本来源 |
| 29 | **字体加载导致布局偏移（FOUT/FOIT）** | 自定义字体加载慢，文字先显示系统字体再切换 | 用 `next/font`（自动预加载、`display: swap`、`adjustFontFallback`）；字体自托管 |
| 30 | **GSAP 动画与 prefers-reduced-motion** | 用户系统开启"减少动画"时，动画仍在播放 | 全局检测 `matchMedia('(prefers-reduced-motion: reduce)')`，开启时禁用所有非必要动画 |
| 31 | **导航菜单锚点跳转与 Lenis 冲突** | 点击导航锚点，Lenis 平滑滚动和原生锚点跳转冲突 | 用 Lenis 的 `lenis.scrollTo('#anchor')` 方法跳转，不用原生 `href="#id"` |
| 32 | **GLTF 模型跨域/加载失败** | 模型文件存在 OSS，CORS 配置不当导致 Three.js 加载失败 | OSS 配置 CORS 允许来源；模型用 GLB（单文件）；加载失败时显示降级图 |
| 33 | **联系表单重复提交** | 用户双击提交按钮或网络慢时重复提交 | 提交后按钮禁用+loading；后端幂等校验（同一邮箱+同一内容 5 分钟内去重） |
| 34 | **Next.js 与 Vite 共享组件的 CSS 冲突** | 两个端的 Tailwind 配置/antd 主题不一致，共享组件样式错乱 | shared-tokens 包统一定义 Design Tokens，两个端都从 shared-tokens 引入 |

### 9.2 业务与合规风险

| # | 风险 | 说明 |
|---|---|---|
| 1 | **ICP 备案** | 商城和官网在国内运营必须 ICP 备案，备案期间网站不可访问 |
| 2 | **支付资质** | 微信支付/支付宝需要个体户或企业资质，个人无法申请 |
| 3 | **电商合规** | 需要用户协议、隐私政策、退换货政策，符合《电子商务法》《消费者权益保护法》 |
| 4 | **数据安全** | 用户手机号/地址/表单数据属于个人信息，需脱敏存储，符合《个人信息保护法》 |
| 5 | **发票** | 商家需要给消费者提供发票，系统需支持发票信息记录 |
| 6 | **经营范围** | 销售特定商品（食品/医疗器械/化妆品），需要相应经营资质 |
| 7 | **3D/动画版权** | 使用第三方 3D 模型、纹理、HDR 环境图需注意版权，商用需授权 |
| 8 | **字体版权** | 自定义字体（如方正、汉仪）商用需购买授权，免费字体（思源、Noto）可商用 |
| 9 | **SEO 内容合规** | 官网内容需真实，不得虚假宣传，符合《广告法》 |
| 10 | **表单数据隐私** | 联系表单收集的个人信息需在隐私政策中说明用途 |

### 9.3 项目管理风险

| # | 风险 | 说明 |
|---|---|---|
| 1 | **范围蔓延** | 低代码+ERP+商城+官网+3D 五个系统叠加，功能容易无限膨胀 | 严格按 MVP 范围执行，二期/三期功能不进入一期 |
| 2 | **低代码搭建器耗时超预期** | 拖拽编辑器+属性面板+组件库开发量大 | 1b 期只做 5 个核心商城组件（二期补齐至 10 个，见 4.3.2），用 dnd-kit 减少自研拖拽成本；官网组件二期做 |
| 3 | **支付联调耗时** | 微信支付/支付宝沙箱环境配置复杂，回调调试耗时 | 提前申请商户号，先用沙箱联调，预留 1 周支付联调时间 |
| 4 | **3D 学习曲线陡** | React Three Fiber + Three.js 学习成本高，调试困难 | 3D 放三期，先做 2D 动画；3D 从简单预设场景（粒子/几何体）开始，不做复杂自定义模型 |
| 5 | **单人开发精力分散** | H5+官网+后台+后端+低代码五端并行，单人容易顾此失彼 | 按优先级串行开发：先后端核心 API → 再 H5 交易流程 → 再后台管理 → 再商城低代码 → 最后官网+3D |
| 6 | **Next.js 学习成本** | 开发者熟悉 Vite，Next.js App Router/RSC/SSG/ISR 有学习曲线 | 官网端二期才做，一期有时间学习；从简单 SSG 页面开始，逐步引入 ISR/RSC |

---

## 十、开发排期

### 10.1 一期（商城核心 + 商城低代码）— 约 15 周

| 阶段 | 周期 | 交付内容 |
|---|---|---|
| **第 1 阶段：基础架构** | 第 1-2 周 | Monorepo 初始化、shared-tokens、后端项目骨架（FastAPI+SQLAlchemy+PostgreSQL+Redis）、数据库设计、用户认证、OSS 上传签名、短信验证码 |
| **第 2 阶段：商品与库存** | 第 3-4 周 | 商品分类/SPU/SKU 管理（后台+API）、库存管理（实时库存/锁定库存/库存流水/原子扣减）、H5 商品分类/列表/详情页、SKU 选择弹窗、搜索 |
| **第 3 阶段：交易闭环** | 第 5-7 周 | 购物车（H5+Redis）、订单确认/提交、订单状态机、微信支付+支付宝（下单+回调+退款）、订单列表/详情、取消订单/确认收货、收货地址管理、订单超时自动取消 |
| **第 4 阶段：后台 ERP** | 第 8-10 周 | 数据看板、订单管理、售后管理、会员管理、系统设置、RBAC 权限、操作日志 |
| **第 5 阶段：商城低代码搭建器** | 第 11-13 周 | 三栏布局搭建器、dnd-kit 拖拽、10 个商城基础组件（shared-components）、属性面板（内容+样式）、预设样式、撤销重做、保存/自动保存、预览、页面管理、3 套模板、商城主题配置、H5 SchemaRenderer |
| **第 6 阶段：优化与上线** | 第 14-15 周 | 性能优化（首屏/图片/列表虚拟滚动）、兼容性测试、支付正式环境联调、部署（Docker Compose+Nginx+HTTPS）、数据备份、错误监控（Sentry）、文档编写 |

### 10.2 二期（官网低代码）— 约 6-8 周（在一期完成后）

| 阶段 | 周期 | 交付内容 |
|---|---|---|
| **官网端基础** | 第 1-2 周 | Next.js 项目初始化、App Router 结构、SSG/ISR 配置、官网 SchemaRenderer、Lenis+GSAP 集成、SEO 基础（generateMetadata/sitemap/robots） |
| **官网组件库** | 第 3-4 周 | 18 个官网组件（导航/Hero/内容展示/媒体交互）、每个组件动画配置、预设样式、响应式适配 |
| **后台搭建器扩展** | 第 5-6 周 | 画布尺寸切换、官网组件注册、动画 Tab、官网页面管理（路由/SEO）、导航菜单配置、全局网站设置、5 套官网模板、联系表单后台 |
| **后端扩展 + 优化上线** | 第 7-8 周 | 官网页面 CRUD、导航/设置存储、表单提交 API+反垃圾+通知、ISR webhook、性能优化（Core Web Vitals）、部署（Vercel/自托管+CDN）、测试上线 |

### 10.3 三期（3D + 高级功能）— 约 4-6 周（实验性）

| 阶段 | 周期 | 交付内容 |
|---|---|---|
| **3D 基础** | 第 1-2 周 | React Three Fiber + drei 集成、WebGPU 优先、按需渲染、3D 组件懒加载+移动端降级 |
| **3D 低代码组件** | 第 3-4 周 | Hero3DBackground（6 种预设）、Product3DViewer（GLTF 上传+热点标注）、后台 3D 配置面板+实时预览 |
| **高级动画 + 优化** | 第 5-6 周 | scrollytelling 时间线、3D 场景滚动驱动、性能优化、测试上线 |

---

## 十一、附录

### 11.1 术语表

| 术语 | 说明 |
|---|---|
| SPU | Standard Product Unit，标准化产品单元，如"iPhone 15" |
| SKU | Stock Keeping Unit，库存量单位，如"iPhone 15 黑色 128G" |
| Schema | 低代码页面的 JSON 描述，包含页面样式和组件列表 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |
| OSS | Object Storage Service，对象存储服务 |
| SSG | Static Site Generation，静态站点生成（构建时生成 HTML） |
| ISR | Incremental Static Regeneration，增量静态再生（运行时重新生成特定页面） |
| SSR | Server-Side Rendering，服务端渲染 |
| CSR | Client-Side Rendering，客户端渲染 |
| RSC | React Server Components，React 服务端组件 |
| R3F | React Three Fiber，Three.js 的 React 渲染器 |
| LCP | Largest Contentful Paint，最大内容绘制（Core Web Vitals 指标） |
| INP | Interaction to Next Paint，交互到下一次绘制（Core Web Vitals 指标） |
| CLS | Cumulative Layout Shift，累积布局偏移（Core Web Vitals 指标） |
| JSON-LD | JSON for Linking Data，结构化数据格式，用于 SEO |
| OG | Open Graph，社交分享标签协议 |

### 11.2 核心数据表清单

**商城相关（一期）**：

| 表名 | 说明 |
|---|---|
| users | 用户表（H5 端消费者） |
| admins | 管理员表（后台） |
| roles | 角色表 |
| permissions | 权限表（菜单/按钮） |
| categories | 商品分类表 |
| products | 商品 SPU 表 |
| product_specs | 商品规格定义表 |
| product_spec_values | 规格值表 |
| skus | SKU 表（价格/库存/编码） |
| stock_logs | 库存流水表 |
| carts | 购物车表（或 Redis） |
| orders | 订单主表 |
| order_items | 订单商品明细表 |
| order_logs | 订单操作日志表 |
| payments | 支付记录表 |
| refunds | 退款单表 |
| after_sales | 售后单表 |
| addresses | 收货地址表 |
| favorites | 收藏表 |
| pages | 低代码页面表（商城页 + 官网页，含 Schema JSON） |
| page_templates | 页面模板表 |
| themes | 商城主题配置表 |
| suppliers | 供应商表（二期完善） |
| purchase_orders | 采购单表（二期完善） |
| settings | 系统设置表（键值对） |
| operation_logs | 操作日志表 |

**官网相关（二期新增）**：

| 表名 | 说明 |
|---|---|
| site_settings | 官网全局设置表（基础信息/SEO/主题/动画/脚本/域名） |
| nav_menus | 导航菜单表（顶部导航 + Footer） |
| form_submissions | 官网表单提交记录表 |
| form_notifications | 表单通知配置表 |

### 11.3 参考项目与技术资源

| 项目/资源 | 说明 | 参考点 |
|---|---|---|
| CRMEB | PHP 开源商城 | 低代码 DIY 装修、营销功能 |
| 萤火商城 2.0 | PHP 开源商城 | 轻量架构、H5+小程序 |
| Builder.io | React 可视化页面搭建 | 组件驱动低代码、Next.js 集成 |
| H5-Dooring | H5 页面可视化搭建平台 | 低代码编辑器架构 |
| v0.dev (Vercel) | AI 生成 React 页面 | shadcn/ui + Tailwind + Next.js 技术栈参考 |
| antd-mobile | React 移动端 UI 库 | H5 端组件 |
| dnd-kit | React 拖拽库 | 低代码拖拽排序 |
| FastAPI | Python 异步 Web 框架 | 后端 |
| Next.js App Router | React 全栈框架 | 官网 SSG/ISR/SEO |
| Lenis | 平滑滚动库 | 官网滚动体验 |
| GSAP + ScrollTrigger | 动画库 | 官网滚动动画 |
| React Three Fiber + drei | React 3D 库 | 官网 3D 效果 |
| shadcn/ui | 可定制 React 组件库 | 官网组件基础 |
| Tailwind CSS v4 | 原子化 CSS 框架 | 样式方案 |

---

---

# 附录 D：详细设计（v1.2 增补）

> 以下为 v1.2 评审后增补的详细设计。若与 v1.1 主体冲突，以附录 D 为准。

---

## D1. 数据模型详细设计

### D1.1 订单与售后状态机拆分（D-01）

#### D1.1.1 订单正向状态机

`orders.status` 只承担正向交易流转，共 5 个状态：

```
PENDING_PAYMENT ──支付成功──→ PAID ──发货──→ SHIPPED ──确认收货──→ COMPLETED
      │                                               ↑
      └──超时/手动取消──→ CANCELLED                    │
      └──支付前取消──→ CANCELLED                       │
```

| 状态 | 枚举值 | 含义 | 允许流转到 |
|---|---|---|---|
| 待付款 | `PENDING_PAYMENT` | 订单创建，等待支付 | PAID, CANCELLED |
| 已支付 | `PAID` | 支付成功，等待发货 | SHIPPED, CANCELLED（仅后台关闭） |
| 已发货 | `SHIPPED` | 已发货，等待收货 | COMPLETED |
| 已完成 | `COMPLETED` | 交易完成 | —（不可流转，售后走 after_sales） |
| 已取消 | `CANCELLED` | 订单取消，库存已回滚 | —（终态） |

**并发保护**：每次状态变更执行 `UPDATE orders SET status=:new WHERE id=:id AND status=:old`，检查影响行数为 1，否则抛 `ORDER_STATUS_CONFLICT`。

**冗余字段**：`orders.refund_status` 记录售后状态快照（仅用于 UI 展示，不参与状态机流转）：

| refund_status | 含义 |
|---|---|
| `NONE` | 无售后 |
| `APPLYING` | 有进行中售后申请 |
| `PARTIAL_REFUNDED` | 部分退款成功 |
| `REFUNDED` | 全额退款成功 |

#### D1.1.2 售后独立状态机

`after_sales.status` 独立流转，共 7 个状态：

```
PENDING_REVIEW ──同意──→ APPROVED ──买家退货──→ GOODS_RETURNED ──退款──→ REFUNDING ──退款成功──→ REFUNDED
      │                                                                                ↑
      └──拒绝──→ REJECTED                                               退款失败──→ REFUND_FAILED
                                                                                    │
                                                                      ┌─────────────┘
                                                                      ↓
                                                                   重试──→ REFUNDING
      └──买家超时未退货──→ CLOSED
      └──买家撤回──→ CLOSED
```

| 状态 | 枚举值 | 含义 | 允许流转到 |
|---|---|---|---|
| 待审核 | `PENDING_REVIEW` | 用户提交售后申请 | APPROVED, REJECTED, CLOSED |
| 已同意 | `APPROVED` | 后台同意，等待买家退货 | GOODS_RETURNED, CLOSED |
| 已拒绝 | `REJECTED` | 后台拒绝 | —（终态） |
| 已退货 | `GOODS_RETURNED` | 买家已寄回商品 | REFUNDING |
| 退款中 | `REFUNDING` | 调用支付渠道退款 | REFUNDED, REFUND_FAILED |
| 已退款 | `REFUNDED` | 退款成功 | —（终态） |
| 退款失败 | `REFUND_FAILED` | 退款接口失败 | REFUNDING（重试） |
| 已关闭 | `CLOSED` | 超时/撤回 | —（终态） |

**售后类型**：

| type | 含义 |
|---|---|
| `REFUND_ONLY` | 仅退款（未发货/已发货未收货） |
| `REFUND_AND_RETURN` | 退货退款（已收货） |
| `EXCHANGE` | 换货（二期） |

**与订单的关联**：
- 一个订单可以有多次售后（如部分退款），每次售后关联 `order_items` 子集
- 售后金额 ≤ 关联 order_items 实付总额
- 售后成功后库存回滚（`stock_logs.source_type = 'after_sale'`）
